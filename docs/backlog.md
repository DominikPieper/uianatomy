# Backlog

Persistente Arbeitsliste aus dem Senior UX/UI + Senior Frontend Review (2026-04-29). Quelle: `~/.claude/plans/mache-ein-detailliertes-review-cheeky-cocke.md`.

Status-Legende: `[ ]` offen · `[~]` in Arbeit · `[x]` erledigt

---

## P0 — sofort, blockiert Phase 2

- [x] **P0-1 Tabs nachziehen** — data states erweitert (`busy`, `lazy`, `error`), 2 mistakes ergänzt (lazy-panel-no-aria-busy, overflow-no-scroll-into-view, indicator-not-rtl-aware), 1 mismatch ergänzt (overflow wrap vs scroll). Erledigt 2026-04-29. Datei: `content/components/tabs.yaml`.
- [x] **P0-2 Mindesttiefe-Checkliste** verankert in `docs/methodology.md` (Section "Minimum depth contract", Tabelle mit 8 Schwellwerten). Validator: `shared/tests/depth.test.ts` — bricht `pnpm -r test` bei Unterschreitung mit `<id>: <dimension> N < M`-Meldungen. Per-Component-Override via `overrides`-Map möglich. Erledigt 2026-04-29.
- [x] **P0-3 ADR-006: Token-Layer** — Decision-Doc geschrieben: kanonische Token-Namen pro Slot, Werte in `implementations/<lib>/`. Fünf Kategorien (`spacing`, `radius`, `color`, `elevation`, `typography`). Schema-Skizze + `tokenBindings`-Form fixiert für P1-5. Erledigt 2026-04-29. Datei: `docs/adr/006-token-layer.md`.
- [x] **P0-4 Roadmap-Pin** — `<aside class="phase-banner">` auf Site-Index oberhalb Hero: "Phase 1 · Reference only — no live examples and no implementation code yet. Phase 2 (implementation audits) begins Q3 2026." Light/Dark-tauglich, Mobile-Stack ab 540px. Erledigt 2026-04-29. Dateien: `site/src/pages/index.astro`, `site/src/styles/global.css`.
- [x] **P0-5 Anatomy-SVG Label- und Floating-Overlaps fixen** — alle drei verifizierten Layout-Bugs behoben. (a) **Tabs indicator vs tabpanel**: `layoutGrid` reserviert jetzt Floating-Extension pro Anker-Slot (`fHeight + offset`); next root row droppt um den Overflow-Betrag. tabpanel sitzt jetzt bei y=136 (vorher 112), indicator endet bei y=128, 8px Lücke. (b/c) **Parent-Slot-Labels (Modal container/header, Combobox listbox)**: neuer `PARENT_LABEL_STRIP=24` reserviert Top-Strip in Nested-Area; `emitSlotGroup` bekommt `hasChildren`-Flag und rendert Labels für Parent-Slots top-left mit `dominant-baseline=hanging` statt zentriert. Modal container label jetzt y=30 (war 144), header label y=66 (war 72). Combobox listbox label y=106 (war 242), liegt über den Option-Children (y=128). 4 neue invariant-Tests prüfen: Tabs indicator-bottom ≤ tabpanel-top; Modal container/header label-y < first-child-rect-y; Combobox listbox label-y < first-option-y. Erledigt 2026-04-29. Dateien: `shared/src/svg.ts`, `shared/tests/schema.test.ts`.

## P1 — strukturelle Schema-Erweiterungen

- [x] **P1-5 Token-Layer Schema + Migration** — `anatomySlotSchema` um optionales `tokens`-Feld (5 Kategorien als optionale `Record<string, dotted-token-name>`-Maps) erweitert, Vokabular fixiert + in `docs/schema.md` dokumentiert, alle 5 YAMLs (33 Slots) migriert, neue Designer-View-Komponente `TokensTable.astro` mit sparse-column-Logik integriert. Tests + `pnpm -C site build` grün, Browser-Smoke-Test bestätigt Designer-only-Sichtbarkeit + warmer Akzent. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/TokensTable.astro`, `site/src/components/views/DesignerView.astro`, `docs/schema.md`.
- [x] **P1-6 Motion-Sektion** — `motion?` als Top-Level-Feld auf `componentSchema` (per-Component, nicht per-Slot), drei Pflicht-Keys (`durations` non-empty Record, `easing` als single dotted token, `reducedMotionFallback` Enum `instant | reduced | preserved`). Vokabular `motion.duration.{instant, fast, base, slow, slower}` + `motion.easing.{standard, decelerate, accelerate, sharp}` in `docs/schema.md`. Migration Modal/Combobox/Tabs durchgeführt; Card/Button bewusst ohne motion. Designer-View-Section `MotionTable.astro` zwischen Tokens und Axes (Bridge/Dev hidden via `data-view-section="designer"`-Pattern aus P3-20). 6 neue vitest-Cases (positiv: modal/tabs parse; negativ: raw `200ms`, leere durations, unbekannter Fallback; baseline: card/button ohne motion). Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `content/components/{modal,combobox,tabs}.yaml`, `site/src/components/sections/MotionTable.astro`, `site/src/components/views/DesignerView.astro`, `docs/adr/007-motion-section.md`, `docs/schema.md`, `shared/tests/schema.test.ts`.
- [x] **P1-7 Responsive-Sektion** — `responsive?` als Top-Level-Feld auf `componentSchema` (per-Component, parallel zu `motion`). `breakpoints` ist non-empty Array von `{ at: dotted-token, change: prose }`-Records. Vokabular `breakpoint.{xs, sm, md, lg, xl}` in `docs/schema.md`. Direction (oberhalb/unterhalb/zwischen) wird in der `change`-Prosa kodiert, nicht im Schema. Migration Card (orientation-Collapse bei sm), Modal (fullscreen below sm), Tabs (vertical forbidden below lg), Combobox (Plattform-Picker mobile) durchgeführt; Button bewusst ohne responsive. Designer-View-Section `ResponsiveTable.astro` zwischen Motion und Axes (Bridge/Dev hidden via `data-view-section="designer"`-Pattern). 6 neue vitest-Cases (modal/tabs parsen; button omittet; raw `640px`/leeres Array/leere change-Prosa abgewiesen). ADR-008 dokumentiert per-Component, Free-Text-`change`, Direction-in-Prose, dotted-Token-`at`. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `content/components/{card,modal,tabs,combobox}.yaml`, `site/src/components/sections/ResponsiveTable.astro`, `site/src/components/views/DesignerView.astro`, `docs/adr/008-responsive-section.md`, `docs/schema.md`, `shared/tests/schema.test.ts`.
- [x] **P1-8 State-Maschine modellieren** — `axes.states.transitions?: [{ from, to, trigger }]` als optionales nested-Feld unter `axes.states` (nicht top-level — States-Vokabular und State-Graph gehören zusammen). Erste cross-field refine im Schema: `from`/`to` müssen in `axes.states.interactive ∪ data` deklariert sein, sonst Zod-Fehler mit Pfad `axes.states.transitions[i].from|to`. Migration Modal (4 Transitions: closed→opening→open→closing→closed), Combobox (6 Transitions: closed↔open, open↔busy, open→invalid, invalid→closed) durchgeführt; Card/Tabs/Button bewusst ohne (triviale Graphen). `TransitionsTable.astro` rendert in allen drei Views (`role="both"`) direkt nach AxesTable — Bridge-View profitiert besonders, weil der "Designer denkt closed→open in einem Klick, Developer weiß opening dazwischen" mismatch genau dort sichtbar wird. 6 neue vitest-Cases (modal/combobox parsen + edge-Set-Assertion; card/tabs/button ohne; refine-Fehler bei unbekanntem from/to; leerer trigger). ADR-009 dokumentiert nested-vs-top-level, free-text trigger, cross-field refine, ADR-007-Verweis-Auflösung. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `content/components/{modal,combobox}.yaml`, `site/src/components/sections/TransitionsTable.astro`, `site/src/components/views/{Designer,Dev,Bridge}View.astro`, `docs/adr/009-state-transitions.md`, `docs/schema.md`, `shared/tests/schema.test.ts`.
- [x] **P1-9 Property-Type strukturiert** — `property.type: string` → `z.discriminatedUnion('kind', [primitive, enum])` mit `kind: primitive, of: boolean` und `kind: enum, values: [...]` (min 2, eindeutig, non-empty). Alle 17 Properties über die fünf YAMLs migriert (6 primitive, 11 enum). Render-Helper `formatPropertyType` in `shared/src/format.ts` hält die `' | '`-Anzeige unverändert; `AxesTable.astro` rendert in allen drei Views identisch zur Pre-Migration. 7 neue vitest-Cases (positive/negative/baseline + duplicate-refine + unknown-kind). ADR-010 begründet zwei-Arme-statt-drei, `kind`-Discrimination, `.strict()`-Pattern, fehlenden Pipe-Shortcut. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/src/format.ts`, `shared/src/index.ts`, `shared/package.json`, `shared/tests/schema.test.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/AxesTable.astro`, `docs/schema.md`, `docs/adr/010-property-type-union.md`.
- [x] **P1-10 Events Top-Level-Feld** — `events?: Array<{ name (camelCase), payload (prose), frameworkNotes: { webComponents, react, angularSignals, vue } }>`, optional pro Komponente, `min(1)` wenn vorhanden, `frameworkNotes` als `.strict()`-Record mit allen vier Frameworks required. Migration Modal (2 Events: openChange, dismiss), Tabs (2 Events: selectedChange, tabActivate), Combobox (3 Events: inputChange, selectionChange, openChange); Card und Button bewusst ohne. Neue `EventsTable.astro` rendert in Dev- und Bridge-View nach `FrameworkMapTable`; Designer-View bewusst ohne (events sind dev-Konzept). 7 neue vitest-Cases (positive Modal/Tabs/Combobox, baseline Card/Button, leerer Array, Nicht-camelCase-Name, fehlender frameworkNotes-Key). ADR-011 begründet per-Komponente, strukturierte frameworkNotes statt Free-Text, prose-payload, camelCase-name, required-all-four, Dev+Bridge-Render. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{modal,tabs,combobox}.yaml`, `site/src/components/sections/EventsTable.astro`, `site/src/components/views/{Dev,Bridge}View.astro`, `docs/schema.md`, `docs/adr/011-events-section.md`.

## P2 — Inhalts-Sektionen

- [x] **P2-11 Component-Property-Mapping-Tabelle** — `propertyMapEntrySchema` mit `figma` (string), `code` (string), `type` (closed enum `Boolean | Variant | Text | Instance Swap`), `notes?` (optional prose). Top-level `componentSchema.propertyMap?: array.min(1)`. Per-Entry `.strict()`. `code`-Feld bewusst loose — referenziert nicht via Refine zu `axes.properties[].name`, weil Entries auch Slot-IDs (`iconLeading`), default-Slot-children (`children`) und DOM-Attribute (`data-state`) erfassen. Migration aller fünf Komponenten: Button (8 Entries), Card (14), Modal (10), Tabs (6), Combobox (9). Neue `PropertyMapTable.astro` (4-Spalten Figma | Type-Badge | Code | Notes) rendert in Designer-View (zwischen TokensTable und MotionTable) und Bridge-View (nach AxesTable); Dev-View bewusst aus (code-side props sind in axes.properties + frameworkMap). CSS-Type-Badges via `color-mix()` Light/Dark-Parity (Boolean=neutral, Variant=cool, Text=warm, Instance Swap=green). 7 neue vitest-Cases. ADR-015 begründet Figma-Vokabular-statt-code-side-type, loose-coupling-zu-axes.properties, per-Komponente, optional-notes, Designer+Bridge-Render, closed-enum-type. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/PropertyMapTable.astro`, `site/src/components/views/{Designer,Bridge}View.astro`, `site/src/styles/global.css`, `docs/schema.md`, `docs/adr/015-property-map.md`.
- [x] **P2-12 "When to use"-Sektion** — `whenToUseSchema` mit `use` (required prose), `avoid` (required prose), `vsRelated?: [{ id (slug), difference (prose) }]` (optional, `min(1)` wenn vorhanden); alle drei Felder `.strict()`. Bare `related[]`-Feld aus `componentSchema` und allen 5 YAMLs entfernt — `whenToUse.vsRelated[].id` ist jetzt der einzige Identifier-Träger. Neue `WhenToUseSection.astro` rendert direkt nach Hero (vor Anatomy-Diagram), view-agnostisch in `[id].astro`. Migration aller 5 Komponenten mit `use`+`avoid`+`vsRelated` (Button: 3, Card: 2, Modal: 3, Tabs: 3, Combobox: 3 vsRelated-Einträge). 7 neue vitest-Cases (positive Card/Combobox, negative empty-use/empty-avoid/non-slug-id/empty-vsRelated, baseline whenToUse-ohne-vsRelated). ADR-012 begründet replace-statt-coexist (ADR-001 single-source), per-Komponente-statt-global, required-use-und-avoid, optional-aber-non-empty-vsRelated, Free-Text-difference, view-agnostischen Render. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/WhenToUseSection.astro`, `site/src/pages/components/[id].astro`, `docs/schema.md`, `docs/adr/012-when-to-use-section.md`.
- [x] **P2-13 Form-Integration-Sektion** — `formIntegrationSchema` mit vier optionalen Prose-Feldern (`name`, `formData`, `reset`, `validation`), `.strict()` plus refine "mindestens-eines-required". Migration Button (form control, alle vier Felder), Combobox (form control mit inner-input, alle vier Felder), Modal (form container, alle vier Felder dokumentieren Container-Sicht statt Control-Sicht); Card/Tabs bewusst aus (keine canonical Form-Participation). Neue `FormIntegrationSection.astro` (definition-list mit term-and-description-Layout) rendert in Dev- und Bridge-View nach Events vor Accessibility; Designer-View bewusst aus (HTML-form-prose ist dev-side technical content). 7 neue vitest-Cases. ADR-016 begründet vier-Sub-Fields-statt-prose, alle-optional-mit-refine, prose-statt-strukturiert, per-Komponente, Dev+Bridge-Render, vier-Universal-Core-Concerns. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{button,combobox,modal}.yaml`, `site/src/components/sections/FormIntegrationSection.astro`, `site/src/components/views/{Dev,Bridge}View.astro`, `docs/schema.md`, `docs/adr/016-form-integration.md`.
- [x] **P2-14 i18n-Sektion** — `i18nSchema` mit nested `rtl: { mirroring }` plus top-level `textExpansion`, beide Felder required prose. `i18nRtlSchema` als nested-namespace für künftige Facets (numerals, dates, dir-attribute, bidi). Migration aller fünf Komponenten — jede deklariert beide Facets mit komponenten-spezifischer Prosa (Button: icon-swap + 30-50% expansion; Card: orientation-flip + line-clamps; Modal: close-button-position + footer-wrap; Tabs: arrow-key-reversal + density risk; Combobox: clear-button-position + listbox max-width). Neue `I18nSection.astro` (zwei prose blocks) rendert in Designer-View (nach Responsive vor Axes) und Bridge-View (nach FormIntegration vor Accessibility); Dev-View bewusst aus. 6 neue vitest-Cases. ADR-017 begründet nested-rtl-namespace, beide-required, prose-statt-strukturiert, per-Komponente, Designer+Bridge-Render. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/I18nSection.astro`, `site/src/components/views/{Designer,Bridge}View.astro`, `docs/schema.md`, `docs/adr/017-i18n-section.md`.
- [x] **P2-15 A11y-Acceptance-Set pro Komponente** — `a11yAcceptanceSchema` mit drei optionalen Sub-Arrays: `keyboardWalk` (`{ keys, expected }`), `announcements` (`{ trigger, expected }`), `axeRules` (kebab-case ids). Top-level `.strict()` plus refine "mindestens eines der drei muss vorhanden sein". `axeRuleId`-Regex enforced kebab-case Shape; Membership gegen axe-core nicht validiert (CI catches via `axe.run()`-Rejection). Migration aller fünf Komponenten — alle haben mindestens `axeRules`, die meisten auch `keyboardWalk` + `announcements`. Neue `A11yAcceptanceTable.astro` rendert in Dev- und Bridge-View nach existing A11yTable; Designer-View unverändert (per-slot `a11y.hint` deckt das ab). 7 neue vitest-Cases (positive Modal/Combobox/all-five, negative non-kebab-axe-id/empty-object/empty-keyboardWalk-expected/empty-axeRules). ADR-014 begründet drei-Sub-Arrays statt-flach, alle-optional-aber-mindestens-eines, prose-keys-und-trigger statt structured, kebab-axe-ids ohne Membership-Validation, Dev+Bridge-Render. Discovery: YAML-Colons innerhalb Backtick-Prose (`` `dismissible: true` ``) brauchen Quote — als Schema-Doc-Footnote dokumentiert. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/A11yAcceptanceTable.astro`, `site/src/components/views/{Dev,Bridge}View.astro`, `docs/schema.md`, `docs/adr/014-a11y-acceptance.md`.
- [x] **P2-16 Performance-Schwellwerte first-class** — `performanceSchema` als top-level optionale `array.min(1)` von `performanceThresholdSchema` (`{ name (camelCase), metric (prose), threshold (positive number), unit (prose), rationale (prose) }`, jede Entry `.strict()`). Backlog-Spec "in `axes.properties` ziehen" verworfen — `axes.properties` sind Consumer-set values, Thresholds sind Consumer-read advice; getrennte Concerns. Migration Combobox (virtualisedListbox 200 items + asyncFilterDebounce 150ms), Tabs (tablistOverflow 7 tabs + lazyPanelRender 100kb), Modal (stackDepth 1 modal); Card und Button bewusst aus. Neue `PerformanceSection.astro` (Liste mit name + metric/threshold/unit-Header + rationale-prose) rendert in Dev-View (nach FormIntegration) und Bridge-View (nach FormIntegration vor I18n); Designer-View bewusst aus (UX-Rule liegt in `whenToUse.avoid`, threshold ist dev-capacity-planning). 7 neue vitest-Cases. ADR-018 begründet top-level-statt-axes-embed, single-point-numeric-statt-range, free-string-unit-statt-enum, name-und-metric-getrennt, Dev+Bridge-Render. **Schließt den P2-Bucket** — Phase-1 Schema ist strukturell vollständig. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{combobox,tabs,modal}.yaml`, `site/src/components/sections/PerformanceSection.astro`, `site/src/components/views/{Dev,Bridge}View.astro`, `docs/schema.md`, `docs/adr/018-performance-thresholds.md`.

## P3 — Site & SVG-Polish

- [x] **P3-17 Site-A11y-Fixes** — Skip-Link in `site/src/layouts/Base.astro` (off-screen, slides in on `:focus`/`:focus-visible`, jumps to `#main`), `<main id="main">`, `<nav aria-labelledby="components-heading">` mit `<h2 id="components-heading">` um den Component-Grid auf der Index-Seite. Erledigt 2026-04-29. Dateien: `site/src/layouts/Base.astro`, `site/src/pages/index.astro`, `site/src/styles/global.css`.
- [x] **P3-18 Anatomy-SVG-Verbesserungen** — Required-vs-Optional bereits via `stroke-dasharray="6 4"` differenziert (kein Touch nötig). Floating-Slots erhalten jetzt `class="anatomy-floating"` (CSS `drop-shadow`-Filter, light/dark) plus eine kreisförmige `z`-Badge in Akzentfarbe. Wiederholte Slots (`repeats > 1`) zeigen oben rechts ein `n×`-Pill (`anatomy-repeat-count`). Asymmetrische Layouts: bestehender `aspect`-Pfad in `layoutGrid` bleibt korrekt — keine Anpassung nötig. Tests: zwei neue vitest-Cases gegen Combobox. Erledigt 2026-04-29. Dateien: `shared/src/svg.ts`, `site/src/styles/global.css`, `shared/tests/schema.test.ts`.
- [x] **P3-19 Shiki-Integration** — `markdown.shikiConfig` mit Dual-Theme (`github-light` / `github-dark-default`), `defaultColor: false`, `wrap: true`. Dark-Theme über `:root[data-theme='dark']` statt `prefers-color-scheme`. Erledigt 2026-04-29. Dateien: `site/astro.config.mjs`, `site/src/styles/global.css` (`.astro-code` Basis-Styles + Dark-Override).
- [x] **P3-20 Eyebrow ohne MutationObserver** — Per-View-Labels jetzt CSS-only via `:root[data-view='…'] [data-view-eyebrow]::before { content: '…'; }` (gleiches Muster für `[data-anatomy-caption]`). Komplettes `<script>`-Block aus `[id].astro` entfernt. Erledigt 2026-04-29. Dateien: `site/src/pages/components/[id].astro`, `site/src/styles/global.css`.

## P4 — Skalierung & Tooling

- [x] **P4-21 Cross-Component-Konsistenz-Audit-Script** — Bei 23 Komponenten ripe geworden; implementiert als `shared/tests/consistency.test.ts` (vitest, gleicher CI-pattern wie `depth.test.ts`). Acht Checks: (1) Token-Referenzen aus canonical spacing/radius/color/elevation/typography Vokabular; (2) Motion-durations + easing aus canonical motion-Vokabular; (3) Responsive-breakpoints aus canonical breakpoint-Vokabular; (4) Properties `density` deklarieren exakt `[comfortable, compact]`; (5) Properties `size` aus bounded set `[sm, md, lg, xl, full]`; (6) Interactive-states subset of `{hover, focus-visible, active, disabled, visited, current}`; (7) `tokens.color.ring` equals `color.border.focus`; (8) axe rule ids kebab-case + event names camelCase. Canonical Vokabulare in test-file als Konstanten gespiegelt — drift zwischen docs/schema.md und test ist same-PR-update-discipline. **Erste echte drift gefunden + resolved:** `drawer.size` enthielt `full` während andere components `[sm, md, lg, xl]` nutzen — `full` als canonical-extension für full-viewport-variants akzeptiert (decision in ADR-019, doc + test in same commit). 8 neue vitest-Cases (alle grün post-resolution); CI loop unverändert via `pnpm -r test`. ADR-019 begründet vitest-statt-CLI, mirror-statt-shared-module, test-level statt Zod-membership-enforcement, full-canonical-statt-drawer-divergence. Erledigt 2026-04-30. Dateien: `shared/tests/consistency.test.ts` (neu), `docs/adr/019-consistency-audit.md` (neu), `docs/schema.md` (consistency-section).
- [x] **P4-22 Search/Filter** auf Index-Page (Pagefind) — Threshold (>15) bei 23 Komponenten klar überschritten. `astro-pagefind` integration in `astro.config.mjs`; Pagefind CLI runs nach `astro build` und schreibt index + default-UI nach `site/dist/pagefind/`. Neue `/search.astro` page mit `astro-pagefind/components/Search`-component (autofocus + sub-results + 24-token excerpts). Header-search-icon link in Base.astro routed zu `/search`; magnifier icon in Icon.astro. ComponentPageShell setzt `data-pagefind-body` auf canonical designer-page (`/components/<id>`) und `data-pagefind-ignore` auf alt-view-pages (`/components/<id>/<view>`) — pagefind indexes 23 fragments statt 69, vermeidet duplicate-clutter. Pagefind-UI inherit site CSS-custom-properties für light/dark-parity. Index-size ~1MB total (index + UI + WASM), lazy-loaded auf `/search` only — kein impact auf component-page bundle. Netlify-deploy unverändert: `site/dist` publish-path inkludiert `site/dist/pagefind/`. Erledigt 2026-04-30. Dateien: `site/astro.config.mjs`, `site/package.json` (deps `astro-pagefind` + `pagefind`), `site/src/pages/search.astro` (neu), `site/src/components/ComponentPageShell.astro`, `site/src/components/Icon.astro` (search-glyph), `site/src/layouts/Base.astro`, `site/src/styles/global.css` (header-search styles).
- [ ] **P4-23 Versionierung** — `deprecated?`-Flag pro Variant/Property/Slot, `since`-Feld pro Komponente, Changelog-Sektion.
- [x] **P4-24 MCP-Server-Tool-Erweiterung** — sechs neue Tools im `mcp-server/src/server.ts`: `get_tokens` (Array von `{slotId, tokens}` für alle Slots mit Token-Bindings), `get_motion` (Motion-Block oder `null`), `get_responsive` (Responsive-Block oder `null`), `get_transitions` (axes.states.transitions oder `null`), `get_events` (Events-Array oder `null`), `get_when_to_use` (whenToUse-Block oder `null`). Konvention: optionale Felder geben `null` zurück, nicht Error — "Komponente hat kein Motion" ist sinnvolle Antwort. Tools-List-Test aktualisiert (15 Tools statt 9). 9 neue vitest-Cases (jeweils Happy-Path + null-Case für Modal/Card/Button/Combobox). Erledigt 2026-04-29. Dateien: `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`.
- [x] **P4-25 View-Strategy reevaluieren** — Re-Eval bei 23 Komponenten durchgeführt. Vorher: alle 3 views (Designer/Dev/Bridge) in jeder page-DOM, CSS-toggle via `:root[data-view='X']`. Modal-page (heaviest): 208k raw / 30k gzip / ~4500 DOM-nodes. Astro-docs konsultiert via MCP — Browser-native MPA cross-document View-Transitions (`@view-transition { navigation: auto }`) sind zero-JS-Lösung; Astro's `<ClientRouter />` wird laut Astro-team "increasingly unnecessary". Lösung gewählt: per-URL-routing — `/components/<id>` rendert designer-default, `/components/<id>/[view]` rendert dev oder bridge (23×2=46 plus 23 = 69 component pages). `ComponentPageShell.astro` faktorisiert page-chrome, jeder route rendert single view-section. `ViewSwitcher` von buttons-mit-JS-toggle auf anchor-links-mit-`aria-current="page"`; `Base.astro` setzt `<html data-view={view}>` server-side; localStorage-persistence weg (URL ist jetzt source-of-truth). CSS-rules für `[data-view-section]`-visibility entfernt; `@view-transition` für browser-native cross-doc fade. Messung post-conversion (Modal-page): 101k raw (-52%) / 16k gzip (-47%) / 2393 DOM-nodes (-47%). Cross-package average: 76kb raw per page (war median 185kb, -59%). Side-benefits: per-view URLs deeplinkable + eigene SEO-surface; eyebrow+anatomy-caption ohne JS-placeholders; works zero-JS via standard MPA-navigation. `<ClientRouter />` bewusst nicht hinzugefügt — kein shared-state-bedarf. Erledigt 2026-04-30. Dateien: `site/src/components/ComponentPageShell.astro` (neu), `site/src/pages/components/[id]/[view].astro` (neu), `site/src/pages/components/[id].astro`, `site/src/components/ViewSwitcher.astro`, `site/src/layouts/Base.astro`, `site/src/styles/global.css`.

## P5 — Phase 2 (Implementations)

- [x] **P5-26 `implementations/radix/`** als erste React-Referenz — Divergenz-First-MVP gelandet (ADR-013). `implementationSchema` mit `componentId`, `libraryId`, `componentName`, `exampleCode?`, `divergence?` (discriminated union: omitted/renamed/extended/reshaped), `rationale?`, `lastReviewed` (required ISO). `divergenceSchema` als `z.discriminatedUnion('type', [...])` mit `.strict()` pro Arm. `from`-Pfad-Regex erlaubt `identifier(.identifier|[index])+` mit camelCase-Bracket-Indexes (deckt slot-ids/event-names/property-names/variant-names). `loadImplementation` + `loadImplementations` in `shared/src/loader.ts`; aggregierter Loader returned `Map<libraryId, Map<componentId, Implementation>>` und enforced `libraryId === parentDir`. **Erstes Audit:** `implementations/radix/modal.yaml` — Radix Dialog vs canonical Modal, 17 Divergenz-Einträge über alle drei genutzten Typen (renamed: backdrop/title/close-button/alertdialog; omitted: header/footer/fullscreen/size/scrollBehavior/dismiss-event-no-direct-equiv/reducedMotionFallback; reshaped: container-split/body-split/dismissible-callbacks/openChange-via-onOpenChange/dismiss-via-three-callbacks/motion-via-data-state/transitions-via-data-state). 8 neue vitest-Cases (positive parse, all-four-types-coverage, group-by-library, missing-lastReviewed, unknown-type, renamed-missing-to, bad-from-path, strict-rejection). Bindings (`tokenBindings`/`motionBindings`/`transitionBindings`/`eventBindings`) bewusst deferred — eigene Folge-ADRs (014+) wenn echte Audits sie brauchen. **P5-29 implizit erledigt:** Divergenz-Schema gegen reale Implementation getestet; ein Regex-Widening (camelCase im Bracket-Index) als einzige Discovery. Site-Rendering und MCP-Tools deferred bis nach zweitem Library-Audit. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/src/loader.ts`, `shared/tests/schema.test.ts`, `implementations/radix/modal.yaml`, `docs/schema.md`, `docs/adr/013-implementation-schema.md`.
- [x] **P5-27 `implementations/headlessui/`** für Vue — Modal-audit (Dialog) gelandet als zweites library-audit nach Radix. Mirroring der Radix-divergenz-shape (selbe omissions: backdrop/header/footer/close-button; selbe reshapes: container-split, body-split, events; selber rename: title). Headless-UI-spezifische divergenzen: animation als separates composable-primitive (TransitionRoot + TransitionChild) statt bundled-hooks-auf-content; decoupled state-management (consumer owns isOpen ref; Dialog only emits @close); AlertDialog nicht separates package sondern composition-pattern (role="alertdialog" + :initialFocus). Pattern-recap across both audits: beide libraries stop at accessibility-primitive; design-system-specifics (size-scales, motion-timing, dismissible-flags, fullscreen-variants) sind consumer-composed-layer above library. Site rendert beide library-cards auf /components/modal alphabetisch (headlessui, radix); **stacked-layout works fine at 2 libraries — refactor-trigger wäre 3+**. Render-refactor-decision (tabs vs side-by-side) deferred bis P5-28 (CDK/Angular) landet. 106 shared tests grün; 71 site pages mit Pagefind-index. Erledigt 2026-04-30. Datei: `implementations/headlessui/modal.yaml`.
- [x] **P5-28 `implementations/cdk/`** für Angular Signals — Modal-audit (Dialog from `@angular/cdk/dialog`) gelandet als drittes library-audit. Most-divergent shape: imperative service-based API (`dialog.open(ContentComponent, config)`) statt declarative-component-tree. Consumer's content component IS the body; header/title/close-button/footer ALL omitted from CDK and live in consumer's component template. CDK-strengths: built-in backdrop primitive (`hasBackdrop` config), native commit-with-result via DialogRef.closed Observable, config-driven alertdialog (`role: 'alertdialog'`), comprehensive Overlay scroll-strategy. CDK-weaknesses: imperative-first requires separate content-component per dialog (more boilerplate), `closed` Observable does not surface dismissal-reason (consumer wires Escape+backdrop manually), Angular animations verbose. Pattern-recap across 3 audits: alle libraries stop at accessibility-primitive; consumer-composition shape unterscheidet sich (declarative-children Radix, declarative-wrappers+render-props Headless UI, imperative-service+consumer-component CDK). **3 stacked impl-cards messurement:** modal-page 155k raw / 24k gzip / ~3900 nodes — still **-25% vs pre-P4-25 baseline (208k/30k/4500)**. Per-impl ~18kb. Tabs/side-by-side render-refactor erst ab ~5+ implementations attraktiv, nicht jetzt. 106 shared tests grün; 71 site pages. Erledigt 2026-04-30. Datei: `implementations/cdk/modal.yaml`.
- [x] **P5-29 Divergenz-Schema testen** (omitted/renamed/extended/reshaped) an realer Implementation — implizit erledigt durch P5-26 Radix-Modal-Audit. Drei der vier Typen genutzt (omitted, renamed, reshaped); extended in vitest-Skelett-Test abgedeckt aber im echten Audit nicht benötigt — Radix erweitert Modal nicht über canonical hinaus, nur reshapes/omits. Discovery: Bracket-Index-Regex musste von `[a-z0-9-]+` auf `[a-zA-Z0-9-]+` erweitert werden, weil camelCase-Property-/Event-Namen (`events[openChange]`) sonst nicht parsen. Erledigt 2026-04-29 als Teil von P5-26.

- [x] **P5-30 Site-Rendering Phase-2** — Phase-2-Implementations auf canonical Component-Page sichtbar. Neue `implementations` Astro-Collection in `site/src/content.config.ts` via `glob('*/*.yaml', base: '../implementations')` mit `implementationSchema`. Drei neue Section-Components in `site/src/components/sections/`: `ImplementationsSection.astro` (Wrapper, library-sortiert, conditional-render), `ImplementationCard.astro` (Library-Badge + Component-Name, Shiki-Code-Block mit lang-per-libraryId-Lookup `radix→tsx`/`headlessui→vue`/`cdk→typescript`, DivergenceTable, optional `<details>`-collapsed rationale), `DivergenceTable.astro` (4-Spalten From/Type/→To/Rationale, type-farbige Badges). View-agnostisch platziert in `[id].astro` zwischen Anatomy-Diagram und View-Tabs. CSS-Type-Badges in `global.css` via `color-mix()` Light/Dark-Parity (omitted=neutral, renamed=cool, extended=green, reshaped=warm). Smoke: Modal zeigt 17 Badges (6 omitted + 5 renamed + 6 reshaped), Card/Button/Tabs/Combobox ohne Section. Erledigt 2026-04-29. Dateien: `site/src/content.config.ts`, `site/src/pages/components/[id].astro`, `site/src/styles/global.css`, `site/src/components/sections/{ImplementationsSection,ImplementationCard,DivergenceTable}.astro`.

---

## Empfohlener Pfad

P0-1 (✓) → P0-2 (✓) → P0-3 (✓) → P0-4 (✓) → P3-17 (✓) → P3-19 (✓) → P3-20 (✓) → P3-18 (✓) → P1-5 (✓) → P1-6 (✓) → P1-7 (✓) → P1-8 (✓) → P1-9 (✓) → P1-10 (✓) → P2-12 (✓) → P4-24 (✓) → P5-26 (✓) → P5-29 (✓) → P5-30 (✓) → P2-15 (✓) → P2-11 (✓) → P2-13 (✓) → P2-14 (✓) → P2-16 (✓) → P4-25 (✓) → P4-22 (✓) → P4-21 (✓) → P5-27 (✓) → P5-28 (✓).

**Backlog ist nahezu vollständig durchgelaufen.** Verbleibend: P4-23 (Versionierung) als einziges offenes Item — bewusst deferred weil keine echten deprecated-states vorhanden, premature.

**Komplette P2-Inhalts-Sektion-Suite und Phase-1-Schema strukturell fertig.** Alle ADRs 006–018 gelandet. Verbleibend: P5-27 + P5-28 (Phase-2-Library-Audits), P4-21/P4-22/P4-23/P4-25 (Tooling/Skalierung, größen-getriggert).

Alle P0-, P1-, P3-Items plus P2-12, P4-24, P5-26, P5-29, P5-30 abgeschlossen. Phase 1 strukturell vollständig (ADRs 006–011); P2-12 erste P2-Inhalts-Sektion (ADR-012); P4-24 macht alle Phase-1-Felder über MCP konsumierbar (15 Tools); P5-26 landet Phase-2-Schema (ADR-013, divergence-first MVP) plus erstes Audit (Radix Dialog vs Modal); P5-29 implizit dadurch geschlossen; P5-30 macht Phase-2-Audits auf canonical Component-Pages sichtbar. **Phase-2 ist jetzt vollständig sichtbar** (Schema + Loader + erstes Audit + Render). Zwei unabhängige Stränge bleiben (parallel oder sequenziell):
1. **P5-27 `implementations/headlessui/`** für Vue — zweite Library, treibt Render-Design-Entscheidung "wie sehen mehrere Library-Audits nebeneinander aus?" mit echten Daten. Voraussichtlicher Refactor-Trigger: Tabs vs stacked layout, lang-per-library map → schema-field, etc.
2. **Restliche P2-Items** (P2-11 propertyMap, P2-13 formIntegration, P2-14 i18n, P2-15 a11yAcceptance, P2-16 performance) — Inhalts-Sektionen, jeweils ADR + Schema + Migration + Render. Empfohlen P2-15 (a11yAcceptance) als nächstes weil testbar (axe-rules in CI) und größter Wert für Bridge-View.

Restliche P4-Items (P4-21 Konsistenz-Audit, P4-22 Pagefind, P4-23 Versionierung, P4-25 View-Strategy-Re-Eval) sind größen-getriggert (warten auf >15 / ~20 Komponenten) bzw. CI-Folge-Arbeit.

- [x] **P3-21 Editorial-Reference-Polish** — additive Stildisziplin im bestehenden Cream/Warm-Cool-Neutral-System. Body bekommt subtilen warmen Halo-Gradient oben (6% accent-warm, im Dark-Mode auf cool 4% gedreht). `.component-card` mit zweischichtigem Schatten-Stack (4% + 16% fg, im Hover auf 12% + 35% accent-active). `.phase-banner` mit linearem Akzent-Wash auf den ersten 35% statt flachem Surface. `.section-header::before` als 2px-Akzent-Schiene links jeder Section (folgt `--accent-active`, also warm/cool/neutral pro View). `.hero-title::before` mit dezentem warmen Slab-Gradient hinter dem Index-Hero-Titel. Inspiriert von Refactoring UI / Designsystems.com / GOV.UK. Anatomy-Diagramme bewusst unverändert. Erledigt 2026-04-29. Datei: `site/src/styles/global.css`.

## Wartung dieses Backlogs

Bei jeder erledigten Aufgabe Status auf `[x]` setzen, Erledigungs-Datum + Datei-Verweis ergänzen. Neue Items nach Priorität einsortieren. Phase-2-Items (P5) bleiben offen bis Phase 1 stabil.
