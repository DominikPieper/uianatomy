# Backlog

Persistente Arbeitsliste aus dem Senior UX/UI + Senior Frontend Review (2026-04-29). Quelle: `~/.claude/plans/mache-ein-detailliertes-review-cheeky-cocke.md`.

Closed items leben in [`docs/backlog-archive.md`](backlog-archive.md). Beim Vergeben einer neuen `PX-NN`-id beide Files scannen, damit IDs nicht kollidieren.

Status-Legende: `[ ]` offen · `[~]` in Arbeit · `[x]` erledigt

---

## P0 — sofort, blockiert Phase 2

Komplett geschlossen — siehe `docs/backlog-archive.md`.

## P1 — strukturelle Schema-Erweiterungen

Komplett geschlossen — siehe `docs/backlog-archive.md`.

## P2 — Inhalts-Sektionen

- [x] **P2-17 propertyMap fehlt auf 4 Form-Komponenten** — `propertyMap`-Sektion (P2-Pflicht, ADR-025) auf allen vier Form-Komponenten ergänzt: **checkbox** (12 Einträge: Variant + Size + Checked + Indeterminate + Required + Disabled + Invalid + Label + Description + Error Message + Indicator + Name), **radio-group** (12: Variant + Size + Orientation + Required + Disabled + Invalid + Value + Name + Legend + Description + Error Message + Indicator), **switch** (10: Variant + Size + Checked + Required + Disabled + Label + Description + Name + Thumb + Track), **textarea** (13: Variant + Size + Resize + Rows + Max Rows + Disabled + Readonly + Required + Invalid + Label + Placeholder + Description + Error Message + Character Count). Total **47 Einträge**. Quelle pro Eintrag: `axes.variants` → `Variant` (kind:enum), `axes.properties` (primitive→boolean / enum→enum / number→number), Anatomie-Slots → slot/text, controlled-state-Props (checked/value) wo strukturell zentral; `notes`-Prosa verdichtet aus `mismatches`/`mistakes`-Begründungen der jeweiligen YAML — kein Neuschreiben. Insertion-Position pro text-input-Konvention zwischen `events:` und `formIntegration:`. Keine Schema-Änderungen — `propertyMapEntrySchema` (`shared/src/schema.ts:566-575`) deckte alle Shapes ab. Validation: 225 shared + 68 mcp = 293 tests grün (unverändert), site-build 137 pages clean (0 neue broken-links; pre-existing `methodology → schema.md#versioning-optional` unverwandt). Schließt erstes Major-Finding aus Audit-Pass 2026-05-05; entfernt eine echte P2-Pflicht-Lücke aus dem Backlog. Erledigt 2026-05-06. Dateien: `content/components/{checkbox,radio-group,switch,textarea}.yaml`.

## P3 — Site & SVG-Polish

Komplett geschlossen — siehe `docs/backlog-archive.md`.

## P4 — Skalierung & Tooling

- [ ] **P4-26 State-Graph-Renderer** — eigener Graph-Renderer für `axes.states.transitions`. Heute reine Tabelle (`TransitionsTable.astro`); Graph wäre zweiter SVG-Generator parallel zu `renderAnatomySVG`, gleicher Wireframe-Stil, eigener Layout-Algorithmus (Stack-Layout für lineare State-Maschinen wie Modal: closed→opening→open→closing; force-directed mit fixem Seed für Combobox-artige Graphen). Position auf Component-Page: unter `TransitionsTable`. Eigene ADR begründet warum separat statt in Anatomy-SVG integriert (Graph-Layout-Problem vs Grid-Layout-Problem; anderer User-Need: "Wie wechselt die Komponente ihren State?" vs "Welche Slots gibt es?"). Aus P3-24 Plan extrahiert als eigenes Item.
- [x] **P4-27 MCP-Server-Härtung gegen mcp-builder-Skill-Best-Practices (Tiers 1–3)** — Audit des `@uianatomy/mcp-server` gegen `mcp-builder`-Skill-Checkliste (`reference/mcp_best_practices.md` + `node_mcp_server.md`), gefiltert für einen statischen read-only Canon-Server (kein Pagination-Offset/Cursor, kein OAuth/DNS-Rebinding/Rate-Limit — bewusst ausgelassen). **Tier 1 (Metadata):** alle 29 Tools von Legacy-`server.tool()` auf modernes `server.registerTool()` migriert; `title` + `annotations` (`READ_ONLY` = readOnlyHint/idempotentHint true, destructiveHint/openWorldHint false — Canon ist geschlossenes gebündeltes Korpus) pro Tool; Inputs auf `z.strictObject({...})` (unbekannte Args werden abgewiesen) mit `.describe()` auf jedem Feld (id-Felder nennen `Call list_components`); generische `notFoundResult(kind,id,recovery)`-Errors die das Recovery-Tool benennen (component→list_components, pattern→list_patterns, sub-anatomy→list_sub_anatomies, contracts→beide). **Tier 2 (outputSchema):** `outputSchema` + `structuredContent` auf den 12 objekt-rückgebenden Tools (`output-schemas.ts` wiederverwendet `componentSchema`/`patternSchema`/`subAnatomySchema`/`axesSchema`/`frameworkMapSchema`/`contractsSchema` aus `@uianatomy/shared/schema` — sicher per Konstruktion, weil der Loader die Daten gegen dieselben Schemas validiert); array-/nullable-Tools bleiben bewusst content-only (MCP verlangt non-null-Objekt für structuredContent — Envelopes würden die Wire-Shape für Array-Konsumenten brechen). **Gotcha gefunden:** `z.record(...)` als top-level outputSchema crasht das SDK (`Cannot read .shape`) → `z.looseObject({})` statt record. **Tier 3 (Size-Guards):** `constants.ts` `CHARACTER_LIMIT=25000` + warnungs-only Truncation in `contentFor()` (verwirft NIE Daten — silent-slicing hatte `get_implementations(modal)` 3→1 korrumpiert; stattdessen Warn-Block + opt-in `limit`-Param auf `search_components`/`list_implementations`); neuer stdio-Entry `src/local.ts` (stderr-only logging, fixt das tote `dev`-Script + dient als Eval-Target). 6 neue Best-Practice-Tests (title/annotations/outputSchema/structuredContent/strict-reject/error-hint/limit). Validation: 225 shared + 75 mcp = 300 tests grün (war 294 +6), typecheck + build clean, stdio-Entry bootet. Erledigt 2026-05-27. Dateien: `mcp-server/src/server.ts`, `mcp-server/src/annotations.ts` (neu), `mcp-server/src/constants.ts` (neu), `mcp-server/src/output-schemas.ts` (neu), `mcp-server/src/local.ts` (neu), `mcp-server/tests/server.test.ts`.
- [ ] **P4-28 MCP-Eval-Harness ausführen + Report triagieren (P4-27 follow-up)** — Harness liegt unter `mcp-server/eval/` (`evaluation.py` + `requirements.txt` aus dem Skill kopiert, `evaluation.xml` mit 10 verifizierten multi-hop Q&A über den Canon, `README.md`). Ausführen blockiert lokal: braucht **Python ≥ 3.10** (lokal nur 3.9.6), `ANTHROPIC_API_KEY` (kostet API-Budget), und `-m claude-sonnet-4-6` (Harness-Default `claude-3-7-sonnet-20250219` ist retired). Lauf: `pnpm -F @uianatomy/mcp-server build && python mcp-server/eval/evaluation.py -t stdio -c node -a mcp-server/dist/local.js -m claude-sonnet-4-6 -o mcp-server/eval/report.md mcp-server/eval/evaluation.xml`. Danach `report.md` lesen: Accuracy + per-Task Tool-Feedback → niedrige Accuracy in schärfere Tool-Descriptions/outputSchemas zurückspeisen (Frage nicht aufweichen). Eval-Antworten bei Canon-Änderung re-verifizieren. Datei: `mcp-server/eval/`.

## P5 — Phase 2 (Implementations)

- [~] **P5-34b sources[] pilot-migration (verification-required)** — Pilot phase landed for `button.yaml`. 8 library URLs migrated to `{url, library, verifiedAt}` object form (radix, reactAria, headlessUi, polaris, carbon, material3, atlassian, govuk); APG + MDN entries stayed bare-string per backlog rule. Verification protocol split: 3 audit-runner agents (radix / react-aria / headlessui — produced new `implementations/{radix,headlessui,react-aria}/button.yaml` Phase-2 drafts as side-effect, react-aria dir created) + 5 direct WebFetch verifications for design-systems where audit-runner is structurally inappropriate. **4 URL drifts caught and corrected in canon**: react-aria moved `react-spectrum.adobe.com/react-aria/Button.html` → `react-aria.adobe.com/Button` (301); polaris moved to `polaris-react.shopify.com` host (301); atlassian dropped `/examples` segment; govuk fully migrated from `gov.uk/service-manual` (404) to `design-system.service.gov.uk/components/button/`. `lastReviewed` bumped 2026-05-04 → 2026-05-05. Test-fixture updates: `mcp-server/tests/server.test.ts` 3 stale assertions about button-has-zero-implementations updated (now expects 3 audits: headlessui/radix/react-aria; radix lib-filter expects 6 components incl. button). Validation gates: shared build green, 286 tests grün (was 286 baseline, no regression), site build 137 pages clean. Pilot result: protocol works; cost per (lib, component) = ~120-190 s for audit-runner agents (sonnet, 9-21 tool-uses each), <30 s for direct WebFetch. **40 components remaining** for scale-out — see P5-34c. Erledigt-Pilot 2026-05-05. Dateien: `content/components/button.yaml`, `implementations/radix/button.yaml` (neu), `implementations/headlessui/button.yaml` (neu), `implementations/react-aria/button.yaml` (neu, neuer dir), `mcp-server/tests/server.test.ts`.
- [~] **P5-34c sources[] scale-out for remaining 40 components (P5-34b follow-up)** — Batches 2-4 landed 2026-05-05: accordion + alert + avatar + badge + banner + breadcrumbs + card + checkbox + avatar-group + code-block + combobox + disclosure migrated. **Batch 4 (avatar-group/code-block/combobox/disclosure)**: 17 lib URLs migrated (avatar-group 2: atlassian/primer; code-block 5: carbon/atlassian/primer/shiki/prism — first `shiki` + `prism` library entries in canon; combobox 6: reactAria/headlessUi/polaris/carbon/material3/atlassian; disclosure 5: radix/reactAria/headlessUi/polaris/carbon). 5 audit-runner agents (radix-disclosure, reactAria-disclosure, headlessUi-disclosure, reactAria-combobox refresh, headlessUi-combobox refresh — all draftet/refreshed cleanly). 2 dead URLs replaced (carbon /combo-box → /dropdown, atlassian /combobox → /select — both retired by upstream, closest-fit substitutes documented as drift). 7 drifts: polaris-host ×3 (combobox/disclosure/avatar-group already on new), atlassian /examples ×2 (code-block/code, combobox→select), reactAria-host ×2 (combobox + disclosure — `Disclosure.html` 301 → `/Disclosure`). 5 stay-bare in avatar-group (mui/mantine/chakra/primefaces/heroui — none in LIBRARY_KEYS); 2 stay-bare elsewhere (downshift, tpgi/inclusive-components non-libraries). No agent-draft YAML errors this batch — pre-emptive prompt-warning about colon-in-value worked. Drift-rate batch 4: ~40% (2 dead-replaced + 7 drift over ~22 URLs touched). Test-fixture: agent self-updated radix-lib-filter 11 → 12 (added disclosure) during its run — accepted intentionally per system note. **Batch 3 (banner/breadcrumbs/card/checkbox)**: 17 URLs migrated, 3 dead URLs dropped (m3-banners, atlassian-card, polaris-breadcrumbs), 9 drifts, 4 stay-bare, 4 audit-runners, 2 YAML parse fixes. **Batch 2 (accordion/alert/avatar/badge)**: 19 URLs migrated, 5 dead dropped, 8 drifts, 7 audit-runners. Cumulative progress: **13 of 41 components done** (button + accordion + alert + avatar + badge + banner + breadcrumbs + card + checkbox + avatar-group + code-block + combobox + disclosure); 61 lib URLs migrated; 28 components remaining. Erledigt-Batch-4 2026-05-05. Dateien: `content/components/{avatar-group,code-block,combobox,disclosure}.yaml`, `implementations/radix/disclosure.yaml` (neu), `implementations/react-aria/{combobox,disclosure}.yaml` (1 refresh + 1 neu), `implementations/headlessui/{combobox,disclosure}.yaml` (1 refresh + 1 neu), `mcp-server/tests/server.test.ts`. **Batch 3 (banner/breadcrumbs/card/checkbox)**: 17 lib URLs migrated (banner 4: polaris/carbon/atlassian/govuk; card 4: polaris/carbon/material3/radix; checkbox 5: radix/reactAria/material3/govuk/polaris; breadcrumbs 4: reactAria/atlassian/govuk/primer). 3 dead URLs dropped (m3-banners 404, atlassian-card 404 — Atlassian retired plain Card, polaris-breadcrumbs 404 — Shopify retired Polaris Breadcrumbs). 9 drifts: polaris-host ×4, atlassian /examples ×2 + /usage ×1, polaris path /forms→/selection-and-input ×1, govuk service-manual → design-system ×1. **First `primer` library entry** in canon (Primer breadcrumbs). 4 stay-bare (gdpr.eu, inclusive-components, mui, heroui — non-tracked sources). 4 audit-runner agents (radix-card, radix-checkbox, react-aria-checkbox, react-aria-breadcrumbs — all new). 2 agent-draft YAML parse errors fixed (unquoted strings with `(arg: type)` colon patterns: `boolean | 'indeterminate'` in radix/checkbox.yaml line 148, `onChange(isSelected: boolean)` in react-aria/checkbox.yaml line 263 — recurring failure mode worth flagging in audit-runner agent prompt). Test fixture update: `mcp-server/tests/server.test.ts` radix-lib-filter 9 → 11 components (added card + checkbox). Drift-rate batch 3: ~50% (3 dead + 9 drift over ~20 URLs touched). Validation: 286 tests grün, 137 site pages clean. **Batch 2 (accordion/alert/avatar/badge)**: 19 URLs migrated, 5 dead URLs dropped (atlassian-expand, m3-dividers/banners/avatar, react-aria-alert not-shipped), 8 drifts corrected (polaris-host × 4, react-aria-host × 1, atlassian /examples × 3), 2 stay-bare (chakra/mantine — not in LIBRARY_KEYS), 7 audit-runners (3 new radix + react-aria/accordion + headlessui/accordion refresh + radix/accordion refresh + 1 not-shipped), 1 agent canonicalRefPath fix (`[ArrowDown / ArrowUp (focus on trigger)]` → `[arrowUpDown]` per regex `[a-zA-Z0-9-]+`). Cumulative progress: **9 of 41 components done** (button + accordion + alert + avatar + badge + banner + breadcrumbs + card + checkbox); 44 lib URLs migrated; 32 components remaining. Erledigt-Batch-3 2026-05-05. Dateien: `content/components/{banner,breadcrumbs,card,checkbox}.yaml`, `implementations/radix/{card,checkbox}.yaml` (neu), `implementations/react-aria/{checkbox,breadcrumbs}.yaml` (neu), `mcp-server/tests/server.test.ts`. **19 library URLs migrated** to object form across the 4 components (accordion 5: radix/reactAria/headlessUi/polaris/carbon; alert 4: radix/polaris/carbon/atlassian; avatar 4: radix/polaris/carbon/atlassian; badge 6: polaris/carbon/material3/atlassian/radix/govuk). **5 dead URLs dropped from canon**: atlassian/components/expand (404 — Atlassian retired Expand component), m3.material.io/components/dividers/overview (404 — Material 3 dividers/accordion claim was always dubious), m3.material.io/components/banners/overview (404 — Material 3 banners gone), m3.material.io/components/avatar/overview (404 — Material 3 ships no Avatar), react-spectrum.adobe.com/react-aria/Alert.html (audit-runner confirmed React Aria does not ship an Alert primitive — `status: "not-shipped"`, no `implementations/react-aria/alert.yaml` written). **8 URL drifts corrected**: 4× polaris.shopify.com → polaris-react.shopify.com host migration; 1× react-spectrum.adobe.com/react-aria/Disclosure.html → react-aria.adobe.com/Disclosure (301); 3× atlassian /examples suffix dropped (button precedent — /examples 301-redirects to general overview). **2 sources stay bare-string** (chakra-ui.com, mantine.dev — not in `LIBRARY_KEYS` enum; schema permits unkeyed bare strings). 7 audit-runner invocations (3 new: radix/avatar, radix/alert, radix/badge, react-aria/accordion; 3 refreshed: radix/accordion, headlessui/accordion existing, headlessui/disclosure refresh; 1 not-shipped: react-aria/alert). 17 direct WebFetches (12 dispatched, all 15 verified via `curl -sIL` for ground truth — WebFetch summarization-truncation again proved unreliable on Carbon/Polaris). One audit-runner draft (`implementations/react-aria/accordion.yaml`) had 2 invalid `canonicalRefPath` entries with spaces/parens in `[...]` brackets (`[ArrowDown / ArrowUp (focus on trigger)]`) — fixed to `[arrowUpDown]` / `[homeEnd]` per the regex `[a-zA-Z0-9-]+` constraint; flagging this as a recurring failure mode for future scale-out batches. Test-fixture update: 1 stale assertion in `mcp-server/tests/server.test.ts` (`list_implementations({ libraryId: 'radix' })` 6 → 9 components, expanded to 9-element componentIds array). Drift-rate this batch: 13/24 = 54% (5 dead + 8 drift over 24 URLs touched), confirms 50% pilot estimate. Cumulative progress: **5 of 41 components done** (button + accordion + alert + avatar + badge); 27 lib URLs migrated; 36 components remaining. Validation gates: shared build green, 286 tests grün, site 137 pages clean. Erledigt-Batch-2 2026-05-05. Dateien: `content/components/{accordion,alert,avatar,badge}.yaml`, `implementations/radix/{accordion,alert,avatar,badge}.yaml` (1 refresh + 3 neu), `implementations/headlessui/accordion.yaml` (refresh), `implementations/react-aria/accordion.yaml` (neu), `mcp-server/tests/server.test.ts`.
- [ ] **P5-36 Library-Versionen Phase-4: auto-version-bump CI** — Script das LIBRARY_VERSIONS-keys via npm-registry / docs-sniff fetcht; PR öffnet wenn local pin lagged > N months. Defer until P5-33-baseline-versions gefüllt sind. Datei: `scripts/check-library-versions.mjs` + `.github/workflows/library-version-bump.yml`.
- [ ] **P5-37 sources[] bare-URL Restcleanup auf 3 bereits-migrierten Komponenten** — Audit 2026-05-05 fand bare-URL-Einträge in `sources[]` ohne `library`/`verifiedAt`-Keys: `avatar-group` (5: mui, mantine, chakra, primefaces, heroui), `avatar` (2: chakra, mantine), `checkbox` (1: mui.com). Diese Einträge stammen aus Pre-P5-34c-Phase und wurden bei den Batches übersehen weil ihre Libraries nicht in `LIBRARY_KEYS` enum sind — Schema akzeptiert bare strings, deshalb keine Zod-Fehler. Abhängig von **P6-152** (LIBRARY_VERSIONS-Expansion): Falls Mantine/Chakra/MUI/PrimeReact/HeroUI Einträge bekommen → auf strukturiertes `{url, library, verifiedAt}` heben. Andernfalls bare-string belassen mit Bewusstsein. Datei: `content/components/{avatar-group,avatar,checkbox}.yaml`.

## P6 — Reichweite (SEO + AI-Discovery)

- [x] **P6-158 /about page + best-practice-not-rules framing on three surfaces** — Authored `docs/about.md` (manifesto: "what this site is", "what it is *not*", "what canon means here", how-researched, how-to-use, source-of-truth). Site renders at `/about` via `site/src/pages/about.astro` (mirrors methodology.astro pattern — `?raw` import + `marked.parse` + AboutPage + TechArticle JSON-LD). Footer-nav `/about` first link; index.astro hero-lede gains "**Best-practice convergence, not a fixed rule book** — synthesised from W3C APG, MDN, WCAG, mature headless libraries, and production design systems" disclaimer plus "What is this?" hero-meta link. SKILL.md updated: prominent paragraph after first-paragraph framing the canon as best-practice synthesis; new `get_about` row in tools-table; bulk-load query and stale 26-tool-count fixed to 29; `Call get_about` advice woven into the framing paragraph. New MCP tool **`get_about`** returns `{ markdown, summary }` — markdown is the full `docs/about.md` body, summary is a 599-char hardcoded distillation suitable for verbatim paste into tool-call traces. Bundle pipeline: `shared/scripts/bundle-about.mjs` reads `docs/about.md` → emits `shared/dist/about-bundle.json` (chained into `pnpm -F shared build`); `loadAboutFromBundle` in `shared/src/bundle.ts` validates shape; runtime-disk path via `loadAbout({ aboutPath })` in `shared/src/loader.ts` (constant `ABOUT_SUMMARY` is single-source-of-truth shared between bundle script and runtime loader to prevent drift). `mcp-server/src/state.ts` gains `aboutCache` + `setAbout`/`getAbout`/`setAboutPromise`; `mcp-server/src/data.ts` gains `pickAboutPath` + `setAboutPath` mirroring sub-anatomy pattern (env override `UIANATOMY_ABOUT_PATH`). Worker `worker/index.ts` imports `@uianatomy/shared/about-bundle.json` and calls `setAbout(loadAboutFromBundle(...))` at module-init. New `./about-bundle.json` subpath-export added to `shared/package.json`. **Tool count 28 → 29.** New mcp-server test `get_about returns the project framing prose with markdown + summary` asserts shape + framing-keywords (`best-practice convergence`, `not as a fixed rule book`). Validation: 225 shared tests grün (unverändert), 69 mcp tests grün (was 68 +1), site build 138 pages clean (was 137 +1: /about; 0 neue broken-links). User-facing impact: when any agent calls the MCP server, the `get_about` tool description tells it to call this *before* relaying canonical claims — every downstream user inherits the "best-practice convergence with rationale, not regulation" framing as part of the canon's epistemic posture. Erledigt 2026-05-06. Dateien: `docs/about.md` (neu), `site/src/pages/about.astro` (neu), `site/src/pages/index.astro`, `site/src/layouts/Base.astro`, `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`, `shared/scripts/bundle-about.mjs` (neu), `shared/src/loader.ts`, `shared/src/bundle.ts`, `shared/package.json`, `mcp-server/src/state.ts`, `mcp-server/src/data.ts`, `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `worker/index.ts`.

- [ ] **P6-44 Backlinks + Outreach** (non-code, user-driven) — größter klassischer SEO-Hebel. Domain-Authority bei 0 ohne Backlinks. Konkrete Targets: (a) GitHub awesome-* listen — `awesome-design-systems`, `awesome-ui-component-library`, `awesome-react-components`, `awesome-a11y` PRs. (b) Show-HN ("UI Anatomy — canonical reference for UI component anatomy with MCP server"). (c) Designsystems.com community-Slack + Storybook-blog-pitch. (d) Refactoring UI / GOV.UK design-blog-mentions. (e) Twitter/Mastodon design-community. Out-of-scope für Code-Changes; wird vom Owner selbst getrieben. Backlog-Eintrag dient als Reminder + Tracking-Surface für gelandete Mentions.
- [x] **P6-45 PSI / Lighthouse audit + Core Web Vitals** — Lab-metrics-baseline gelandet via Lighthouse 13.2.0 gegen `wrangler dev` localhost (Cloudflare Workers Static Assets — same surface wie prod). Run-Setup: `fnm exec --using v22.22.2 -- npx -y lighthouse@latest …` mit `CHROME_PATH` auf `/Applications/Google Chrome.app/...` (Universal-Binary, arm64-arm gewählt durch arm-node), `--preset=desktop`, `--only-categories=performance,accessibility,best-practices,seo`. Pristine Run am 2026-05-05.

  **Home (`/`)** — Performance 95 / Accessibility 100 / Best-Practices 96 / SEO 100. FCP 0.4 s · LCP 0.6 s · TBT 180 ms · CLS 0.015 · SI 1.0 s.

  **Modal (`/components/modal`)** — Performance 92 / Accessibility 95 / Best-Practices 100 / SEO 100. FCP 0.6 s · LCP 0.8 s · TBT 220 ms · CLS 0.005 · SI 1.0 s.

  CWV alle in green-zone (LCP < 2.5 s ✓, TBT-as-INP-proxy ≤ 220 ms ✓, CLS < 0.1 ✓). **Localhost-bias:** Throttling-Method `simulate` modelt typischen Broadband-Stack auf Loopback — Werte sind optimistisch vs prod-CDN-fetch. PSI gegen `https://uianatomy.dev/` nach Deploy ist die echte Real-User-Baseline (separater Step, kein lokales Chrome nötig).

  **Real findings (3 follow-ups als P6-144/145/146 angehängt):** Home `errors-in-console` 2 SVG-rect-negative-width-Errors aus `renderAnatomySVG`; Modal `color-contrast` 3.22 statt 4.5 auf `.impl-card__library`-chip (`#1b1a17` ink auf `#b5471f` warm-Akzent); Modal `heading-order` skipt level (h2 → h4 in ImplementationCard divergence-section + I18nSection rtl-mirroring-block).

  Erledigt 2026-05-05. JSON-Outputs: `/tmp/lh-home.json`, `/tmp/lh-modal.json` (1.2 + 1.5 MB, ephemeral). Datei: `docs/backlog.md` (this entry + P6-144/145/146 follow-ups).
- [x] **P6-144 SVG `<rect>` negative-width regression** — `layoutGrid` clampte `innerWidth = area.w - GAP*(COLUMNS-1)` nicht — bei nested slots in already-1-col-Parents (Checkbox / RadioGroup `indicator`-slot parented to `input`) wurde innerWidth negativ → colWidth negativ → slotWidth negativ → `<rect width="-4.61…">`. Fix: `Math.max(0, …)`-clamp auf `innerWidth` in `layoutGrid` + `estimateNestedHeight`; defensiver `Math.max(0, w/h)`-clamp im rect-emit als Insurance. Test-guard in `shared/tests/schema.test.ts` "emits no negative-width rects across all canonical components" iteriert alle 41 components, parst alle `<rect>`-tags via Regex, asserted width ≥ 0 + height ≥ 0. Score-Impact verifiziert: Home Best-Practices 96 → 100. Tests 280 grün (war 279 +1). Erledigt 2026-05-05. Dateien: `shared/src/svg.ts`, `shared/tests/schema.test.ts`.
- [x] **P6-145 `.impl-card__library`-chip color-contrast 3.22 (a11y blocker)** — Chip-styling resolved `color: var(--surface-bg)` (undefined-token, fell zurück auf `var(--fg) = #1b1a17`) auf `background: var(--accent-active) = #b5471f`. Fix: explicit token-pair `color: var(--fg)` + `background: var(--accent-warm-soft)` + `border: 1px solid color-mix(--accent-warm 35%)`. Light: ink-on-peach ≈10:1, dark: cream-on-darkroast ≈10:1. Pass-comment dokumentiert reason. Bonus-fix entdeckt im selben Lighthouse-re-run: `[data-role='*'] .role-badge` (DESIGNER/DEV/BOTH eyebrow-badges) hatte `color: var(--accent-warm) = #b5471f` auf `--accent-warm-soft = #f4dcc9` = 4.09:1 (fail bei 10.5px). Fix: `color: color-mix(in oklab, var(--accent-warm) 70%, var(--fg))` darkens text-color ohne Affordance zu verlieren. Plus `.mismatch__index` (`#8c8678` ink-400 auf cool-tint = 3.13:1) → `var(--fg-muted)` (ink-700) für ≥4.5:1. Erledigt 2026-05-05. Dateien: `site/src/components/sections/ImplementationCard.astro`, `site/src/styles/global.css`, `site/src/components/sections/MismatchesTable.astro`.
- [x] **P6-146 heading-order skip auf Modal-page (h2 → h4)** — `<h4 class="impl-card__sub">Divergence</h4>` (ImplementationCard) und 2× `<h4>` (I18nSection rtl + textExpansion) skippten von umgebendem h2 auf h4. Fix: h4 → h3 in beiden Dateien; CSS-Selector in I18nSection mitgezogen (`.i18n__block h4` → `.i18n__block h3`). Erledigt 2026-05-05. Dateien: `site/src/components/sections/ImplementationCard.astro`, `site/src/components/sections/I18nSection.astro`.
- [x] **P6-49 Patterns Layer (stage 1: schema + first pattern + minimal site + MCP)** — author-first-before-commit-schema discipline (ADR-013-style) durchgeführt: Confirmation-Flow-pattern (Modal-alertdialog + destructive-Button) als first canonical pattern in `content/patterns/confirmation-flow.yaml`. **Login-form pivoted weg** weil canonical roster keinen plain `input`/`text-field` hat — backlog item P6-51 (text-input canonical) tracked es. Confirmation-Flow als first-pattern reicher in bridge-content (alertdialog vs dialog ist most-cited a11y-failure-category) und exercises das schema gut: 2-component composition, 4 decisions, 5 mistakes, 4 framework-skeletons. Schema (`shared/src/schema.ts`): `patternSchema` mit `composition: array.min(2)<{componentId (slug), role (prose), notes? (prose)}>`, `whenToUse: {use, avoid}` (strict), `decisions: array.min(1)<{question, answer, rationale}>` (strict), `mistakes: array.min(3)<mistakeSchema>` (reuses component schema), `frameworkSkeletons: {webComponents, react, vue, angularSignals}` alle required strings, optional `notes`, required `lastReviewed` ISO. Cross-refine `composition[].componentId` ∈ canonical-roster im consistency-test (Zod kann roster nicht sehen — same pattern wie vsRelated-cross-refine in P3-29). Loader (`loadPattern` + `loadPatterns`), Bundle (`loadPatternsFromBundle` + `bundle-patterns.mjs` build-script + `./patterns-bundle.json` subpath-export). Site routes: `/patterns` index + `/patterns/[id]` single-page (3-view-routing als stage-2-followup, ADR-024 D1 deferred). Page emit JSON-LD `@graph: [TechArticle, HowTo]` — HowTo ist legit hier weil patterns LITERAL how-tos sind (P3-39-Verdikt war "forced fit für components"; für patterns ist es spec-fit). Sections: composition (jeder slot mit role + notes, link zu canonical component-page), whenToUse (use+avoid dl), canonical-decisions (question→answer→rationale tripel), common-mistakes, framework-skeletons (alle 4 frameworks via expressive-code), notes-block, footer-lastReviewed. MCP: 3 neue tools (`list_patterns` + `get_pattern` + `get_patterns_for_component`), tool-count 19→22. Worker bundle-load erweitert. SKILL.md tool-table erweitert + count-zeile geflippt. Footer-nav um `/patterns`-link erweitert. Verifikation: `pnpm -r typecheck` 0 errors über 64 files (war 60); `pnpm -r test` 174 grün (139 shared + 35 mcp-server, +11 vs pre: 4 schema + 1 consistency + 5 mcp-server + 1 unaccounted); `pnpm -C site build` 77 pages (war 75, +2 patterns-pages), 0 broken links; pattern-page rendert alle sections, expressive-code 4-tab-style framework-skeletons, JSON-LD HowTo mit step-array. **Stage-1 out-of-scope** (gehören zu P6-49 stage 2/3): three-view-routing (D1 von ADR-024), `/patterns/[id]/dev` + `/bridge` URL-split, "Used in patterns"-section auf component-pages (B1, auto-derived), composition-SVG-renderer (F1, reuse `renderAnatomySVG`), per-pattern-OG-image, JSON-API endpoint `/api/patterns.json`, llms-md exclude für /patterns. Erste echte erfolgsmessung wenn 3-5 patterns im roster (drift-detection wird relevant). Erledigt 2026-05-01. Dateien: `content/patterns/confirmation-flow.yaml` (neu), `shared/src/schema.ts`, `shared/src/loader.ts`, `shared/src/bundle.ts`, `shared/scripts/bundle-patterns.mjs` (neu), `shared/package.json` (subpath-export + build-chain), `shared/tests/schema.test.ts`, `shared/tests/consistency.test.ts`, `mcp-server/src/state.ts`, `mcp-server/src/data.ts`, `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `worker/index.ts`, `site/src/content.config.ts`, `site/src/pages/patterns/index.astro` (neu), `site/src/pages/patterns/[id].astro` (neu), `site/src/layouts/Base.astro` (footer-link), `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`.
- [ ] **P6-52 `@uianatomy/skill` npm package + install CLI** — heavier alternative zur curl-one-liner aus SKILL.md. Package enthält die canonical SKILL.md + small `uianatomy-skill install`-CLI das `mkdir -p .claude/skills/uianatomy-mcp` + writes SKILL.md + writes `.mcp.json` mit MCP-config-snippet (Claude Code default) + optional appends to `.gitignore`. Pro: zero-config (`npx @uianatomy/skill install` reicht), version-pinned, idempotent. Contra: extra-publish-pipeline + maintenance, npm-registry-coupling. Trade-off-decision in eigener ADR (vermutlich ADR-025) bei landing. Trigger wenn ≥3 echte external-projects die curl-one-liner adopted haben + feedback signalisiert friction. Out-of-scope für P6-49 stage 1.
- [ ] **P6-74 i18n.rtl-restructure** — User-feedback: "prose. Could be structured: mirroredAxes, directionNeutralGlyphs, logicalProperties[]." ADR-017 chose prose deliberately for flexibility. Restructure touches 24 components (those with i18n block); improves queryability for "components-that-mirror-by-X-axis"-style queries. Defer until prose-search-pain ist real (heute: keine queries laufen gegen i18n-prose). Write ADR-017-followup ADR. Multi-session.
- [ ] **P6-78 frameworkMap framework expansion** — User-feedback: "missing Svelte, SolidJS, Lit, Qwik. `angularSignals` overly specific — drop suffix." Trade-off: adding frameworks dilutes per-component depth (each new framework = +25% prose); `angularSignals` was deliberate (signals-based-API specificity); rename loses precision. Defer until consumer-demand. Wenn landing: ADR-027 Framework Selection.
- [ ] **P6-84 migrate-tool / codemod-hints** — User-feedback: "no migrate tool — when canon renames `nonLinear` → `linear`, no diff/codemod hint." Heavy infra: codemod-tooling for canonical schema-renames. Premature — schema-renames sind selten, P6-72 severity-add ist erste echte migration die diese tool brauchen würde. Defer.
- [ ] **P6-85 testRecipes block** — User-feedback: "every component would benefit from a 'minimum a11y test' snippet for downstream test gen." New schema-field `testRecipes?: { framework, snippet }[]` — pre-baked test-snippets pro framework × component. Combines mit a11y-fixture-endpoint (P3-37). Heavy: schema + per-component authoring. Multi-session.
- [x] **P6-151 vsRelated bidirectional reverse-refs fehlen — 4 Hits aus Audit 2026-05-05** — **False-positive — closed without action.** Verifikation gegen `consistency.test.ts:248-281` (echter bidir-Lint mit `ALLOWED_ASYMMETRIC = new Set<string>()` — null Asymmetrie erlaubt) und gegen Ziel-YAMLs zeigt: alle 4 angeblich fehlenden Reverse-Refs existieren bereits. (a) `tile.yaml` hat `- id: avatar` (Z. ~700); (b) `menu-button.yaml` hat `- id: menu` (Z. ~660+); (c) `popover.yaml` hat `- id: menu` (Z. ~720+); (d) `list-item.yaml` hat `- id: table`. 225 shared tests grün bestätigt — Audit-Agents skimmten unzureichend tief, false-positive über alle 4 Hits. Lessons-learned für canon-auditor: Rule 2 in agent prompt ("vsRelated reverse-refs require literal file-read of the target") greift nur wenn Agent tatsächlich genug Zeilen liest; bei längeren YAMLs reichten 100-Zeilen-Reads nicht. Erledigt 2026-05-05 (no-op, false-positive). Datei: keine.
- [ ] **P6-152 LIBRARY_VERSIONS-Expansion (ADR-028 Followup) — 11+ unzitierte Libraries** — Audit 2026-05-05 fand Bibliotheks-Zitate in `frameworkMap` und `sources[]` ohne Eintrag in `shared/src/vocabulary.ts.LIBRARY_VERSIONS`. Cluster: `mantine` (8 Komponenten: avatar, avatar-group, badge, checkbox, menu, skeleton, switch, segmented-control), `chakraUi` (4: avatar, avatar-group, checkbox, skeleton), `heroui` (3: avatar-group, breadcrumbs, menu), `mui` (3 als bare URLs in avatar/checkbox/avatar-group; teilweise via `material3`-Alias erreichbar), `naiveUi` (3: badge, skeleton, switch), `vuetify` (2: segmented-control, skeleton), `primereact` (1: avatar-group — separat von `primevue`), `elementPlus` (1: tree-grid), `radixVue` (1: segmented-control), `downshift` (1: combobox), 4 Router-Libraries `nextRouter`/`reactRouter`/`angularRouter`/`vueRouter` (1: link). Pro Library entscheiden: Eintrag in LIBRARY_VERSIONS anlegen (Name + URL + ggf. Alias) oder Zitat im YAML entfernen / auf bereits gepinnte Library umlenken. Schließt **P5-37** ab sobald MUI/Mantine/Chakra/PrimeReact/HeroUI-Entscheid steht. Datei: `shared/src/vocabulary.ts` + 11+ component-yamls.
- [x] **P6-153 events.optional hedging-without-flag (P6-76 Compliance)** — `optional: true` ergänzt auf `search-input.queryChange` (Z. 468), `sidebar-nav.itemActivate` (Z. 543), `tooltip.openChange` (Z. 363). 3 Einzeiler-Edits, 225 shared tests grün. Erledigt 2026-05-05. Dateien: `content/components/{search-input,sidebar-nav,tooltip}.yaml`.
- [ ] **P6-154 link.notes prose → contracts migration (ADR-027 Followup)** — `link.yaml` `notes:`-Feld ist 542 Zeichen lang, überschreitet 400-Zeichen-Threshold für strukturierbare Inhalte. Inhalt beschreibt Designentscheid hinter Varianten-Leiter — passt nach `contracts.editorial` oder `contracts.designRationale`. `contracts`-Sektion existiert bereits, also reine Verschiebung + Strukturierung. Datei: `content/components/link.yaml`.
- [x] **P6-155 segmented-control: faktischer Rollen-Fehler in radio-group reverse-ref** — Verifikation gegen `segmented-control.yaml` (Z. 49 + 85-87) bestätigt: SegmentedControl nutzt `role="radiogroup"` plus roving-tabindex, identisch zu RadioGroup. Falscher `role="tablist"`-Claim ersetzt durch korrekte Aussage "Both use `role=\"radiogroup\"` with roving-tabindex; the difference is intent — form value (Radio Group) vs UI state (Segmented Control)". Erledigt 2026-05-05. Datei: `content/components/radio-group.yaml`.
- [x] **P6-156 tabs.propertyMap.Tab-Count: kind:enum → kind:number** — `tabs.yaml` Z. 657 `kind: enum` → `kind: number`. Einzeiler. 225 shared tests grün. Erledigt 2026-05-05. Datei: `content/components/tabs.yaml`.
- [ ] **P6-157 textarea.responsive-Sektion ergänzen** — `textarea.yaml` hat keine `responsive`-Sektion, obwohl `resize`-Axis und `minRows`/`maxRows`-Bounds direkte Implikationen unter Narrow-Viewports haben (WCAG 1.4.10 wird in `contracts` zitiert). Kurze Sektion mit Breakpoint-Verhalten + Kontextualisierung der WCAG-Referenz reicht. Datei: `content/components/textarea.yaml`.
- [x] **P6-148 sub-anatomy `header-bar` (third canonical sub-anatomy, ADR-033)** — Authored `content/sub-anatomies/header-bar.yaml` (2 slots: `header` wrapper region structural+heading-region + `title` heading content). Migrated 3 consumers: Modal, Drawer, Popover. **Modal becomes first triple-sub-anatomy component** (header-bar + close-button + action-group co-mounted via three separate `$ref` entries; close-button + title both reference `parent: header` so the structural relations are preserved). Per-consumer overrides: Modal/Drawer/Popover all override `header.required: false` (alert dialogs / filter panels / small popovers can omit); Drawer overrides `header.figma.hint` (inline-start edge) + `title.a11y.hint` (visually-hidden-title canon for drawers without visible titles); Popover overrides `header.tokens.spacing` to `spacing.tight` (smaller surface than Modal/Drawer's `spacing.compact`) + `title.required: false` (small popovers omit title; accessible name from trigger or body). **Closed Popover canon gap** — Popover's `header.purpose` previously claimed "grouping title and close affordance" but had no title slot; sub-anatomy migration gives Popover a canonical (optional) title slot for the first time. **Schema constraint discovered + documented in ADR-033**: `subAnatomySchema.slots` is `array(anatomySlotSchema)` only — sub-anatomies cannot nest other sub-anatomies via `$ref`. Workaround chosen (consumer mounts both header-bar and close-button as separate `$ref` entries with shared `parent: header`) is structurally equivalent in the resolved anatomy and exercises only the existing mechanism. **Accordion deliberately excluded** — Accordion's `header.code.semantic: heading` is canonical-different (APG accordion-pattern requires header-as-heading wrapping the toggle button, not heading-region wrapper containing a title). **Card/Banner/Alert deliberately excluded** — Card's eyebrow+title+subtitle is article-top-matter (vertical body-grid, not wrapper); Banner/Alert's title is body-grid-cell next to severity icon (horizontal grid, no wrapper region). Open question from initial Phase-1 plan ("Card/Banner/Alert oversight or canon?") resolved in ADR-033 favor of canonical-exclusion based on layout-pattern divergence. No schema/loader/bundle/site changes — third proof that the ADR-030 mechanism scales additively. 2 new mcp-server tests + 1 new shared consistency soft-lint (`heading-region` slots without `$ref` haystack — empty allowlist). 286 tests grün (224 shared + 62 mcp; was 283 = +1 shared +2 mcp). Site spot-check: Modal renders 3 sub-anatomy pill-clusters (`from action-group` 3× + `from close-button` 3× + `from header-bar` 2×); Drawer + Popover render 2× `from header-bar` each. 137 pages clean, 0 new broken-links. Erledigt 2026-05-05. Dateien: `content/sub-anatomies/header-bar.yaml` (neu), `docs/adr/033-header-bar-sub-anatomy.md` (neu), `content/components/{modal,drawer,popover}.yaml`, `mcp-server/tests/server.test.ts`, `shared/tests/consistency.test.ts`.
- [x] **P6-149 sub-anatomy `icon-leading-text` (fourth canonical sub-anatomy, ADR-034)** — Authored `content/sub-anatomies/icon-leading-text.yaml` (3 slots: `icon-leading` decorative + `label` content + `icon-trailing` decorative; `aria-hidden` on icons + label drives host's accessible name). 2 consumers migrated to `$ref: icon-leading-text`: Button (slot ids already match — direct reuse with per-slot tokens overrides for accent.fg colors + label.weight semibold + label.tracking + label.span 8) and List-item (slot ids `leading-icon`/`primary`/`trailing-icon` retired in canon, renamed to `icon-leading`/`label`/`icon-trailing` per ADR-034 — confirmed zero downstream impact, no `implementations/*/list-item.yaml` exists; per-slot overrides for muted/primary colors + label.weight medium + label.span 5 + icon-trailing.col 7). Token canonicalization: `spacing.gap` between icons and label + `typography.size: text.md` on label moved into the sub-anatomy (was redeclared at both call-sites). Foreground colors and label weight stay consumer-context-specific via merged token overrides (loader.ts:351-352 `mergeTokens` partial-merge). **Backlog roster overestimated (5 consumers expected)**: exploration showed only Button + List-item ship the 3-slot pattern in current canon; Menu-item structurally different (2-slot icon + kbd-shortcut, not icon-trailing); Tab and Breadcrumb-item ship no icon affordance at all → P6-150 (new) covers their canon-expansion. **Variable-arity `omitted`-override stays untested** by this ADR — both consumers use full slot set; P6-150 is the natural first `omitted` exercise. **No schema changes** — ADR-030 machinery covered the rollout entirely; fourth proof of additivity. Tests: 2 new mcp-server cases (`list_sub_anatomies returns icon-leading-text`, `get_sub_anatomy returns full icon-leading-text body`) + 1 new shared consistency soft-lint (`full icon-text composite prefers the icon-leading-text sub-anatomy ($ref) over inline declaration` — full-shape match on inline `icon-leading + label`, deliberately excludes partial adopters Badge with `icon-leading + content` and Link with `label + icon-trailing` which are tracked under P6-150). Soft-lint discovery: tighter shape-match matters — the original `id === icon-leading || icon-trailing` rule false-flagged Badge + Link partial adopters; revised to `inlineSlotIds.has('icon-leading') && inlineSlotIds.has('label')` after that surface. 289 tests grün (225 shared + 64 mcp-server; was 286 = +1 shared +2 mcp). Site spot-check: Button + List-item render `from icon-leading-text` provenance pills on the 3 resolved slots in dev + bridge views; 137 pages clean. **Initial schema-validation surprise**: per-slot override `layout` blocks need explicit `row` (refine `row || parent || overlay || floating`) — fixed at all 6 override sites (3 in Button, 3 in List-item). Erledigt 2026-05-05. Dateien: `content/sub-anatomies/icon-leading-text.yaml` (neu), `docs/adr/034-icon-leading-text-sub-anatomy.md` (neu), `content/components/{button,list-item}.yaml`, `mcp-server/tests/server.test.ts`, `shared/tests/consistency.test.ts`.
- [x] **P6-150 Tab + Breadcrumb-item icon-affordance canon-expansion** — All 4 P6-149 followup-consumers migrated to `$ref: icon-leading-text`: **Tab** (full 3-slot, no omit, parent: tab — first canon-expansion adding icon affordance to a tab anatomy; evidence cited Material 3 `LeadingIconTab` + Atlassian Tabs + Radix UI composability + APG no-prohibition), **Breadcrumb-item** (parent: link; `omitted: icon-trailing` — first variable-arity `omitted`-override in canon; rationale: separator slot is canonical between-items affordance, trailing-on-item collides), **Badge** (replaces inline `icon-leading + content` with $ref + `omitted: icon-trailing`; **slot-id rename `content` → `label`** per sub-anatomy convergence; `propertyMap` updated for the rename; rationale: Badge is W3C single-affordance status-glyph, trailing collides with Tag-Input pattern), **Link** (replaces inline `label + icon-trailing` with $ref + `omitted: icon-leading`; rationale: leading-icon collides with WCAG underline-onset; W3C link-purpose convention is trailing-only). `visited-marker` stays inline as decorative root-level slot. **Soft-lint tightened** in `shared/tests/consistency.test.ts`: full-shape match (`icon-leading + label`) generalized to `inline icon-leading || icon-trailing` — now catches Badge-like + Link-like partial-adopter drift in addition to full-3-slot drift; comment updated, P6-150 cited. **No schema changes** — `slotOverrideOmittedSchema` + `resolveAnatomyRefs` skip-on-omit (loader.ts:333) shipped with ADR-030 and worked first-try for variable-arity. Resolved-anatomy spot-check via bundle: tabs `[tablist, tab, icon-leading, label, icon-trailing, indicator, tabpanel]`; breadcrumbs `[..., link, icon-leading, label, current-marker, separator, overflow-collapse]`; badge `[root, icon-leading, label]` (no `content`, no `icon-trailing`); link `[root, label, icon-trailing, visited-marker]` (no `icon-leading`). Tests **293 grün** (225 shared + 68 mcp; was 289 = +4 mcp-server P6-150 resolution tests + extended `get_canonical_vocabularies` to assert `icon-leading-text` in subAnatomies-list). MCP test-shape-discovery: bundle JSON-serialization strips non-enumerable `__subAnatomy` provenance (P6-126b note — only site's `content.config.ts` lifts it), so resolution-tests fingerprint by slot-id-presence + first-slot `layout.parent` rather than by provenance marker. Site build 137 pages clean. ADR-034 amended with status-update note ("P6-150 landed; reuse-roster now 6; `omitted`-override exercised 3×"). icon-leading-text reuse-roster: **Button + List-item + Tab + Breadcrumb-item + Badge + Link** = 6 consumers (was 2 at ADR-acceptance). Erledigt 2026-05-05. Dateien: `content/components/{tabs,breadcrumbs,badge,link}.yaml`, `shared/tests/consistency.test.ts`, `mcp-server/tests/server.test.ts`, `docs/adr/034-icon-leading-text-sub-anatomy.md`.
- [x] **P6-126c orphan `AnatomyTable.astro` — delete or revive** — Deleted. Decision: revive-path adds zero value (Slot column already in FigmaSlotTable on Designer; `purpose`-prose already conveyed via SVG `<title>`-tooltip; Bridge would carry 3 stacked tables). File was wired with P6-126b SubAnatomyBadge for symmetry only — pure dead code. ADR-030 line 76 historical-context mention left intact. Site typecheck 77 files (was 78), 280 tests grün, 137 pages clean. Erledigt 2026-05-05. Datei: `site/src/components/sections/AnatomyTable.astro` (deleted).
- [x] **P6-147 sub-anatomy `close-button` (second canonical sub-anatomy)** — Authored `content/sub-anatomies/close-button.yaml` (3 slots: `close-button` interactive host + `close-icon` decorative aria-hidden + `close-label` content visually-hidden), per [ADR-032](./adr/032-close-button-sub-anatomy.md). Six consumers migrated to `$ref: close-button`: Modal / Drawer / Popover keep their existing `close-button` slot id (nested under `header`); Alert / Banner / Toast renamed from legacy `dismiss-button` slot id to canonical `close-button`. Canonical-name decision: `close` over `dismiss` — spec-anchored (WAI-ARIA dialog "close", MDN `HTMLDialogElement.close()`, Radix `Dialog.Close`, Headless UI `Dialog.Close`). Three-slot composite over single-slot: counting `close-label` as a slot makes the WCAG 4.1.2 / APG icon-button accessible-name dependency structurally visible in every consumer's anatomy table, not just in `a11y.hint` prose. Per-consumer overrides minimal: each consumer overrides `close-button.required: false` (close is optional in every host — visible only when `dismissible: true` or when the host pattern explicitly requires explicit close), overrides `close-button.layout` for col/span/parent, and overrides `close-label.a11y.hint` to encode the host-specific accessible-name canon ("Close dialog" / "Close filters" / "Dismiss <severity> message" / "Close notification" / "Dismiss notification"). No consumer overrides `close-icon` — the decorative-aria-hidden rule is invariant. Token canonicalization: spacing.tight + radius.sm + color.text.muted + color.border.focus moved into the sub-anatomy (was redeclared at all 6 call-sites). **No schema changes** — ADR-030 machinery (`subAnatomySchema`, `anatomySlotRefSchema`, `resolveAnatomyRefs`, bundle pre-resolution, `componentSchema.transform()`, `SubAnatomyBadge.astro`) covered this rollout entirely; second proof that the mechanism scales additively. Tests: 2 new mcp-server cases (`list_sub_anatomies returns close-button`, `get_sub_anatomy returns full close-button body`) + 1 new shared consistency soft-lint (`close-button slots prefer the close-button sub-anatomy ($ref) over inline declaration` — empty-array assertion enforces zero drift, no allowlist needed; pattern matches `^(close|dismiss)(-button)?$` against `code.semantic: button` slots that lack `__subAnatomy` provenance). 283 tests grün (223 shared + 60 mcp; war 280 = +2 mcp +1 shared). Site spot-check: all 6 consumer pages render `from close-button` provenance pills (Modal renders both `from action-group` and `from close-button` — first component to mount two sub-anatomies). 137 pages clean, 0 new broken-links, typecheck 0 errors. **`dismiss-button` slot id retired without compat shim** — clean rename documented in ADR-032; no implementations file references the legacy id, no MCP query is known to depend on it. Erledigt 2026-05-05. Dateien: `content/sub-anatomies/close-button.yaml` (neu), `docs/adr/032-close-button-sub-anatomy.md` (neu), `content/components/{modal,drawer,popover,alert,banner,toast}.yaml`, `mcp-server/tests/server.test.ts`, `shared/tests/consistency.test.ts`.
- [x] **P6-143 `SEVERITY_SYNONYMS` decommission (P6-127 follow-up)** — Server-side `SEVERITY_SYNONYM_REVERSE` reverse-index + query-side `expanded`-array logic in `mcp-server/src/server.ts` removed. `search_components` now does single-query haystack scan; per-variant `alternativeNames` (P6-127) feed the haystack directly. `SEVERITY_SYNONYMS` retained in `shared/src/vocabulary.ts` as canonical synonym-vocabulary documentation (still exposed via `get_canonical_vocabularies`-tool); only the consumer in mcp-server changed. **Badge severity-cluster backfill** added in same change (war bei P6-127 vergessen): badge.warning → [caution, attention], badge.error → [danger, destructive, critical] — required for `search_components({query:'caution'})` to keep hitting Badge via the haystack route post-decommission. Tool-description rewritten: per-variant alternativeNames named als kanonischer Surface; SEVERITY_SYNONYMS-Verweis raus. 2 mcp-server-tests umbenannt + Kommentare updated (P6-118b → P6-143). Tests 279 grün (221 shared + 58 mcp, unchanged from P6-127). Site-build 137 pages, 0 neue broken-links. Spot-check: Badge rendert jetzt `aka caution · attention` + `aka danger · destructive · critical` aliases-rows. Erledigt 2026-05-05. Dateien: `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `content/components/badge.yaml`.
- [x] **P6-127 per-variant alternative names** — `axes.variants` reshape von `string[]` auf `Array<{ name, alternativeNames? }>` per [ADR-031](./adr/031-variant-alternative-names.md). 41 component-yamls migriert via `scripts/migrate-variants-shape.py` (109 entries, idempotent dry-run + apply). 7 high-value alternativeNames-backfills (toast/alert/banner severity-cluster: error → [danger, destructive, critical], warning → [caution, attention]; modal.alertdialog → [confirm, destructive]). Drawer/Stepper-Pläne aus Plan verworfen weil `side`/`linear` properties sind, nicht variants — scope-correction. `axesSchema.superRefine` erweitert (unique-name + no-overlap + no-self-duplicate); `variantDeprecations`-Refine liest `axes.variants.map(v => v.name)`. Consumers: `compare.ts` setDiff auf `v.name`, `validate.ts` haystack auf `[v.name, ...alternativeNames]`, `mcp-server` `search_components` haystack erweitert + tool-description aktualisiert (ADR-031-Verweis), `AxesTable.astro` rendert italic muted "aka X · Y"-row unter Variant-Chip wenn alternativeNames non-empty (CSS `.variant-aliases`), `consistency.test.ts` SEVERITY_AXIS_REGISTRY iteriert `v.name`. 6 neue schema-tests (object-shape, alternativeNames optional, bare-string-reject, duplicate-name-reject, alias-collision-reject, alias-own-name-reject), 1 neuer mcp-server search-test (`search_components({query:'confirm'})` hits Modal via variant-alternativeNames-haystack — distinct from severity-synonym-path). Tests 279 grün (221 shared + 58 mcp; war 272 = +6 schema +1 mcp). 137 site-pages, 0 neue broken-links. Site spot-check: Modal rendert "aka confirm · destructive" auf alertdialog-chip; Toast rendert beide severity-aliases-rows; Button negative-control 0 aliases-rows. Erledigt 2026-05-05. Dateien: `docs/adr/031-variant-alternative-names.md` (neu), `shared/src/schema.ts`, `shared/src/compare.ts`, `shared/src/validate.ts`, `mcp-server/src/server.ts`, `site/src/components/sections/AxesTable.astro`, `shared/tests/{schema,consistency}.test.ts`, `mcp-server/tests/server.test.ts`, `docs/schema.md`, `scripts/migrate-variants-shape.py` (neu, one-shot), `content/components/{toast,alert,banner,modal}.yaml` (alternativeNames-backfills) plus alle 41 component-yamls (variants-shape-migration).

## Empfohlener Pfad

**Phase 1–2 + Phase-2 audit infrastructure complete + sub-anatomy machinery shipped + 4 canonical sub-anatomies live (action-group + close-button + header-bar + icon-leading-text) + Modal as first triple-sub-anatomy component + provenance UI surfaced + per-variant alternativeNames (ADR-031) shipped + SEVERITY_SYNONYMS decommissioned + Lighthouse 100/100/100 a11y+BP+SEO baseline + first variable-arity `omitted`-override exercised three times (P6-150).** Component canon at 41 entries; **`action-group`** (ADR-030) reused across Card / Alert / Banner / Modal / Drawer footer; **`close-button`** (P6-147 / ADR-032) reused across Modal / Drawer / Popover / Alert / Banner / Toast; **`header-bar`** (P6-148 / ADR-033) reused across Modal / Drawer / Popover; **`icon-leading-text`** (P6-149 / ADR-034 + P6-150) reused across **6 consumers**: Button / List-item (full 3-slot) + Tab (full 3-slot, canon-expansion) + Breadcrumb-item (`omitted: icon-trailing`) + Badge (`omitted: icon-trailing` + `content→label` rename) + Link (`omitted: icon-leading`). **Modal mounts three orthogonal sub-anatomies** via separate `$ref` entries (header-bar at top, close-button parented to header, action-group parented to footer) — proof that the mechanism stacks cleanly. Site renders `from <sub-anatomy>` pills on every resolved slot in dev + bridge views (293 tests grün — 225 shared + 68 mcp-server, 27 MCP tools); `axes.variants` is object-shape with optional `alternativeNames`. Lighthouse post-fix: Home 97 perf / 100 a11y / 100 BP / 100 SEO; Modal 87 perf (variance) / 100 a11y / 100 BP / 100 SEO. Active path now: **P5-34c sources[] migration batch-5** (28 components remaining; resumable in groups of 4). **P5-34c sources[] migration paused at 13/41 components** — batches 1-4 landed (button + accordion/alert/avatar/badge + banner/breadcrumbs/card/checkbox + avatar-group/code-block/combobox/disclosure); 61 lib URLs verified + drift-corrected; 28 components remaining, resumable per P5-34c entry in groups of 4. **P2-17 closed 2026-05-06** — propertyMap auf checkbox/radio-group/switch/textarea (47 Einträge gesamt); letztes echtes Major-Finding aus Audit-Pass 2026-05-05 erledigt. **P6-158 closed 2026-05-06** — `/about` page + best-practice-not-rules framing across SKILL.md + new MCP tool `get_about` (28 → 29 tools); framing now surfaced at every consumer surface so downstream users inherit the "convergence with rationale, not regulation" posture.

Done 2026-05-06 batch (chronological):
- P6-158 /about page + best-practice-not-rules framing on three surfaces: new `docs/about.md` manifesto + `site/src/pages/about.astro` rendering at `/about` (footer-nav + hero-meta link); SKILL.md gains prominent best-practice-convergence-not-rules paragraph + new `get_about` tools-table row + bulk-load query + 26→29 tool-count fix; new MCP tool `get_about` returns `{ markdown, summary }` with hardcoded 599-char distillation suitable for verbatim paste into tool-call traces; bundle-pipeline `shared/scripts/bundle-about.mjs` chained into shared build, `loadAboutFromBundle` shape-validates, `loadAbout({ aboutPath })` is the runtime-disk path, `ABOUT_SUMMARY` constant in `loader.ts` is single-source-of-truth for both bundle and runtime to prevent drift. Worker imports `@uianatomy/shared/about-bundle.json` at module-init. State + data layers extended with `aboutCache`/`setAbout`/`getAbout`/`pickAboutPath`/`setAboutPath` mirroring sub-anatomy pattern (env override `UIANATOMY_ABOUT_PATH`). Tool-count 28→29. New mcp-server test asserts framing keywords (`best-practice convergence`, `not as a fixed rule book`). 138 site pages clean (137 +1), 69 mcp tests grün (68 +1), 225 shared tests grün unverändert.
- P2-17 propertyMap auf 4 Form-Komponenten: checkbox (12 Einträge), radio-group (12), switch (10), textarea (13) — total 47 Einträge. Quelle: `axes.variants` → `Variant` enum, `axes.properties` (primitive→boolean / enum→enum / number→number), Anatomie-Slots → slot/text, controlled-state-Props (checked/value) wo strukturell zentral. `notes`-Prosa verdichtet aus `mismatches`/`mistakes`-Begründungen, kein Neuschreiben. Insertion-Position pro text-input-Konvention zwischen `events:` und `formIntegration:`. Keine Schema-Änderungen — `propertyMapEntrySchema` deckte alle Shapes ab. Validation 293 tests grün (225 shared + 68 mcp, unverändert), site-build 137 pages clean. Schließt erstes Major-Finding aus Audit-Pass 2026-05-05.

Done 2026-05-05 batch (chronological):
- Audit-Pass über alle 41 kanonischen Komponenten gelandet (canon-auditor in 3 Parallel-Batches à 14/14/13 Agents). Coverage: 41/41 auditiert · 22 sauber · 19 mit Findings · 0 critical · 8 major (4 davon false-positive) · ~25 minor. **22 saubere Komponenten:** accordion, alert, banner, button, card, code-block, disclosure, drawer, grid-pattern, icon, list-item, menu-button, modal, pagination, popover, progress, select, stepper, tag-input, text-input, tile, toast. **Quick-wins gleich abgearbeitet (Pfad A):** P6-153 (events.optional auf 3 Events), P6-155 (segmented-control-Rolle), P6-156 (tabs Tab-Count kind:number) — alle 3 in eine Session. **P6-151 als false-positive geschlossen (Pfad B):** Verifikation zeigte alle 4 angeblich fehlenden vsRelated-Reverse-Refs existieren bereits — bidir-Lint grün ist die Wahrheit, canon-auditor-Agents lasen unzureichend tief in längere YAMLs. **Offen bleiben:** P2-17 (propertyMap auf 4 Form-Komponenten — echter Major), P5-37 (sources[]-bare-URL-Restcleanup), P6-152 (LIBRARY_VERSIONS-Expansion — 11+ Libraries), P6-154 (link.notes→contracts), P6-157 (textarea.responsive). Lessons-learned: canon-auditor-Agent-Rule-2 greift nur bei echten Volltext-Reads des Ziels — agent-prompt Verschärfung für nächsten Audit-Pass könnte 4× false-positive auf vsRelated-Dimension verhindern. 225 shared tests grün durchgehend.
- P6-150 Tab + Breadcrumb-item icon-affordance canon-expansion (P6-149 / ADR-034 followup): all 4 deferred consumers migrated to `$ref: icon-leading-text` in a single commit. **Tab** (full 3-slot, canon-expansion adding icon-leading + icon-trailing affordance to the tab anatomy for the first time; cited Material 3 LeadingIconTab + Atlassian Tabs + Radix UI composability). **Breadcrumb-item** (parent: link, `omitted: icon-trailing` — first variable-arity `omitted`-override in canon; trailing competes with the canonical separator slot). **Badge** (replaces inline `icon-leading + content` with $ref + `omitted: icon-trailing`; **slot rename `content` → `label`** per sub-anatomy convergence; propertyMap updated). **Link** (replaces inline `label + icon-trailing` with $ref + `omitted: icon-leading`; leading collides with WCAG underline-onset; visited-marker stays inline). Soft-lint generalized in `shared/tests/consistency.test.ts` from full-shape match (`icon-leading + label`) to `inline icon-leading || icon-trailing` — now catches partial-adopter drift. **No schema changes** — `slotOverrideOmittedSchema` + `resolveAnatomyRefs` skip-on-omit (loader.ts:333) shipped with ADR-030 and worked first-try for variable-arity. icon-leading-text reuse-roster grew 2 → 6 consumers; `omitted`-override exercised 3×. ADR-034 amended with status-update note. 293 tests grün (was 289 = +4 mcp-server P6-150 resolution tests + extended `get_canonical_vocabularies` to assert `icon-leading-text` in subAnatomies-list). Resolution tests fingerprint by slot-id + first-slot `layout.parent` (bundle JSON-strips non-enumerable `__subAnatomy` provenance per P6-126b — only site lifts it). 137 site pages clean.
- P6-149 sub-anatomy `icon-leading-text` (fourth canonical sub-anatomy, ADR-034): `content/sub-anatomies/icon-leading-text.yaml` with 3 slots (icon-leading decorative + label content + icon-trailing decorative); 2 consumers migrated (Button + List-item — slot ids `leading-icon`/`primary`/`trailing-icon` retired in canon, renamed to Button-canonical ids per ADR-034). Backlog roster overestimated (5 expected, only 2 fit current canon) — Tab/Breadcrumb-item/Badge/Link tracked under new P6-150 followup as variable-arity `omitted`-override candidates. Token canonicalization: `spacing.gap` + `typography.size: text.md` moved into sub-anatomy; foreground colors stay consumer-specific via merged token overrides. No schema changes — fourth proof of ADR-030 additivity. 2 new mcp-server tests + 1 new shared consistency soft-lint with full-shape match (`inlineSlotIds.has('icon-leading') && inlineSlotIds.has('label')`) — tighter shape needed to avoid false-positives on Badge (`icon-leading + content`) and Link (`label + icon-trailing`) partial adopters. Schema-validation surprise: per-slot override `layout` blocks need explicit `row` per layoutHintSchema refine — fixed at all 6 override sites. 289 tests grün (was 286 +3); 137 pages clean.
- P5-34c sources[] batch-4 (avatar-group/code-block/combobox/disclosure): 17 lib URLs migrated, 2 dead URLs replaced (carbon /combo-box → /dropdown, atlassian /combobox → /select — both retired upstream, closest-fit substitutes), 7 drifts (polaris-host × 3, atlassian /examples × 2, reactAria /Disclosure.html → /Disclosure × 2). 5 audit-runner agents (radix-disclosure + reactAria-disclosure + headlessUi-disclosure + reactAria-combobox refresh + headlessUi-combobox refresh). First **`shiki`** + **`prism`** library entries actually used in canon (code-block.yaml — keys were registered in LIBRARY_VERSIONS but had zero canon usage before). 5 stay-bare in avatar-group (mui/mantine/chakra/primefaces/heroui — none in LIBRARY_KEYS). No agent-draft YAML errors — pre-emptive prompt-warning about colon-in-value worked. Cumulative: 13/41 components, 61 URLs migrated.
- P5-34c sources[] batch-3 (banner/breadcrumbs/card/checkbox): 17 lib URLs migrated, 3 dead URLs dropped (m3-banners 404, atlassian-card 404 — Atlassian retired plain Card, polaris-breadcrumbs 404 — Shopify retired Polaris Breadcrumbs), 9 drifts (polaris-host × 4, atlassian /examples × 2 + /usage × 1, polaris path /forms → /selection-and-input × 1, govuk service-manual → design-system × 1). First **`primer`** library entry in canon (Primer breadcrumbs). 4 audit-runner agents (radix-card + radix-checkbox + reactAria-checkbox + reactAria-breadcrumbs). 2 agent-draft YAML parse errors fixed (unquoted colon-in-value pattern: `boolean | 'indeterminate'`, `onChange(isSelected: boolean)`). Test fixture: radix-lib-filter 9 → 11. Drift-rate ~50%. Cumulative: 9/41 components, 44 URLs migrated.
- P5-34c sources[] batch-2 (accordion/alert/avatar/badge): 19 library URLs migrated to object form, 5 dead URLs dropped from canon (atlassian-expand 404, 3× m3-* 404, react-aria-alert not-shipped), 8 URL drifts corrected (polaris-host × 4, react-aria-host × 1, atlassian /examples × 3), 2 stay-bare (chakra/mantine — not in LIBRARY_KEYS). 7 audit-runner agents (3 new radix audits + react-aria/accordion + headlessui/accordion refresh + radix/accordion refresh + 1 not-shipped). 1 invalid canonicalRefPath in agent draft fixed (`[ArrowDown / ArrowUp (focus on trigger)]` → `[arrowUpDown]` per regex). Test fixture update: radix-lib-filter 6 → 9. Drift-rate 13/24 = 54% confirms pilot 50% estimate. Cumulative: 5/41 components, 27 URLs migrated.
- P5-34b sources[] pilot-migration (button.yaml only): 8 library URLs migrated bare-string → `{url, library, verifiedAt}` object form per existing `sourceEntrySchema` (no schema changes). Verification split: 3 audit-runner agents (radix/react-aria/headlessui — Phase-2 implementation YAMLs created as side-effect, new `implementations/react-aria/` dir) + 5 direct WebFetches (polaris/carbon/material3/atlassian/govuk where audit-runner is design-system-inappropriate). **4 URL drifts caught**: react-spectrum.adobe.com → react-aria.adobe.com (301), polaris.shopify.com → polaris-react.shopify.com (301), atlassian dropped `/examples`, govuk service-manual → design-system.service.gov.uk (was 404). 3 stale `mcp-server/tests/server.test.ts` fixture-assertions updated (button-has-zero-impls → button-has-three-impls; radix-lib-filter 5 → 6 components). 286 tests grün; site 137 pages clean. Pilot result frames cost for P5-34c scale-out across remaining 40 components (~150 s/audit-runner-agent × 3 eligible libs + ~25 s/WebFetch × 5 design-systems; expect ~50% URL-drift-rate based on pilot 4/8).
- P6-148 sub-anatomy `header-bar` (third canonical sub-anatomy, ADR-033): `content/sub-anatomies/header-bar.yaml` with 2 slots (`header` wrapper region structural + `title` heading content); 3 consumers migrated (Modal / Drawer / Popover). Modal becomes first triple-sub-anatomy component. Closed Popover canon gap — gave Popover a canonical optional title slot for the first time. Schema constraint discovered: `subAnatomySchema.slots` cannot nest `$ref` (consumer-level co-mount used instead, structurally equivalent). Accordion / Card / Banner / Alert deliberately excluded (different layout patterns documented in ADR). No schema changes — third proof of mechanism additivity. 2 new mcp-server tests + 1 new shared consistency soft-lint (`heading-region`-without-`$ref` haystack). 286 tests grün (was 283 +3).
- P6-147 sub-anatomy `close-button` (second canonical sub-anatomy, ADR-032): `content/sub-anatomies/close-button.yaml` with 3 slots (interactive host + decorative icon + content accessible-name carrier); 6 consumers migrated (Modal / Drawer / Popover / Alert / Banner / Toast — last three renamed from legacy `dismiss-button` slot id without compat shim). Per-consumer overrides minimal: `close-button.required → false`, layout col/span/parent per host grid, `close-label.a11y.hint` for host-specific accessible-name canon. Token canonicalization absorbed (was redeclared at all 6 call-sites). No schema changes — ADR-030 machinery covered the rollout entirely; second proof that the sub-anatomy mechanism scales additively. 2 new mcp-server tests + 1 new shared consistency soft-lint. 283 tests grün (was 280 +3); 137 pages clean; first dual-sub-anatomy component (Modal) renders both `from action-group` and `from close-button` pills.
- P6-144/145/146 quick-bundle Lighthouse-fixes: SVG layoutGrid innerWidth-clamp + defensive rect-clamp (Home BP 96→100, Modal BP 100); impl-card__library chip on `--accent-warm-soft` (no longer ink-on-warm-orange 3.22:1) + role-badge text darkened via color-mix 70/30 + mismatch__index ink-400→fg-muted (Modal a11y 95→100); h4→h3 in ImplementationCard divergence-section + I18nSection rtl/textExpansion-blocks (Modal a11y 95→100). Final scores 100/100/100 a11y+BP+SEO on both Home and Modal.
- P6-45 Lighthouse Lab-metrics-baseline (arm-node v22.22.2 + Chrome universal-binary + wrangler dev): Home 95/100/96/100, Modal 92/95/100/100; CWV green-zone (LCP ≤ 0.8 s, TBT ≤ 220 ms, CLS ≤ 0.015). 3 real findings als P6-144/145/146 follow-ups: SVG-rect-negative-width-error im Home-anatomy-render, color-contrast 3.22 auf impl-card__library-chip, heading-order-skip h2→h4 in ImplementationCard + I18nSection.
- P6-143 `SEVERITY_SYNONYMS` decommission (P6-127 follow-up): `SEVERITY_SYNONYM_REVERSE` reverse-index + query-side expansion in `mcp-server/src/server.ts` entfernt; `search_components` läuft jetzt single-query haystack-scan über variant-`alternativeNames`. Badge severity-cluster aliases backfilled (war bei P6-127 vergessen). Tool-description rewritten — per-variant `alternativeNames` ist die canonical-cross-system-surface. `SEVERITY_SYNONYMS` bleibt in `shared/src/vocabulary.ts` als doc-Surface (via `get_canonical_vocabularies` exposed). Tests 279 grün unverändert.
- P6-127 per-variant `alternativeNames` (ADR-031): `axes.variants` reshape von `string[]` auf `Array<{ name, alternativeNames? }>`; 41 yamls migriert via `scripts/migrate-variants-shape.py`; 7 high-value backfills (toast/alert/banner severity-cluster + modal.alertdialog); axesSchema.superRefine extended (unique-name + no-overlap + no-self-duplicate); consumers `compare.ts` / `validate.ts` / `mcp-server search_components` (haystack-Erweiterung) / `AxesTable.astro` (italic "aka …"-row unter Variant-Chip) / `consistency.test.ts` (SEVERITY_AXIS_REGISTRY auf v.name) updated; 6 schema-tests + 1 mcp-server search-test; 279 tests grün (war 272 +7).
- ADR-030 sub-anatomy + 5-component migration: `subAnatomySchema` + `anatomySlotRefSchema` + `resolveAnatomyRefs` eager-resolution + `content/sub-anatomies/action-group.yaml` (3 slots: primary/secondary/tertiary-action + a11y groupRule/focusRule). Card/Alert/Banner inline `actions` slot replaced with `$ref`; Modal/Drawer keep `footer` wrapper + add child `$ref` with `parent: footer`; Toast deliberately excluded (single-button, structurally not a group). 2 new MCP tools (`list_sub_anatomies`, `get_sub_anatomy`); `search_components` + `get_canonical_vocabularies` extended; worker bundle pre-resolves at bundle-write time so the worker is sub-anatomy-agnostic. Site `content.config.ts` resolves refs at Astro content-parse via `componentSchema.transform()` reading the sub-anatomies bundle. Soft-lint in `consistency.test.ts` enforces zero drift with WRAPPER_ALLOWLIST for Modal/Drawer footer. Backlog citation of ADR-029 was a numbering bug — corrected to ADR-030.
- P6-126b sub-anatomy provenance UI badge: new `ResolvedAnatomySlot` type (additive on `AnatomySlot` with optional readonly `__subAnatomy`), `Component.anatomy` retyped to `ResolvedAnatomySlot[]`, new `<SubAnatomyBadge entry={s} />` component wired into `FigmaSlotTable` + `CodeSlotTable` + orphan `AnatomyTable`. Caveat caught mid-flight: Astro 5's content layer JSON-caches glob entries → strips non-enumerable own properties, so the original "non-enumerable property survives Astro parse" assumption was wrong for static-build cache hits; fix lifts `__subAnatomy` enumerably in the site `content.config.ts` transform only, leaving the canonical loader contract (non-enumerable, JSON-invisible) intact. New follow-up P6-126c logged for the orphan `AnatomyTable.astro` (delete vs. revive).

Done 2026-05-04 batch (chronological):
- New canon: `table` (P6-99 — 35. component; 12-slot anatomy mit scrollable-region wrapper + native `<table>` semantics + selectable variant + 9 properties; 6 events; 5 nonNegotiable contracts gated by WCAG 1.3.1 + 4.1.2 + APG; 7-system vocabularyDrift; pending forward-refs zu grid-pattern + tree-grid)
- New canon: `pagination` (P6-109 — 36. component; paart sich mit table als data-page-navigation; 12-slot anatomy mit nav-landmark + 3 strukturelle Variants numbered/prev-next-only/load-more; 6 number/text propertyMap-only fields kapseln Material UI's siblingCount + boundaryCount truncation API; 5 nonNegotiable contracts incl. disabled-not-removed canon-rule; 7-system vocabularyDrift; 2 events; no PERFORMANCE_REQUIRED entry per registry-discipline)
- New canon: `progress` (P6-106 — 37. component; 2 strukturelle variants linear/circular; 8-slot anatomy mit role=progressbar root + announcement aria-live-polite milestone-only; determinate boolean positive-default per polarity-konvention; 5 contracts incl. omit-aria-valuenow-when-indeterminate APG explicit rule + prefers-reduced-motion fallback WCAG 2.3.3; eventsRationale state-reflective-no-events; native `<progress>` als prose-only alternate-implementation; 8-system vocabularyDrift incl. WAI-ARIA + HTML + 6 design systems)
- New canon: `code-block` (P6-113 — 38. component; 2 strukturelle variants block `<pre><code>` chrome / inline bare `<code>`; 8-slot anatomy mit syntax-highlight tokens als decorative aria-hidden + line-numbers via CSS counter-increment so they don't enter clipboard + status-announcement aria-live-polite for copy-feedback; 5 contracts incl. clean-clipboard-discipline canon-rule + scrollable-region-on-overflow WCAG-2.1.1; 8-system vocabularyDrift mit Shiki canonical-aria-hidden-by-default vs Prism client-side-no-aria-hidden divergence highlighted)
- New canon: `grid-pattern` (P6-139 — 39. component; APG escalation aus table für interactive-cells; 2 strukturelle variants static / editable; 9-slot anatomy incl. cell-editor optional + selection-overlay decorative; 6 mistakes incl. grid-no-roving-tabindex + grid-edit-mode-no-escape blocker per APG; 5 contracts gated by APG keyboard interaction + virtualization guidance; PERFORMANCE_REQUIRED entry mit cellFocusLatency 16ms one-frame budget; schließt eine pending forward-ref aus table.yaml)
- New canon: `tree-grid` (P6-140 — 40. component; APG escalation aus grid für hierarchical-rows; 2 strukturelle variants static / editable; 11-slot anatomy mit row-expander + row-indent + aria-level/aria-expanded per row; 6 mistakes incl. treegrid-no-aria-level + treegrid-no-aria-expanded blocker per APG; 5 contracts gated by APG keyboard interaction + aria-level-drives-visual-indent canon-rule; schließt letzte pending forward-ref im table↔grid-pattern↔tree-grid-Bogen)
- New canon: `menu` (P6-112 — 41. component; APG menu pattern standalone distinct from menu-button; 2 variants vertical/horizontal aria-orientation; 10-slot anatomy incl. menuitemcheckbox + menuitemradio + group + separator + submenu-trigger + nested submenu + shortcut mit aria-keyshortcuts; 6 mistakes incl. menuitem-no-role + menu-submenu-no-aria-haspopup blocker; 5 contracts gated by APG keyboard model + typeahead-for-long-menus; PERFORMANCE_REQUIRED entry mit typeaheadMatchLatency 16ms; letzter open data-gap-canon-item)
- Schema/registry: Shiki + Prism als 16.+17. LIBRARY_VERSIONS-Einträge (P6-113 dependency); AG Grid + Glide Data Grid als 18.+19. (P6-139 dependency); PrimeVue als 20. (P6-140 dependency, TreeTable als first-class Vue treegrid primitive)
- Schema/registry: TanStack Table als 15. LIBRARY_VERSIONS-Eintrag (P6-99 dependency; unblocks P6-114 Phase-2-audit-coverage); PERFORMANCE_REQUIRED registry +`'table'`; 6 vsRelated-bidirektional-paare gesamt (list-item↔table, card↔table; table↔pagination, tabs↔pagination, stepper↔pagination, breadcrumbs↔pagination)
- New backlog: P6-139 grid-pattern + P6-140 tree-grid als follow-ups (target von table.yaml pending-refs)

Done 2026-05-03 batch (chronological):
- New canon: `badge` (P6-100), `icon` (P6-110, +xs-size vocab extension), `avatar` (P6-101), `avatar-group` (P6-102), `breadcrumbs` (P6-108)
- Schema: `eventsRationale` field (P6-122), `formIntegration` structured-fields schema (P6-121a), severity vocabulary + ADR-029 (P6-118a), positive-default boolean polarity convention (P6-119), boolean polarity migration `nonLinear → linear` on stepper, `count → multiple` on skeleton (P6-131), categories-cleanup for sidebar (avatar/avatar-group/badge/icon/skeleton/text-input + breadcrumbs)
- Doc + skill: SKILL.md spec-to-canon recipe (P6-129), deferred-tool bulk-load preamble (P6-130), two-kind-vocabularies clarification (P6-132), frameworkMap selector-prefix preamble (P6-128), changelog editorial-trigger contract (P6-116), generalised canon-first first-paragraph
- Polish: `alternateNames` rollout (P6-124, toast/alert), Primer in LIBRARY_VERSIONS (P6-134), modal performance-block (P6-88: focusTrapAttachLatency + inertSubtreeSize + backdropOverdraw)

Earlier path (Phase 0 → Phase-2): P0-1..4 → P3-17/18/19/20 → P1-5..10 → P2-11..16 → P4-21..25 → P5-26..32 → P3-24 → P6-41/43/46/47/48 → P6-49 stages 1–5 → P6-51 (text-input) → P6-66/67/68/69/71/72/73/75/76/77/79/80/81/82/83 → P6-86 (vsRelated bidir backfill) → P6-87/89..98 (contracts + events + motion bundles) → P6-100/101/102/108/110 (canon expansion).

**Phase-1 + Phase-2 Bucket vollständig.** P6-Bucket (SEO + AI-Discovery): code-items P6-41 + P6-43 + P6-46 alle gelandet. Verbleibend: P4-26 (State-Graph-Renderer, deferred), P6-42 prose-intro (User-authored canon-Content, kein Code-Item), P6-44 Backlinks/Outreach (non-code, user-driven), P6-45 Lighthouse Lab-metrics (Node-arm64-Reinstall oder PSI-API-call gegen Production deferred).

## Wartung dieses Backlogs

Bei jeder erledigten Aufgabe Status auf `[x]` setzen, Erledigungs-Datum + Datei-Verweis ergänzen. Neue Items nach Priorität einsortieren. Periodisch die `[x]`-Einträge in `docs/backlog-archive.md` auslagern, sobald sie nicht mehr für die Empfohlener-Pfad-Erzählung benötigt werden — diese Datei bleibt schlank und schnell zu lesen.
