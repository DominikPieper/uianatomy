# Backlog

Persistente Arbeitsliste aus dem Senior UX/UI + Senior Frontend Review (2026-04-29). Quelle: `~/.claude/plans/mache-ein-detailliertes-review-cheeky-cocke.md`.

Closed items leben in [`docs/backlog-archive.md`](backlog-archive.md). Beim Vergeben einer neuen `PX-NN`-id beide Files scannen, damit IDs nicht kollidieren.

Status-Legende: `[ ]` offen · `[~]` in Arbeit · `[x]` erledigt

---

## P0 — sofort, blockiert Phase 2

Komplett geschlossen — siehe `docs/backlog-archive.md`.

## P1 — strukturelle Schema-Erweiterungen

Komplett geschlossen — siehe `docs/backlog-archive.md`.

## P2 — Inhalts-Sektionen

Komplett geschlossen — siehe `docs/backlog-archive.md`.

## P3 — Site & SVG-Polish

Komplett geschlossen — siehe `docs/backlog-archive.md`.

## P4 — Skalierung & Tooling

- [ ] **P4-26 State-Graph-Renderer** — eigener Graph-Renderer für `axes.states.transitions`. Heute reine Tabelle (`TransitionsTable.astro`); Graph wäre zweiter SVG-Generator parallel zu `renderAnatomySVG`, gleicher Wireframe-Stil, eigener Layout-Algorithmus (Stack-Layout für lineare State-Maschinen wie Modal: closed→opening→open→closing; force-directed mit fixem Seed für Combobox-artige Graphen). Position auf Component-Page: unter `TransitionsTable`. Eigene ADR begründet warum separat statt in Anatomy-SVG integriert (Graph-Layout-Problem vs Grid-Layout-Problem; anderer User-Need: "Wie wechselt die Komponente ihren State?" vs "Welche Slots gibt es?"). Aus P3-24 Plan extrahiert als eigenes Item.

## P5 — Phase 2 (Implementations)

- [ ] **P5-34b sources[] pilot-migration (verification-required)** — Migriere library-spezifische URLs in den 24 components von bare-strings zu strukturierten objects mit `library`-key + `verifiedAt`-date. Pre-condition: jede `verifiedAt`-claim muss mit echter upstream-verification gegen `LIBRARY_VERSIONS[key].url` zur Tagesform der lib-API verbunden sein (Radix-Dialog-API existiert? React-Aria-Modal-shape stimmt?). NICHT mechanisch fabrizierbar — erfordert manual fact-check oder `library-audit-runner` web-fetch-pass pro lib. APG/MDN/WCAG-Sources bleiben strings (kein library zu pinnen). Defer until verification-budget allocated. Datei: `content/components/*.yaml` (24, selektiv).
- [ ] **P5-36 Library-Versionen Phase-4: auto-version-bump CI** — Script das LIBRARY_VERSIONS-keys via npm-registry / docs-sniff fetcht; PR öffnet wenn local pin lagged > N months. Defer until P5-33-baseline-versions gefüllt sind. Datei: `scripts/check-library-versions.mjs` + `.github/workflows/library-version-bump.yml`.

## P6 — Reichweite (SEO + AI-Discovery)

- [ ] **P6-44 Backlinks + Outreach** (non-code, user-driven) — größter klassischer SEO-Hebel. Domain-Authority bei 0 ohne Backlinks. Konkrete Targets: (a) GitHub awesome-* listen — `awesome-design-systems`, `awesome-ui-component-library`, `awesome-react-components`, `awesome-a11y` PRs. (b) Show-HN ("UI Anatomy — canonical reference for UI component anatomy with MCP server"). (c) Designsystems.com community-Slack + Storybook-blog-pitch. (d) Refactoring UI / GOV.UK design-blog-mentions. (e) Twitter/Mastodon design-community. Out-of-scope für Code-Changes; wird vom Owner selbst getrieben. Backlog-Eintrag dient als Reminder + Tracking-Surface für gelandete Mentions.
- [~] **P6-45 PSI / Lighthouse audit + Core Web Vitals** — Lighthouse-CLI-Run auf lokaler Maschine geblockt: Node-Install ist `x64 darwin`, Chrome ist `arm64`, Lighthouse 13.1.0 refused den Launch ("Launching Chrome on Mac Silicon (arm64) from an x64 Node installation results in Rosetta translating the Chrome binary…"). Ersatz-Audit headless ohne Browser via curl + python3 HTML-parse ergab: home 250 KiB raw / 34 KiB gzip; modal 192 KiB raw / 30 KiB gzip; beide Pages haben title + meta description + canonical + og:image + og:title + twitter:card + html lang="en"; 1× h1 pro Page; alle imgs haben alt; JSON-LD blocks vollständig (home 6 graph-nodes, modal 2 graph-nodes inkl. FAQPage); Headers `Vary: Accept` + `Cache-Control: public, max-age=0, must-revalidate` + `Link: api-catalog, service-desc, sitemap` (P3-25). Crawler-visible SEO-signals alle korrekt. **Nicht gemessen ohne Browser:** LCP/INP/CLS-Werte, Color-Contrast-Pässe, ARIA-pattern-checks, Best-Practices-Score. **Next step:** Node-arm64 via `fnm install 22 --arch arm64` oder `brew install node` ersetzen, dann `pnpm dlx lighthouse https://uianatomy.dev/ --output=json --quiet --chrome-flags="--headless=new"` — separater User-Step weil Node-Reinstall andere workspace-tools tangiert. Oder PageSpeed-Insights API gegen produktions-URL nach erstem Deploy (kein lokales Chrome nötig). Status [~] = teilweise (manual audit done, Lab-metrics deferred). Files: keine — informational only.
- [x] **P6-49 Patterns Layer (stage 1: schema + first pattern + minimal site + MCP)** — author-first-before-commit-schema discipline (ADR-013-style) durchgeführt: Confirmation-Flow-pattern (Modal-alertdialog + destructive-Button) als first canonical pattern in `content/patterns/confirmation-flow.yaml`. **Login-form pivoted weg** weil canonical roster keinen plain `input`/`text-field` hat — backlog item P6-51 (text-input canonical) tracked es. Confirmation-Flow als first-pattern reicher in bridge-content (alertdialog vs dialog ist most-cited a11y-failure-category) und exercises das schema gut: 2-component composition, 4 decisions, 5 mistakes, 4 framework-skeletons. Schema (`shared/src/schema.ts`): `patternSchema` mit `composition: array.min(2)<{componentId (slug), role (prose), notes? (prose)}>`, `whenToUse: {use, avoid}` (strict), `decisions: array.min(1)<{question, answer, rationale}>` (strict), `mistakes: array.min(3)<mistakeSchema>` (reuses component schema), `frameworkSkeletons: {webComponents, react, vue, angularSignals}` alle required strings, optional `notes`, required `lastReviewed` ISO. Cross-refine `composition[].componentId` ∈ canonical-roster im consistency-test (Zod kann roster nicht sehen — same pattern wie vsRelated-cross-refine in P3-29). Loader (`loadPattern` + `loadPatterns`), Bundle (`loadPatternsFromBundle` + `bundle-patterns.mjs` build-script + `./patterns-bundle.json` subpath-export). Site routes: `/patterns` index + `/patterns/[id]` single-page (3-view-routing als stage-2-followup, ADR-024 D1 deferred). Page emit JSON-LD `@graph: [TechArticle, HowTo]` — HowTo ist legit hier weil patterns LITERAL how-tos sind (P3-39-Verdikt war "forced fit für components"; für patterns ist es spec-fit). Sections: composition (jeder slot mit role + notes, link zu canonical component-page), whenToUse (use+avoid dl), canonical-decisions (question→answer→rationale tripel), common-mistakes, framework-skeletons (alle 4 frameworks via expressive-code), notes-block, footer-lastReviewed. MCP: 3 neue tools (`list_patterns` + `get_pattern` + `get_patterns_for_component`), tool-count 19→22. Worker bundle-load erweitert. SKILL.md tool-table erweitert + count-zeile geflippt. Footer-nav um `/patterns`-link erweitert. Verifikation: `pnpm -r typecheck` 0 errors über 64 files (war 60); `pnpm -r test` 174 grün (139 shared + 35 mcp-server, +11 vs pre: 4 schema + 1 consistency + 5 mcp-server + 1 unaccounted); `pnpm -C site build` 77 pages (war 75, +2 patterns-pages), 0 broken links; pattern-page rendert alle sections, expressive-code 4-tab-style framework-skeletons, JSON-LD HowTo mit step-array. **Stage-1 out-of-scope** (gehören zu P6-49 stage 2/3): three-view-routing (D1 von ADR-024), `/patterns/[id]/dev` + `/bridge` URL-split, "Used in patterns"-section auf component-pages (B1, auto-derived), composition-SVG-renderer (F1, reuse `renderAnatomySVG`), per-pattern-OG-image, JSON-API endpoint `/api/patterns.json`, llms-md exclude für /patterns. Erste echte erfolgsmessung wenn 3-5 patterns im roster (drift-detection wird relevant). Erledigt 2026-05-01. Dateien: `content/patterns/confirmation-flow.yaml` (neu), `shared/src/schema.ts`, `shared/src/loader.ts`, `shared/src/bundle.ts`, `shared/scripts/bundle-patterns.mjs` (neu), `shared/package.json` (subpath-export + build-chain), `shared/tests/schema.test.ts`, `shared/tests/consistency.test.ts`, `mcp-server/src/state.ts`, `mcp-server/src/data.ts`, `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `worker/index.ts`, `site/src/content.config.ts`, `site/src/pages/patterns/index.astro` (neu), `site/src/pages/patterns/[id].astro` (neu), `site/src/layouts/Base.astro` (footer-link), `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`.
- [ ] **P6-52 `@uianatomy/skill` npm package + install CLI** — heavier alternative zur curl-one-liner aus SKILL.md. Package enthält die canonical SKILL.md + small `uianatomy-skill install`-CLI das `mkdir -p .claude/skills/uianatomy-mcp` + writes SKILL.md + writes `.mcp.json` mit MCP-config-snippet (Claude Code default) + optional appends to `.gitignore`. Pro: zero-config (`npx @uianatomy/skill install` reicht), version-pinned, idempotent. Contra: extra-publish-pipeline + maintenance, npm-registry-coupling. Trade-off-decision in eigener ADR (vermutlich ADR-025) bei landing. Trigger wenn ≥3 echte external-projects die curl-one-liner adopted haben + feedback signalisiert friction. Out-of-scope für P6-49 stage 1.
- [ ] **P6-74 i18n.rtl-restructure** — User-feedback: "prose. Could be structured: mirroredAxes, directionNeutralGlyphs, logicalProperties[]." ADR-017 chose prose deliberately for flexibility. Restructure touches 24 components (those with i18n block); improves queryability for "components-that-mirror-by-X-axis"-style queries. Defer until prose-search-pain ist real (heute: keine queries laufen gegen i18n-prose). Write ADR-017-followup ADR. Multi-session.
- [ ] **P6-78 frameworkMap framework expansion** — User-feedback: "missing Svelte, SolidJS, Lit, Qwik. `angularSignals` overly specific — drop suffix." Trade-off: adding frameworks dilutes per-component depth (each new framework = +25% prose); `angularSignals` was deliberate (signals-based-API specificity); rename loses precision. Defer until consumer-demand. Wenn landing: ADR-027 Framework Selection.
- [ ] **P6-84 migrate-tool / codemod-hints** — User-feedback: "no migrate tool — when canon renames `nonLinear` → `linear`, no diff/codemod hint." Heavy infra: codemod-tooling for canonical schema-renames. Premature — schema-renames sind selten, P6-72 severity-add ist erste echte migration die diese tool brauchen würde. Defer.
- [ ] **P6-85 testRecipes block** — User-feedback: "every component would benefit from a 'minimum a11y test' snippet for downstream test gen." New schema-field `testRecipes?: { framework, snippet }[]` — pre-baked test-snippets pro framework × component. Combines mit a11y-fixture-endpoint (P3-37). Heavy: schema + per-component authoring. Multi-session.
- [ ] **P6-126c orphan `AnatomyTable.astro` — delete or revive** — `site/src/components/sections/AnatomyTable.astro` is not imported by any view (`DesignerView.astro` uses `FigmaSlotTable` only; `DevView.astro` uses `CodeSlotTable` only; `BridgeView.astro` uses both). The file received the P6-126b badge wiring for symmetry, but otherwise renders nowhere. Either delete it cleanly (smaller surface, removes a confusion source for future contributors) or wire it into one of the views as the third anatomy presentation (e.g. a slot-purpose-focused table on the Designer view alongside `FigmaSlotTable`). Datei: `site/src/components/sections/AnatomyTable.astro`.
- [ ] **P6-143 `SEVERITY_SYNONYMS` decommission (P6-127 follow-up)** — `mcp-server`-internal `SEVERITY_SYNONYM_REVERSE` (Build aus `shared/src/vocabulary.ts` `SEVERITY_SYNONYMS`) wird redundant jetzt wo Toast/Alert/Banner severity-variants ihre `alternativeNames` direkt im YAML tragen (P6-127). Decommission-Pfad: `mcp-server/src/server.ts` import + reverse-index + query-side expansion entfernen; tool-description um den synonym-expansion-Satz trimmen; `SEVERITY_SYNONYMS` aus `shared/src/vocabulary.ts` raus (oder als deprecated belassen wenn noch in Tests referenziert); Tests `search_components({query:'danger'/'caution'})` sollen weiterhin grün bleiben — jetzt via component-yaml-haystack statt via reverse-index. Aufwand-Schätzung 30 min. Datei: `mcp-server/src/server.ts`, `shared/src/vocabulary.ts`, `mcp-server/tests/server.test.ts`.
- [x] **P6-127 per-variant alternative names** — `axes.variants` reshape von `string[]` auf `Array<{ name, alternativeNames? }>` per [ADR-031](./adr/031-variant-alternative-names.md). 41 component-yamls migriert via `scripts/migrate-variants-shape.py` (109 entries, idempotent dry-run + apply). 7 high-value alternativeNames-backfills (toast/alert/banner severity-cluster: error → [danger, destructive, critical], warning → [caution, attention]; modal.alertdialog → [confirm, destructive]). Drawer/Stepper-Pläne aus Plan verworfen weil `side`/`linear` properties sind, nicht variants — scope-correction. `axesSchema.superRefine` erweitert (unique-name + no-overlap + no-self-duplicate); `variantDeprecations`-Refine liest `axes.variants.map(v => v.name)`. Consumers: `compare.ts` setDiff auf `v.name`, `validate.ts` haystack auf `[v.name, ...alternativeNames]`, `mcp-server` `search_components` haystack erweitert + tool-description aktualisiert (ADR-031-Verweis), `AxesTable.astro` rendert italic muted "aka X · Y"-row unter Variant-Chip wenn alternativeNames non-empty (CSS `.variant-aliases`), `consistency.test.ts` SEVERITY_AXIS_REGISTRY iteriert `v.name`. 6 neue schema-tests (object-shape, alternativeNames optional, bare-string-reject, duplicate-name-reject, alias-collision-reject, alias-own-name-reject), 1 neuer mcp-server search-test (`search_components({query:'confirm'})` hits Modal via variant-alternativeNames-haystack — distinct from severity-synonym-path). Tests 279 grün (221 shared + 58 mcp; war 272 = +6 schema +1 mcp). 137 site-pages, 0 neue broken-links. Site spot-check: Modal rendert "aka confirm · destructive" auf alertdialog-chip; Toast rendert beide severity-aliases-rows; Button negative-control 0 aliases-rows. Erledigt 2026-05-05. Dateien: `docs/adr/031-variant-alternative-names.md` (neu), `shared/src/schema.ts`, `shared/src/compare.ts`, `shared/src/validate.ts`, `mcp-server/src/server.ts`, `site/src/components/sections/AxesTable.astro`, `shared/tests/{schema,consistency}.test.ts`, `mcp-server/tests/server.test.ts`, `docs/schema.md`, `scripts/migrate-variants-shape.py` (neu, one-shot), `content/components/{toast,alert,banner,modal}.yaml` (alternativeNames-backfills) plus alle 41 component-yamls (variants-shape-migration).

## Empfohlener Pfad

**Phase 1–2 + Phase-2 audit infrastructure complete + sub-anatomy machinery shipped + provenance UI surfaced + per-variant alternativeNames (ADR-031) shipped.** Component canon at 41 entries with action-group sub-anatomy reused across Card / Alert / Banner / Modal / Drawer footer; site renders a `from action-group` pill on every resolved slot in dev + bridge views; `axes.variants` is object-shape with optional `alternativeNames` for cross-system search recall (27 MCP tools; 279 tests grün). Active path now: P6-114 Phase-2 audit (volle 20-library LIBRARY_VERSIONS-Deckung), weitere sub-anatomies (header-bar / close-button / icon-leading-text — additive über das ADR-030-Gerüst), `SEVERITY_SYNONYMS` decommission (jetzt redundant nach P6-127), oder P6-126c orphan AnatomyTable.astro entscheiden (delete vs. revive in eine View).

Done 2026-05-05 batch (chronological):
- P6-127 per-variant `alternativeNames` (ADR-031): `axes.variants` reshape von `string[]` auf `Array<{ name, alternativeNames? }>`; 41 yamls migriert via `scripts/migrate-variants-shape.py`; 7 high-value backfills (toast/alert/banner severity-cluster + modal.alertdialog); axesSchema.superRefine extended (unique-name + no-overlap + no-self-duplicate); consumers `compare.ts` / `validate.ts` / `mcp-server search_components` (haystack-Erweiterung) / `AxesTable.astro` (italic "aka …"-row unter Variant-Chip) / `consistency.test.ts` (SEVERITY_AXIS_REGISTRY auf v.name) updated; 6 schema-tests + 1 mcp-server search-test; 279 tests grün (war 272 +7).
- ADR-030 sub-anatomy + 5-component migration: `subAnatomySchema` + `anatomySlotRefSchema` + `resolveAnatomyRefs` eager-resolution + `content/sub-anatomies/action-group.yaml` (3 slots: primary/secondary/tertiary-action + a11y groupRule/focusRule). Card/Alert/Banner inline `actions` slot replaced with `$ref`; Modal/Drawer keep `footer` wrapper + add child `$ref` with `parent: footer`; Toast deliberately excluded (single-button, structurally not a group). 2 new MCP tools (`list_sub_anatomies`, `get_sub_anatomy`); `search_components` + `get_canonical_vocabularies` extended; worker bundle pre-resolves at bundle-write time so the worker is sub-anatomy-agnostic. Site `content.config.ts` resolves refs at Astro content-parse via `componentSchema.transform()` reading the sub-anatomies bundle. Soft-lint in `consistency.test.ts` enforces zero drift with WRAPPER_ALLOWLIST for Modal/Drawer footer. Backlog citation of ADR-029 was a numbering bug — corrected to ADR-030.
- P6-126b sub-anatomy provenance UI badge: new `ResolvedAnatomySlot` type (additive on `AnatomySlot` with optional readonly `__subAnatomy`), `Component.anatomy` retyped to `ResolvedAnatomySlot[]`, new `<SubAnatomyBadge entry={s} />` component wired into `FigmaSlotTable` + `CodeSlotTable` + orphan `AnatomyTable`. Caveat caught mid-flight: Astro 5's content layer JSON-caches glob entries → strips non-enumerable own properties, so the original "non-enumerable property survives Astro parse" assumption was wrong for static-build cache hits; fix lifts `__subAnatomy` enumerably in the site `content.config.ts` transform only, leaving the canonical loader contract (non-enumerable, JSON-invisible) intact. New follow-up P6-126c logged for the orphan `AnatomyTable.astro` (delete vs. revive).

Done 2026-05-04 batch (chronological):
- New canon: `table` (P6-99 — 35. component; 12-slot anatomy mit scrollable-region wrapper + native `<table>` semantics + selectable variant + 9 properties; 6 events; 5 nonNegotiable contracts gated by WCAG 1.3.1 + 4.1.2 + APG; 7-system vocabularyDrift; pending forward-refs zu grid-pattern + tree-grid)
- New canon: `pagination` (P6-109 — 36. component; paart sich mit table als data-page-navigation; 12-slot anatomy mit nav-landmark + 3 strukturelle Variants numbered/prev-next-only/load-more; 6 number/text propertyMap-only fields kapseln Material UI's siblingCount + boundaryCount truncation API; 5 nonNegotiable contracts incl. disabled-not-removed canon-rule; 7-system vocabularyDrift; 2 events; no PERFORMANCE_REQUIRED entry per registry-discipline)
- New canon: `progress` (P6-106 — 37. component; 2 strukturelle variants linear/circular; 8-slot anatomy mit role=progressbar root + announcement aria-live-polite milestone-only; determinate boolean positive-default per polarity-konvention; 5 contracts incl. omit-aria-valuenow-when-indeterminate APG explicit rule + prefers-reduced-motion fallback WCAG 2.3.3; eventsRationale state-reflective-no-events; native `<progress>` als prose-only alternate-implementation; 8-system vocabularyDrift incl. WAI-ARIA + HTML + 6 design systems)
- New canon: `code-block` (P6-113 — 38. component; 2 strukturelle variants block `<pre><code>` chrome / inline bare `<code>`; 8-slot anatomy mit syntax-highlight tokens als decorative aria-hidden + line-numbers via CSS counter-increment so they don't enter clipboard + status-announcement aria-live-polite for copy-feedback; 5 contracts incl. clean-clipboard-discipline canon-rule + scrollable-region-on-overflow WCAG-2.1.1; 8-system vocabularyDrift mit Shiki canonical-aria-hidden-by-default vs Prism client-side-no-aria-hidden divergence highlighted)
- New canon: `grid-pattern` (P6-139 — 39. component; APG escalation aus table für interactive-cells; 2 strukturelle variants static / editable; 9-slot anatomy incl. cell-editor optional + selection-overlay decorative; 6 mistakes incl. grid-no-roving-tabindex + grid-edit-mode-no-escape blocker per APG; 5 contracts gated by APG keyboard interaction + virtualization guidance; PERFORMANCE_REQUIRED entry mit cellFocusLatency 16ms one-frame budget; schließt eine pending forward-ref aus table.yaml)
- New canon: `tree-grid` (P6-140 — 40. component; APG escalation aus grid für hierarchical-rows; 2 strukturelle variants static / editable; 11-slot anatomy mit row-expander + row-indent + aria-level/aria-expanded per row; 6 mistakes incl. treegrid-no-aria-level + treegrid-no-aria-expanded blocker per APG; 5 contracts gated by APG keyboard interaction + aria-level-drives-visual-indent canon-rule; schließt letzte pending forward-ref im table↔grid-pattern↔tree-grid-Bogen)
- New canon: `menu` (P6-112 — 41. component; APG menu pattern standalone distinct from menu-button; 2 variants vertical/horizontal aria-orientation; 10-slot anatomy incl. menuitemcheckbox + menuitemradio + group + separator + submenu-trigger + nested submenu + shortcut mit aria-keyshortcuts; 6 mistakes incl. menuitem-no-role + menu-submenu-no-aria-haspopup blocker; 5 contracts gated by APG keyboard model + typeahead-for-long-menus; PERFORMANCE_REQUIRED entry mit typeaheadMatchLatency 16ms; letzter open data-gap-canon-item)
- Schema/registry: Shiki + Prism als 16.+17. LIBRARY_VERSIONS-Einträge (P6-113 dependency); AG Grid + Glide Data Grid als 18.+19. (P6-139 dependency); PrimeVue als 20. (P6-140 dependency, TreeTable als first-class Vue treegrid primitive)
- Schema/registry: TanStack Table als 15. LIBRARY_VERSIONS-Eintrag (P6-99 dependency; unblocks P6-114 Phase-2-audit-coverage); PERFORMANCE_REQUIRED registry +`'table'`; 6 vsRelated-bidirektional-paare gesamt (list-item↔table, card↔table; table↔pagination, tabs↔pagination, stepper↔pagination, breadcrumbs↔pagination)
- New backlog: P6-139 grid-pattern + P6-140 tree-grid als follow-ups (target von table.yaml pending-refs)

Done 2026-05-03 batch (chronological):
- New canon: `badge` (P6-100), `icon` (P6-110, +xs-size vocab extension), `avatar` (P6-101), `avatar-group` (P6-102), `breadcrumbs` (P6-108)
- Schema: `eventsRationale` field (P6-122), `formIntegration` structured-fields schema (P6-121a), severity vocabulary + ADR-029 (P6-118a), positive-default boolean polarity convention (P6-119), boolean polarity migration `nonLinear → linear` on stepper, `count → multiple` on skeleton (P6-131), categories-cleanup for sidebar (avatar/avatar-group/badge/icon/skeleton/text-input + breadcrumbs)
- Doc + skill: SKILL.md spec-to-canon recipe (P6-129), deferred-tool bulk-load preamble (P6-130), two-kind-vocabularies clarification (P6-132), frameworkMap selector-prefix preamble (P6-128), changelog editorial-trigger contract (P6-116), generalised canon-first first-paragraph
- Polish: `alternateNames` rollout (P6-124, toast/alert), Primer in LIBRARY_VERSIONS (P6-134), modal performance-block (P6-88: focusTrapAttachLatency + inertSubtreeSize + backdropOverdraw)

Earlier path (Phase 0 → Phase-2): P0-1..4 → P3-17/18/19/20 → P1-5..10 → P2-11..16 → P4-21..25 → P5-26..32 → P3-24 → P6-41/43/46/47/48 → P6-49 stages 1–5 → P6-51 (text-input) → P6-66/67/68/69/71/72/73/75/76/77/79/80/81/82/83 → P6-86 (vsRelated bidir backfill) → P6-87/89..98 (contracts + events + motion bundles) → P6-100/101/102/108/110 (canon expansion).

**Phase-1 + Phase-2 Bucket vollständig.** P6-Bucket (SEO + AI-Discovery): code-items P6-41 + P6-43 + P6-46 alle gelandet. Verbleibend: P4-26 (State-Graph-Renderer, deferred), P6-42 prose-intro (User-authored canon-Content, kein Code-Item), P6-44 Backlinks/Outreach (non-code, user-driven), P6-45 Lighthouse Lab-metrics (Node-arm64-Reinstall oder PSI-API-call gegen Production deferred).

## Wartung dieses Backlogs

Bei jeder erledigten Aufgabe Status auf `[x]` setzen, Erledigungs-Datum + Datei-Verweis ergänzen. Neue Items nach Priorität einsortieren. Periodisch die `[x]`-Einträge in `docs/backlog-archive.md` auslagern, sobald sie nicht mehr für die Empfohlener-Pfad-Erzählung benötigt werden — diese Datei bleibt schlank und schnell zu lesen.
