# Backlog

Persistente Arbeitsliste aus dem Senior UX/UI + Senior Frontend Review (2026-04-29). Quelle: `~/.claude/plans/mache-ein-detailliertes-review-cheeky-cocke.md`.

Status-Legende: `[ ]` offen · `[~]` in Arbeit · `[x]` erledigt

---

## P0 — sofort, blockiert Phase 2

- [x] **P0-1 Tabs nachziehen** — data states erweitert (`busy`, `lazy`, `error`), 2 mistakes ergänzt (lazy-panel-no-aria-busy, overflow-no-scroll-into-view, indicator-not-rtl-aware), 1 mismatch ergänzt (overflow wrap vs scroll). Erledigt 2026-04-29. Datei: `content/components/tabs.yaml`.
- [x] **P0-2 Mindesttiefe-Checkliste** verankert in `docs/methodology.md` (Section "Minimum depth contract", Tabelle mit 8 Schwellwerten). Validator: `shared/tests/depth.test.ts` — bricht `pnpm -r test` bei Unterschreitung mit `<id>: <dimension> N < M`-Meldungen. Per-Component-Override via `overrides`-Map möglich. Erledigt 2026-04-29.
- [x] **P0-3 ADR-006: Token-Layer** — Decision-Doc geschrieben: kanonische Token-Namen pro Slot, Werte in `implementations/<lib>/`. Fünf Kategorien (`spacing`, `radius`, `color`, `elevation`, `typography`). Schema-Skizze + `tokenBindings`-Form fixiert für P1-5. Erledigt 2026-04-29. Datei: `docs/adr/006-token-layer.md`.
- [x] **P0-4 Roadmap-Pin** — `<aside class="phase-banner">` auf Site-Index oberhalb Hero: "Phase 1 · Reference only — no live examples and no implementation code yet. Phase 2 (implementation audits) begins Q3 2026." Light/Dark-tauglich, Mobile-Stack ab 540px. Erledigt 2026-04-29. Dateien: `site/src/pages/index.astro`, `site/src/styles/global.css`.
- [x] **P0-5 Anatomy-SVG Label- und Floating-Overlaps fixen** — alle drei verifizierten Layout-Bugs behoben. (a) **Tabs indicator vs tabpanel**: `layoutGrid` reserviert jetzt Floating-Extension pro Anker-Slot (`fHeight + offset`); next root row droppt um den Overflow-Betrag. tabpanel sitzt jetzt bei y=136 (vorher 112), indicator endet bei y=128, 8px Lücke. (b/c) **Parent-Slot-Labels (Modal container/header, Combobox listbox)**: neuer `PARENT_LABEL_STRIP=24` reserviert Top-Strip in Nested-Area; `emitSlotGroup` bekommt `hasChildren`-Flag und rendert Labels für Parent-Slots top-left mit `dominant-baseline=hanging` statt zentriert. Modal container label jetzt y=30 (war 144), header label y=66 (war 72). Combobox listbox label y=106 (war 242), liegt über den Option-Children (y=128). 4 neue invariant-Tests prüfen: Tabs indicator-bottom ≤ tabpanel-top; Modal container/header label-y < first-child-rect-y; Combobox listbox label-y < first-option-y. Erledigt 2026-04-29. Dateien: `shared/src/svg.ts`, `shared/tests/schema.test.ts`.

## P1 — strukturelle Schema-Erweiterungen

- [x] **P1-5 Token-Layer Schema + Migration** — `anatomySlotSchema` um optionales `tokens`-Feld (5 Kategorien als optionale `Record<string, dotted-token-name>`-Maps) erweitert, Vokabular fixiert + in `docs/schema.md` dokumentiert, alle 5 YAMLs (33 Slots) migriert, neue Designer-View-Komponente `TokensTable.astro` mit sparse-column-Logik integriert. Tests + `pnpm -C site build` grün, Browser-Smoke-Test bestätigt Designer-only-Sichtbarkeit + warmer Akzent. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/TokensTable.astro`, `site/src/components/views/DesignerView.astro`, `docs/schema.md`.
- [x] **P1-6 Motion-Sektion** — `motion?` als Top-Level-Feld auf `componentSchema` (per-Component, nicht per-Slot), drei Pflicht-Keys (`durations` non-empty Record, `easing` als single dotted token, `reducedMotionFallback` Enum `instant | reduced | preserved`). Vokabular `motion.duration.{instant, fast, base, slow, slower}` + `motion.easing.{standard, decelerate, accelerate, sharp}` in `docs/schema.md`. Migration Modal/Combobox/Tabs durchgeführt; Card/Button bewusst ohne motion. Designer-View-Section `MotionTable.astro` zwischen Tokens und Axes (Bridge/Dev hidden via `data-view-section="designer"`-Pattern aus P3-20). 6 neue vitest-Cases (positiv: modal/tabs parse; negativ: raw `200ms`, leere durations, unbekannter Fallback; baseline: card/button ohne motion). Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `content/components/{modal,combobox,tabs}.yaml`, `site/src/components/sections/MotionTable.astro`, `site/src/components/views/DesignerView.astro`, `docs/adr/007-motion-section.md`, `docs/schema.md`, `shared/tests/schema.test.ts`.
- [x] **P1-7 Responsive-Sektion** — `responsive?` als Top-Level-Feld auf `componentSchema` (per-Component, parallel zu `motion`). `breakpoints` ist non-empty Array von `{ at: dotted-token, change: prose }`-Records. Vokabular `breakpoint.{xs, sm, md, lg, xl}` in `docs/schema.md`. Direction (oberhalb/unterhalb/zwischen) wird in der `change`-Prosa kodiert, nicht im Schema. Migration Card (orientation-Collapse bei sm), Modal (fullscreen below sm), Tabs (vertical forbidden below lg), Combobox (Plattform-Picker mobile) durchgeführt; Button bewusst ohne responsive. Designer-View-Section `ResponsiveTable.astro` zwischen Motion und Axes (Bridge/Dev hidden via `data-view-section="designer"`-Pattern). 6 neue vitest-Cases (modal/tabs parsen; button omittet; raw `640px`/leeres Array/leere change-Prosa abgewiesen). ADR-008 dokumentiert per-Component, Free-Text-`change`, Direction-in-Prose, dotted-Token-`at`. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `content/components/{card,modal,tabs,combobox}.yaml`, `site/src/components/sections/ResponsiveTable.astro`, `site/src/components/views/DesignerView.astro`, `docs/adr/008-responsive-section.md`, `docs/schema.md`, `shared/tests/schema.test.ts`.
- [x] **P1-8 State-Maschine modellieren** — `axes.states.transitions?: [{ from, to, trigger }]` als optionales nested-Feld unter `axes.states` (nicht top-level — States-Vokabular und State-Graph gehören zusammen). Erste cross-field refine im Schema: `from`/`to` müssen in `axes.states.interactive ∪ data` deklariert sein, sonst Zod-Fehler mit Pfad `axes.states.transitions[i].from|to`. Migration Modal (4 Transitions: closed→opening→open→closing→closed), Combobox (6 Transitions: closed↔open, open↔busy, open→invalid, invalid→closed) durchgeführt; Card/Tabs/Button bewusst ohne (triviale Graphen). `TransitionsTable.astro` rendert in allen drei Views (`role="both"`) direkt nach AxesTable — Bridge-View profitiert besonders, weil der "Designer denkt closed→open in einem Klick, Developer weiß opening dazwischen" mismatch genau dort sichtbar wird. 6 neue vitest-Cases (modal/combobox parsen + edge-Set-Assertion; card/tabs/button ohne; refine-Fehler bei unbekanntem from/to; leerer trigger). ADR-009 dokumentiert nested-vs-top-level, free-text trigger, cross-field refine, ADR-007-Verweis-Auflösung. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `content/components/{modal,combobox}.yaml`, `site/src/components/sections/TransitionsTable.astro`, `site/src/components/views/{Designer,Dev,Bridge}View.astro`, `docs/adr/009-state-transitions.md`, `docs/schema.md`, `shared/tests/schema.test.ts`.
- [x] **P1-9 Property-Type strukturiert** — `property.type: string` → `z.discriminatedUnion('kind', [primitive, enum])` mit `kind: primitive, of: boolean` und `kind: enum, values: [...]` (min 2, eindeutig, non-empty). Alle 17 Properties über die fünf YAMLs migriert (6 primitive, 11 enum). Render-Helper `formatPropertyType` in `shared/src/format.ts` hält die `' | '`-Anzeige unverändert; `AxesTable.astro` rendert in allen drei Views identisch zur Pre-Migration. 7 neue vitest-Cases (positive/negative/baseline + duplicate-refine + unknown-kind). ADR-010 begründet zwei-Arme-statt-drei, `kind`-Discrimination, `.strict()`-Pattern, fehlenden Pipe-Shortcut. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/src/format.ts`, `shared/src/index.ts`, `shared/package.json`, `shared/tests/schema.test.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/AxesTable.astro`, `docs/schema.md`, `docs/adr/010-property-type-union.md`.
- [x] **P1-10 Events Top-Level-Feld** — `events?: Array<{ name (camelCase), payload (prose), frameworkNotes: { webComponents, react, angularSignals, vue } }>`, optional pro Komponente, `min(1)` wenn vorhanden, `frameworkNotes` als `.strict()`-Record mit allen vier Frameworks required. Migration Modal (2 Events: openChange, dismiss), Tabs (2 Events: selectedChange, tabActivate), Combobox (3 Events: inputChange, selectionChange, openChange); Card und Button bewusst ohne. Neue `EventsTable.astro` rendert in Dev- und Bridge-View nach `FrameworkMapTable`; Designer-View bewusst ohne (events sind dev-Konzept). 7 neue vitest-Cases (positive Modal/Tabs/Combobox, baseline Card/Button, leerer Array, Nicht-camelCase-Name, fehlender frameworkNotes-Key). ADR-011 begründet per-Komponente, strukturierte frameworkNotes statt Free-Text, prose-payload, camelCase-name, required-all-four, Dev+Bridge-Render. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{modal,tabs,combobox}.yaml`, `site/src/components/sections/EventsTable.astro`, `site/src/components/views/{Dev,Bridge}View.astro`, `docs/schema.md`, `docs/adr/011-events-section.md`.

## P2 — Inhalts-Sektionen

- [x] **P2-11 Component-Property-Mapping-Tabelle** — `propertyMapEntrySchema` mit `figma` (string), `code` (string), `type` (closed enum `Boolean | Variant | Text | Instance Swap`), `notes?` (optional prose). Top-level `componentSchema.propertyMap?: array.min(1)`. Per-Entry `.strict()`. `code`-Feld bewusst loose — referenziert nicht via Refine zu `axes.properties[].name`, weil Entries auch Slot-IDs (`iconLeading`), default-Slot-children (`children`) und DOM-Attribute (`data-state`) erfassen. Migration aller fünf Komponenten: Button (8 Entries), Card (14), Modal (10), Tabs (6), Combobox (9). Neue `PropertyMapTable.astro` (4-Spalten Figma | Type-Badge | Code | Notes) rendert in Designer-View (zwischen TokensTable und MotionTable) und Bridge-View (nach AxesTable); Dev-View bewusst aus (code-side props sind in axes.properties + frameworkMap). CSS-Type-Badges via `color-mix()` Light/Dark-Parity (Boolean=neutral, Variant=cool, Text=warm, Instance Swap=green). 7 neue vitest-Cases. ADR-015 begründet Figma-Vokabular-statt-code-side-type, loose-coupling-zu-axes.properties, per-Komponente, optional-notes, Designer+Bridge-Render, closed-enum-type. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/PropertyMapTable.astro`, `site/src/components/views/{Designer,Bridge}View.astro`, `site/src/styles/global.css`, `docs/schema.md`, `docs/adr/015-property-map.md`.
- [x] **P2-12 "When to use"-Sektion** — `whenToUseSchema` mit `use` (required prose), `avoid` (required prose), `vsRelated?: [{ id (slug), difference (prose) }]` (optional, `min(1)` wenn vorhanden); alle drei Felder `.strict()`. Bare `related[]`-Feld aus `componentSchema` und allen 5 YAMLs entfernt — `whenToUse.vsRelated[].id` ist jetzt der einzige Identifier-Träger. Neue `WhenToUseSection.astro` rendert direkt nach Hero (vor Anatomy-Diagram), view-agnostisch in `[id].astro`. Migration aller 5 Komponenten mit `use`+`avoid`+`vsRelated` (Button: 3, Card: 2, Modal: 3, Tabs: 3, Combobox: 3 vsRelated-Einträge). 7 neue vitest-Cases (positive Card/Combobox, negative empty-use/empty-avoid/non-slug-id/empty-vsRelated, baseline whenToUse-ohne-vsRelated). ADR-012 begründet replace-statt-coexist (ADR-001 single-source), per-Komponente-statt-global, required-use-und-avoid, optional-aber-non-empty-vsRelated, Free-Text-difference, view-agnostischen Render. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/WhenToUseSection.astro`, `site/src/pages/components/[id].astro`, `docs/schema.md`, `docs/adr/012-when-to-use-section.md`.
- [x] **P2-13 Form-Integration-Sektion** — `formIntegrationSchema` mit vier optionalen Prose-Feldern (`name`, `formData`, `reset`, `validation`), `.strict()` plus refine "mindestens-eines-required". Migration Button (form control, alle vier Felder), Combobox (form control mit inner-input, alle vier Felder), Modal (form container, alle vier Felder dokumentieren Container-Sicht statt Control-Sicht); Card/Tabs bewusst aus (keine canonical Form-Participation). Neue `FormIntegrationSection.astro` (definition-list mit term-and-description-Layout) rendert in Dev- und Bridge-View nach Events vor Accessibility; Designer-View bewusst aus (HTML-form-prose ist dev-side technical content). 7 neue vitest-Cases. ADR-016 begründet vier-Sub-Fields-statt-prose, alle-optional-mit-refine, prose-statt-strukturiert, per-Komponente, Dev+Bridge-Render, vier-Universal-Core-Concerns. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{button,combobox,modal}.yaml`, `site/src/components/sections/FormIntegrationSection.astro`, `site/src/components/views/{Dev,Bridge}View.astro`, `docs/schema.md`, `docs/adr/016-form-integration.md`.
- [x] **P2-14 i18n-Sektion** — `i18nSchema` mit nested `rtl: { mirroring }` plus top-level `textExpansion`, beide Felder required prose. `i18nRtlSchema` als nested-namespace für künftige Facets (numerals, dates, dir-attribute, bidi). Migration aller fünf Komponenten — jede deklariert beide Facets mit komponenten-spezifischer Prosa (Button: icon-swap + 30-50% expansion; Card: orientation-flip + line-clamps; Modal: close-button-position + footer-wrap; Tabs: arrow-key-reversal + density risk; Combobox: clear-button-position + listbox max-width). Neue `I18nSection.astro` (zwei prose blocks) rendert in Designer-View (nach Responsive vor Axes) und Bridge-View (nach FormIntegration vor Accessibility); Dev-View bewusst aus. 6 neue vitest-Cases. ADR-017 begründet nested-rtl-namespace, beide-required, prose-statt-strukturiert, per-Komponente, Designer+Bridge-Render. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/I18nSection.astro`, `site/src/components/views/{Designer,Bridge}View.astro`, `docs/schema.md`, `docs/adr/017-i18n-section.md`.
- [x] **P2-15 A11y-Acceptance-Set pro Komponente** — `a11yAcceptanceSchema` mit drei optionalen Sub-Arrays: `keyboardWalk` (`{ keys, expected }`), `announcements` (`{ trigger, expected }`), `axeRules` (kebab-case ids). Top-level `.strict()` plus refine "mindestens eines der drei muss vorhanden sein". `axeRuleId`-Regex enforced kebab-case Shape; Membership gegen axe-core nicht validiert (CI catches via `axe.run()`-Rejection). Migration aller fünf Komponenten — alle haben mindestens `axeRules`, die meisten auch `keyboardWalk` + `announcements`. Neue `A11yAcceptanceTable.astro` rendert in Dev- und Bridge-View nach existing A11yTable; Designer-View unverändert (per-slot `a11y.hint` deckt das ab). 7 neue vitest-Cases (positive Modal/Combobox/all-five, negative non-kebab-axe-id/empty-object/empty-keyboardWalk-expected/empty-axeRules). ADR-014 begründet drei-Sub-Arrays statt-flach, alle-optional-aber-mindestens-eines, prose-keys-und-trigger statt structured, kebab-axe-ids ohne Membership-Validation, Dev+Bridge-Render. Discovery: YAML-Colons innerhalb Backtick-Prose (`` `dismissible: true` ``) brauchen Quote — als Schema-Doc-Footnote dokumentiert. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{button,card,modal,tabs,combobox}.yaml`, `site/src/components/sections/A11yAcceptanceTable.astro`, `site/src/components/views/{Dev,Bridge}View.astro`, `docs/schema.md`, `docs/adr/014-a11y-acceptance.md`.
- [x] **P2-16 Performance-Schwellwerte first-class** — `performanceSchema` als top-level optionale `array.min(1)` von `performanceThresholdSchema` (`{ name (camelCase), metric (prose), threshold (positive number), unit (prose), rationale (prose) }`, jede Entry `.strict()`). Backlog-Spec "in `axes.properties` ziehen" verworfen — `axes.properties` sind Consumer-set values, Thresholds sind Consumer-read advice; getrennte Concerns. Migration Combobox (virtualisedListbox 200 items + asyncFilterDebounce 150ms), Tabs (tablistOverflow 7 tabs + lazyPanelRender 100kb), Modal (stackDepth 1 modal); Card und Button bewusst aus. Neue `PerformanceSection.astro` (Liste mit name + metric/threshold/unit-Header + rationale-prose) rendert in Dev-View (nach FormIntegration) und Bridge-View (nach FormIntegration vor I18n); Designer-View bewusst aus (UX-Rule liegt in `whenToUse.avoid`, threshold ist dev-capacity-planning). 7 neue vitest-Cases. ADR-018 begründet top-level-statt-axes-embed, single-point-numeric-statt-range, free-string-unit-statt-enum, name-und-metric-getrennt, Dev+Bridge-Render. **Schließt den P2-Bucket** — Phase-1 Schema ist strukturell vollständig. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `content/components/{combobox,tabs,modal}.yaml`, `site/src/components/sections/PerformanceSection.astro`, `site/src/components/views/{Dev,Bridge}View.astro`, `docs/schema.md`, `docs/adr/018-performance-thresholds.md`.

## P3 — Site & SVG-Polish

- [x] **P3-17 Site-A11y-Fixes** — Skip-Link in `site/src/layouts/Base.astro` (off-screen, slides in on `:focus`/`:focus-visible`, jumps to `#main`), `<main id="main">`, `<nav aria-labelledby="components-heading">` mit `<h2 id="components-heading">` um den Component-Grid auf der Index-Seite. Erledigt 2026-04-29. Dateien: `site/src/layouts/Base.astro`, `site/src/pages/index.astro`, `site/src/styles/global.css`.
- [x] **P3-18 Anatomy-SVG-Verbesserungen** — Required-vs-Optional bereits via `stroke-dasharray="6 4"` differenziert (kein Touch nötig). Floating-Slots erhalten jetzt `class="anatomy-floating"` (CSS `drop-shadow`-Filter, light/dark) plus eine kreisförmige `z`-Badge in Akzentfarbe. Wiederholte Slots (`repeats > 1`) zeigen oben rechts ein `n×`-Pill (`anatomy-repeat-count`). Asymmetrische Layouts: bestehender `aspect`-Pfad in `layoutGrid` bleibt korrekt — keine Anpassung nötig. Tests: zwei neue vitest-Cases gegen Combobox. Erledigt 2026-04-29. Dateien: `shared/src/svg.ts`, `site/src/styles/global.css`, `shared/tests/schema.test.ts`.
- [x] **P3-19 Shiki-Integration** — `markdown.shikiConfig` mit Dual-Theme (`github-light` / `github-dark-default`), `defaultColor: false`, `wrap: true`. Dark-Theme über `:root[data-theme='dark']` statt `prefers-color-scheme`. Erledigt 2026-04-29. Dateien: `site/astro.config.mjs`, `site/src/styles/global.css` (`.astro-code` Basis-Styles + Dark-Override).
- [x] **P3-20 Eyebrow ohne MutationObserver** — Per-View-Labels jetzt CSS-only via `:root[data-view='…'] [data-view-eyebrow]::before { content: '…'; }` (gleiches Muster für `[data-anatomy-caption]`). Komplettes `<script>`-Block aus `[id].astro` entfernt. Erledigt 2026-04-29. Dateien: `site/src/pages/components/[id].astro`, `site/src/styles/global.css`.
- [x] **P3-23 Site-Responsiveness-Fix + Phase-1-Banner-Removal** — Sidebar stacked oberhalb des Articles auf Tablet/Phone-Viewports, weil die Hide-Rule `.component-shell > nav.sidebar { display: none }` in `ComponentPageShell.astro`'s scoped `<style>`-Block zwar die `.component-shell` (eigener Scope) traf, aber `<nav class="sidebar">` aus dem child-component `SiteSidebar.astro` (anderer Astro-Scope) nicht — Astro fügt die scope-Klasse jedem element-selector via `:where(.astro-XXXX)` hinzu, und cross-component-Selektoren scheitern dadurch silent. Fix: `.component-shell`-Layout-Rules nach `site/src/styles/global.css` migriert (unscoped, matched egal wer das nav rendert), `<style>`-Block aus `ComponentPageShell.astro` entfernt. Mobile-Nav nicht nur hidden sondern als native `<details>`-Disclosure rendered: `SiteSidebar.astro` bekommt `variant?: 'desktop' | 'mobile'`-Prop und wird zweimal gerendert — desktop in Grid-Spalte 1, mobile als `<details class="sidebar-mobile">` oben in `.component-shell__content` (nur visible ≤960). `<style is:global>` für sidebar+mobile rules damit beide variants konsistent stylen ohne erneut auf scoping zu treffen. `data-pagefind-ignore` wrapper auf mobile-disclosure damit Pagefind-Index nicht durch Sidebar-Links auf jeder Component-Seite verschmutzt wird. Breakpoints angeglichen auf 640/960-cluster: header height 767→639, breadcrumb 900→961, view-tab labels 768→640. `body { overflow-x: hidden }` als safety net gegen wide tables/SVGs. Phase-1-Banner aus `index.astro` und `.phase-banner` CSS aus `global.css` entfernt. Verifiziert in Chrome (1440 / 1280 / 961 / 800 / 500): 1440+1280 desktop-grid + sichtbarer breadcrumb; 961 boundary still-desktop; 800+500 sidebar hidden + mobile-disclosure visible; keine page-horiz-scroll an irgendeiner Größe; divergence-table scrollt horizontal innerhalb `.table-scroll`-wrapper. Erledigt 2026-04-30. Dateien: `site/src/styles/global.css`, `site/src/components/ComponentPageShell.astro`, `site/src/components/SiteSidebar.astro`, `site/src/pages/index.astro`.
- [x] **P3-22 Editorial-Reference visual refresh per UIAnatomy Design System brief** — Token-Vokabular auf paper/ink/three-view-accent-triplets vereinheitlicht (legacy Surface/FG/Accent-Aliasse zeigen auf neue Tokens — null Bruch im Bestand), Newsreader Variable als Display-Font (Source Serif 4 entfernt), Wordmark + Glyph aus `UIAnatomy Design System/assets/` nach `site/public/brand/` plus favicon, 240 px Component-Page-Sidebar mit 5 Kategorien (Containers/Disclosure/Inputs/Overlay/Feedback) als statische Map in `site/src/data/categories.ts` ohne Schema-Field, View-Switcher als Pill mit mono-caps Labels und view-tinted active state, Anatomy in `<figure class="figure">` mit `figure-caption`, Mismatches als 3-Spalten Grid (figma-side designer-50 / gap auf highlight / code-side dev-50), Site-Header ohne backdrop-blur (brief: glass off-brand). Dark-Mode bleibt als invertierte Ink-Canvas. Erledigt 2026-04-30. Dateien: `site/src/styles/global.css`, `site/src/layouts/Base.astro`, `site/src/components/{ComponentPageShell,SiteSidebar,SectionHeader,AnatomyDiagram}.astro`, `site/src/components/sections/MismatchesTable.astro`, `site/src/components/sections/TokensTable.astro`, `site/src/data/categories.ts` (neu), `site/public/brand/{wordmark,glyph}.svg` + `favicon.svg` (neu), `site/package.json` (font-swap).
- [x] **P3-25 Agent-discovery Link headers (RFC 8288 / 9727 / 8631)** — Homepage `/` emittiert jetzt drei Link-Response-Header für agent-crawler-discovery: `rel="api-catalog"` → `/.well-known/api-catalog` (RFC 9727 §3), `rel="service-desc"` → `/api/components.json` (RFC 8631 §4.2 — machine-readable, daher `service-desc` statt `service-doc`), `rel="sitemap"` → `/sitemap-index.xml` (IANA-registered). Mechanismus: Cloudflare-Pages `_headers`-File am publish-root (`site/public/_headers` flowt über Astros static-copy nach `site/dist/_headers`). Catalog-Resource ist statisches RFC-9727-Linkset in `application/linkset+json` an `site/public/.well-known/api-catalog` (extensionlos per RFC 8615); Content-Type via gleichem `_headers`-Block plus `Cache-Control: public, max-age=300`. Linkset enthält zwei Members: Homepage-Anchor (`service-desc` + `sitemap`) und MCP-Anchor (`describedby` → MCP-Spec, plus `type: application/json` + `title`). `/mcp` bewusst **nicht** als top-level-Link-Header (kein IANA-rel für MCP, generic crawler ignorieren extension-URI-rels) — Surfacing nur via Linkset-Member. Scope strikt homepage-only (`/`-Block in `_headers`); negative-Checks bestätigen 0 Link-Header auf `/components/modal` und `/api/components.json`. Lokale Verifikation via `pnpm -C site build` + `wrangler pages dev site/dist`: `curl -sI /` zeigt drei Link-rels (CF Pages comma-joint per RFC 8288 §3 äquivalent), `/.well-known/api-catalog` antwortet `application/linkset+json` mit gültigem JSON. Out-of-Scope: per-component-Endpoints (`/api/components/[id].json`) im Linkset enumerieren — agents drillen via Index-Endpoint; sitewide-Headers; extension-URI-rel für MCP. Erledigt 2026-04-30. Dateien: `site/public/_headers` (neu), `site/public/.well-known/api-catalog` (neu).
- [x] **P3-40 MCP `validate_implementation` tool** — schließt sowohl developer- als auch AI-agent-gap aus `docs/personas.md` ("agents need to self-verify generated UI code"). Pure-derivational tool, kein neues schema. **`shared/src/validate.ts`** (neu) exportiert `validateImplementation({component, code, framework}) → ValidationReport` mit framework-aware event-name-detection: react = `on<PascalCase>`, vue = `@event` / `v-on:event` / `emit('event')`, angular = `(event)`, webComponents = bare-name strings. Lowercase-substring-search gegen anatomy.required-slot-ids, axes.variants, axes.properties.name, events[].name. Output-shape: summary (counts pro axis), missing (nicht-gefundene canonical names), notes (heuristic-disclaimer + edge-case-explainer), `_about` pointing zur a11y-fixture-endpoint für behavioural assertions. **Important caveat baked in**: substring search produziert false-negatives bei aliased/minified identifiers — agents werden explizit vor "treating missing entries as defects" gewarnt; tool ist `did the author at least name the canonical concepts?`-sanity-check, kein Test-Substitute. shared subpath-export `@uianatomy/shared/validate` plus index.ts re-export. 3 vitest-cases: Radix-Modal-exampleCode (broad coverage), garbage-code (zero matches), Vue-headlessui-Modal-exampleCode (vue-event-syntax-path executes cleanly). Test-count shared 117 → 121 (+4). **mcp-server**: neuer `validate_implementation`-tool registriert mit zod-schema (`componentId: z.string()`, `code: z.string().min(1)`, `framework: z.enum(['react','vue','angular','webComponents'])`); wraps shared-`validateImplementation`. Tool-count 17 → 18. Test-count 25 → 28 (+3): garbage-code-zero-matches, unknown-componentId-error, real-radix-example-well-formed-shape (uses `get_implementations` to fetch the example so test stays in sync mit canon ohne YAML-duplication). **Worker** unverändert — tool nutzt nur `getComponents()` cache der schon im module-init via `setComponents` warm ist. **SKILL.md**: tool-table um zeile erweitert mit framework-enum doc, Library-implementations-flow + neuer "How does my generated Modal compare to canon?"-typical-flow, tool-count 17 → 18; sha256-digest in agent-skills/index.json regeneriert sich automatisch. **personas.md**: developer-persona + AI-agent-persona `validate_implementation`-gap auf [x] geflippt mit P3-40-pointer. Verifikation: `pnpm -r test` 121+28=149 grün, `pnpm -r typecheck` + worker-tsc 0 errors. Erledigt 2026-04-30. Dateien: `shared/src/validate.ts` (neu), `shared/src/index.ts`, `shared/package.json` (subpath-export), `shared/tests/validate.test.ts` (neu), `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`, `site/public/.well-known/agent-skills/index.json` (regenerated digest), `docs/personas.md`.
- [x] **P3-39 SEO hardening (3 phases)** — drei discrete commits gegen die SEO-deep-think-audit. **Phase 1** (commit `e6f45e8`): JSON-LD structured-data + canonical-links + view-aware titles+descriptions. Neuer `site/src/lib/jsonLd.ts` mit `siteJsonLd()` (WebSite + Organization + WebAPI + SoftwareApplication graph für home, inkl. SearchAction für sitelink-search-box, MCP-server als SoftwareApplication, /api als WebAPI), `componentJsonLd({component, view, pathname})` (TechArticle mit view-aware headline + dateModified von lastReviewed + author/publisher + breadcrumb + DefinedTerm-mainEntity + per-view OG-image), `compareJsonLd` + `searchPageJsonLd` (WebPage + breadcrumb). Base.astro neue optional props: `ogType` (`'website'|'article'`, defaults `'website'`, components override `'article'`), `articleModifiedTime`, `jsonLd`. Emits `<link rel="canonical">` + `og:site_name` + `og:locale=en_US` + `og:url` + `article:modified_time` + JSON-LD-script. ComponentPageShell.astro view-aware titles: designer = `Modal — UI Anatomy`, dev = `Modal · Dev view — UI Anatomy`, bridge = `Modal · Bridge view — UI Anatomy`; descriptions ähnlich differentiert. **Phase 2** (commit `2a17d06`): sitemap freshness + robots indexing-hygiene. astro.config.mjs liest content/components/*.yaml beim load, baut `lastReviewedById`-map, sitemap-`serialize`-callback maps `/components/<id>(/<view>)`-URLs auf canonical-`lastmod` (statt build-clock). Sitemap-filter dropped `/404` explicit. `js-yaml` als site-devDep formal hinzu (war transitively da). robots.txt um `Disallow`-rules für `/api/`, `/og/`, `/pagefind/`, `/llms.txt`, `/llms-full.txt`, `/*.md$` erweitert mit comment-block der den indexing-vs-fetching-distinction erklärt (Content-Signal-`ai-input=yes` unverändert — robots gates indexing nicht fetching). **Phase 3** (commit `00b4a92`): wrangler.jsonc `html_handling: "auto-trailing-slash"` → `"drop-trailing-slash"` matched Astros `trailingSlash: 'never'` + canonical-link-form. Removed redirect-hop pro request: `/components/modal` jetzt 200 direkt (war 307 → /components/modal/), `/components/modal/` 307→canonical. Verifikation via `wrangler dev`: canonical-form serves direkt 200 + JSON-LD im HTML + sitemap zeigt distinct lastmod pro component (e.g. `<lastmod>2026-04-28T00:00:00.000Z</lastmod>` für button/card/combobox/modal, `2026-04-29T00:00:00.000Z` für andere); robots.txt zeigt 6 Disallow-rules; markdown-negotiation + MCP + OG endpoints unverändert. 73 pages built, 0 broken links, typecheck 0 errors über 56 site files, 143 vitest cases grün. **Bewusst out-of-scope**: keyword-stuffing in titles, content-marketing/blog, AI-generated supplementary content (Google-penalty-risk), backlink-outreach, FAQPage/HowTo schema (forced fit), per-component RSS (depends on P4-23 versioning, deferred), Google-Search-Console submission (manual user-step). Erledigt 2026-04-30. Dateien: `site/src/lib/jsonLd.ts` (neu), `site/src/layouts/Base.astro`, `site/src/components/ComponentPageShell.astro`, `site/src/pages/index.astro`, `site/src/pages/compare.astro`, `site/src/pages/search.astro`, `site/astro.config.mjs`, `site/package.json` (js-yaml devDep), `site/public/robots.txt`, `wrangler.jsonc`.
- [x] **P3-38 Side-by-side `/compare` view** — schließt designer + DS-maintainer-gap aus `docs/personas.md` (vsRelated-decisions ohne 2-tab-flip). Architektur: pure-derivational diff, kein neues schema. **`shared/src/compare.ts`** (neu) exportiert `computeCompareDiff(a: Component, b: Component) → ComponentDiff` als reine TS-funktion: anatomy (required-only-in-A/B + optional-only-in-A/B + shared slot-ids), variants (set-diff), properties (onlyInA/onlyInB/sharedSameKind/sharedDifferentKind mit aKind+bKind für discriminated-union-mismatches), interactive+data states (set-diff), optionalBlocks (matrix für motion/responsive/transitions/events/formIntegration/a11yAcceptance/performance presence per side), axeRules (set-diff), vsRelated (canon-prose wenn A.whenToUse.vsRelated[].id===B.id oder mirror). Output deterministisch sortiert. 4 vitest-cases (`shared/tests/compare.test.ts`): identical-components-trivial-diff, vsRelated-cross-ref-prose-surfaces (Card↔Tile), asymmetric-components (button vs combobox: variants+dataStates non-trivial), property-kind-classification (button vs link). Test-count shared 113 → 117 (+4). Subpath-export `@uianatomy/shared/compare` plus `index.ts`-re-export. **`site/src/components/compare/CompareDiff.astro`** rendered server-side: vsRelated-quote-block + grid mit anatomy/variants/properties/states/optional-matrix/axe sections, only-shows non-empty subarrays, mobile-collapse-stack ≤720. **`CompareSelector.astro`**: zwei `<select>`-dropdowns + submit-button als `<form action="/compare" method="get">` (kein form-action-state, browser-native get-redirect zu `/compare?a=&b=`). **`/compare`** static page server-renders default `card vs tile` (cross-referenziertes pair → vsRelated-block sichtbar ohne JS) plus inline-JSON-roster + client-side TS-script: liest `URLSearchParams`, validated ids gegen roster, fetched parallel `/api/components/<id>.json` für beide sides via `Promise.all`, ruft `computeCompareDiff` (browser-bundle via shared subpath-export), swappt diff-section via `innerHTML` mit XSS-escape-helper. Anatomy-SVG client-side ersetzt durch text-stub-link zum component (SVG-generator ist build-time-only, würde duplication erfordern). Header bekommt third-icon (git-compare lucide) zwischen view-switcher und search; gleiche pill-style wie .site-header__search. **Astro-config**: llms-md exclude erweitert um `'compare', 'compare/**'` (interactive page, MD makes no sense — selector dropdowns + diff-grid sind nicht prose-friendly). Verifikation via `wrangler dev`: `/compare/` 200 mit allen 8 diff-sections gerendered (From the canon, Anatomy slots, Variants, Properties, Interactive states, Data states, Schema sections declared, axe-core rules), beide anatomy-hosts present, vsRelated-quote für card↔tile sichtbar. typecheck 56 files 0 errors (war 52, +4: compare.astro + CompareDiff + CompareSelector + dispatch). 73 pages built (war 72), 0 broken links. Test-total 138 → 143 (113 shared → 117 + 25 mcp-server + 1 für andere). **Bewusst out-of-scope**: 3-way-compare (`?a=&b=&c=`), saved-comparisons, /api/compare-endpoint, anatomy-SVG-diff-overlay, mismatches-section (component-internal not relational), per-permutation-pre-render (506 perms zu schwer für 1-time use), implementations-section (Phase-2 audit-data; gehört nicht zur "which canon component to use"-frage). Erledigt 2026-04-30. Dateien: `shared/src/compare.ts` (neu), `shared/src/index.ts` (re-export), `shared/tests/compare.test.ts` (neu), `shared/package.json` (`./compare` subpath-export), `site/src/components/compare/CompareDiff.astro` (neu), `site/src/components/compare/CompareSelector.astro` (neu), `site/src/pages/compare.astro` (neu), `site/src/layouts/Base.astro` (header git-compare-icon), `site/src/styles/global.css` (.site-header__compare style mirror), `site/astro.config.mjs` (llms-md exclude), `docs/personas.md` (gap closed).
- [x] **P3-37 A11y test fixture endpoint** — schließt highest-leverage developer-gap (per `docs/personas.md`): "documented in canon" → "asserted in test suite"-Friction war der #1 reason warum canonical a11y-promises rotten. Neuer static endpoint `site/src/pages/api/components/[id]/a11y-fixture.json.ts` (Astro `getStaticPaths` über alle 23 components) emittiert pro component eine fixture mit `componentId`, `componentName`, `lastReviewed`, `keyboardWalk` (array von `{keys, expected}`), `announcements` (array von `{trigger, expected}`), `axeRules` (array von kebab-case-rule-ids), plus `_about` (multi-line prose mit explicit Playwright + `@axe-core/playwright` und Jest + `jest-axe` wiring-instructions: axeRules → AxeBuilder.options.runOnly, keyboardWalk → page.keyboard.press iteration, announcements → aria-live-region oder a11y-tree probes). **Uniform shape across components**: leere arrays bedeuten "canon hat dieses axis noch nicht gepinnt" nicht "kein requirement" — test-runners können die manifest stumpf iterieren ohne per-component branching. Cache-Control `public, max-age=300, must-revalidate`. Auf component-pages (`A11yAcceptanceTable.astro` rendered in Dev + Bridge views): neue `<p class="a11y-acceptance__fixture">` mit Direkt-Link zur fixture-URL — devs finden's ohne API-doc-lookup. SKILL.md no-MCP-fallback section um diesen endpoint erweitert. `docs/personas.md` developer-gap auf `[x]` geflippt mit P3-37-pointer. Verifikation: `pnpm -C site build` 23 fixture-files in `dist/api/components/<id>/a11y-fixture.json` (e.g. modal hat 4 keyboardWalk + 3 announcements + 16 axeRules; accordion hat 5 keyboardWalk-rows; alle components emit fixture auch wenn a11yAcceptance fehlt → leere arrays plus _about). 0 broken links. Erledigt 2026-04-30. Dateien: `site/src/pages/api/components/[id]/a11y-fixture.json.ts` (neu), `site/src/components/sections/A11yAcceptanceTable.astro` (download-link + style), `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`, `docs/personas.md`.
- [x] **P3-36 MCP `get_implementations` + `list_implementations`** — Phase-2 audit-data closure für MCP-Konsumenten. Schließt explizit den "implementations not yet on MCP"-Caveat aus SKILL.md. Pattern mirrors P5-32 content-bundle: shared/dist/implementations-bundle.json als build-time JSON-bundle (3 audits × 1 component = 3 entries shape `Record<libraryId, Record<componentId, Implementation>>`, ~150KB), via neuer `loadImplementationsFromBundle()` in `shared/src/bundle.ts` re-validated bei Worker-cold-start. mcp-server: `state.ts` bekam parallel-cache `implCache` + `setImplementations()` + `setImplementationsPromise()` + `getImplementations()` + extended `resetCache()`; `data.ts` neuer `setImplementationsDir()` + `pickImplementationsDir()` walker mit env-var support (`UIANATOMY_IMPLEMENTATIONS_DIR`); `server.ts` zwei neue tools: **`list_implementations`** (`{}` args, returns array sortiert nach libraryId/componentId mit `{libraryId, componentId, componentName, divergenceCount, lastReviewed}`) und **`get_implementations`** (`{componentId: string}` args, returns array von vollständigen Implementation-records sortiert nach libraryId, leer bei unbekanntem oder nicht-auditiertem Component — convention matched bestehende null-returning tools wie get_motion). Tool-count 15 → 17. Worker (`worker/index.ts`): zusätzlicher `setImplementations(loadImplementationsFromBundle(implBundleJson))`-call beim module-init parallel zu setComponents. Tests: `tests/server.test.ts` bekam setImplementationsDir in beforeAll/afterEach + 5 neue cases (tool list contains 17 names, list_implementations returns sorted rows, list_implementations covers cdk/headlessui/radix für modal, get_implementations(modal) returns 3 full records, get_implementations(button) leer, get_implementations(unknown) leer); `tests/bundle.test.ts` bekam impl-bundle equivalence-check parallel zur content-bundle. Test-count 19 → 25 in mcp-server, 132 → 138 total. SKILL.md: tool table um 2 zeilen erweitert, "Library implementations" section von "(Phase 2, not yet on MCP)" → "(Phase 2)" umgeschrieben mit pointer auf neue tools, neuer typical-flow ("How does Radix' Dialog diverge from canonical Modal?" → get_implementations(modal) → inspect radix entry's divergence + exampleCode), tool-count 15 → 17; sha256-digest in agent-skills/index.json regeneriert sich automatisch via prebuild. Verifikation via `wrangler dev` + curl: `tools/list` zeigt beide neuen tool-names; `list_implementations` returned `[{cdk/modal, divergenceCount:17}, {headlessui/modal, divergenceCount:17}, {radix/modal, divergenceCount:17}]` korrekt sortiert; `get_implementations({componentId:"modal"})` returned 3 Implementation-records mit vollständigem exampleCode + divergence + rationale; `get_implementations({componentId:"button"})` returned `[]`. `wrangler deploy --dry-run` 421 assets, 357 KiB gzip. Zusätzlich neu: **`docs/personas.md`** (4 personas: Designer / Developer / Design system maintainer / AI agent) als gap-tracking-ground für künftiges feature-work — referenced aus `docs/CLAUDE.md` reading-order zwischen views.md und decisions/. Erledigt 2026-04-30. Dateien: `shared/src/bundle.ts`, `shared/scripts/bundle-implementations.mjs` (neu), `shared/package.json`, `mcp-server/src/state.ts`, `mcp-server/src/data.ts`, `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `mcp-server/tests/bundle.test.ts`, `worker/index.ts`, `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`, `site/public/.well-known/agent-skills/index.json` (regenerated digest), `docs/personas.md` (neu), `docs/CLAUDE.md`.
- [x] **P3-35 Cloudflare Pages → Workers Static Assets** — CF Pages auto-deploy started failing nach den letzten feature-commits mit "Failed: error occurred while running deploy command" + wrangler-suggestion-template (`{ name, compatibility_date, assets: { directory } }`). Platform konvergiert Pages auf Workers Static Assets — statt am bewegenden Pages-Config-Surface chasen, direkt auf Workers Static Assets migriert (ADR-022 supersedes ADR-021). **Wrangler 3.114 → 4.87**, `@cloudflare/workers-types ^4` als root devDep. Neu: root `wrangler.jsonc` (ersetzt ADR-021's "no wrangler config — Dashboard is source of truth"-Pattern; CF Wrangler 4.x akzeptiert config-as-code am pnpm-workspace-root jetzt sauber) mit `name: uianatomy` + `main: worker/index.ts` + `compatibility_date: 2026-04-30` + `compatibility_flags: ["nodejs_compat"]` + `assets: { directory: ./site/dist, binding: ASSETS, run_worker_first: true, html_handling: auto-trailing-slash, not_found_handling: 404-page }` + `observability.enabled: true`. Neu: `worker/index.ts` (~100 LOC) consolidates die zwei früheren `functions/`-Files in einen einzelnen `export default { fetch }`-Handler — dispatch-by-path: `/mcp` → handleMcp() (Streamable HTTP transport, identische Logik wie ADR-021's `functions/mcp.ts`), sonst `maybeMarkdown()` (Accept-text/markdown-Negotiation, fetched MD-sidecar via `env.ASSETS.fetch()`, content-type-validated weil Worker-Static-Assets 404-page-fallback unter manchen Configs als 200 ausspielt), sonst `env.ASSETS.fetch(request)` als final-passthrough. **`run_worker_first: true`** ist required: ohne wäre der Worker für matched-asset-paths gar nicht invoked, und MD-Negotiation könnte den Accept-header auf HTML-page-requests nicht inspizieren. Cost: 1 zusätzlicher Worker-invoke pro request — negligible at site-scale. `worker/tsconfig.json` (neu) mit `@cloudflare/workers-types`. Deploy-Script: `wrangler pages deploy site/dist --project-name --branch` → `wrangler deploy` (no args; reads wrangler.jsonc). Plus `dev:worker`-script (`wrangler dev`) für end-to-end-local-test der deployed-surface (vs `pnpm dev` = astro-dev für component-work). **Gelöscht**: komplette `functions/`-dir (`functions/mcp.ts` + `functions/_middleware.ts`). **Docs**: ADR-022 neu (supersedes ADR-021); ADR-021 status auf "Superseded by ADR-022"; ADR-003 status-line von "Pages Function" auf "Worker handler"; README.md von "mcp-server: Pages Function" auf "mcp-server: core, worker/: entry"; roadmap.md von "Cloudflare Pages" auf "Cloudflare Workers (Static Assets, ADR-022)". **Verifikation via `wrangler dev`**: `/components/modal` ohne Header = 307→trailing-slash → 200 HTML; mit `Accept: text/markdown` = 200 MD + x-markdown-tokens=11775 + Vary; `/mcp` GET = 405 Method Not Allowed; `/mcp` POST + Accept SSE = 200 mit MCP `tools/list`-response stream (15 tools); `/og/components/modal.png` = 200 image/png; `/.well-known/mcp/server-card.json` = 200 application/json; `/components/buttn` = 404 mit custom-404-page; `/robots.txt` = 200 (Content-Signal preserved). `wrangler deploy --dry-run` parsed 6 valid header rules (`_headers` honored), 421 assets read aus site/dist, gzip 347 KiB. Tests + typecheck unverändert grün. ADR-021's "no root wrangler config — workspace-root detection conflict" obsolete: Wrangler 4.87 + Workers-Static-Assets-path liest `wrangler.jsonc` clean. Erledigt 2026-04-30. Dateien: `wrangler.jsonc` (neu), `worker/index.ts` (neu), `worker/tsconfig.json` (neu), `package.json` (deploy-script + `@cloudflare/workers-types` devDep + wrangler@4 bump), `pnpm-lock.yaml`, `docs/adr/022-host-cloudflare-workers.md` (neu), `docs/adr/021-host-cloudflare-pages.md` (status), `docs/adr/003-mcp-server-architecture.md` (status + decision-text), `README.md`, `docs/roadmap.md`. Gelöscht: `functions/`.
- [x] **P3-34 GitHub Actions CI** — `.github/workflows/ci.yml` (vorher kein CI). Triggert auf push to main + pull_request to main, single job auf ubuntu-latest mit timeout-minutes:12. Steps: checkout → pnpm-action-setup@v4 (version aus `packageManager` von `package.json`, 9.12.0) → setup-node@v4 (`node-version-file: .nvmrc`, 22, `cache: pnpm`) → `pnpm install --frozen-lockfile` → **`pnpm -F @uianatomy/shared build`** (cold-runner-fix: andere packages importieren via subpath-exports `@uianatomy/shared/schema` und `./bundle` aus `dist/` — ohne initial-shared-build crash typecheck mit "Cannot find module '@uianatomy/shared/schema'") → `pnpm -r typecheck` → `pnpm -r test` → `pnpm -r build` (heaviest step: prebuild-scripts MCP-server-card + agent-skills-index, astro-build, postbuild fix-llms-md, astro-llms-md sidecars+llms.txt, astro-og-canvas 71 PNGs, astro-broken-links-checker — letzterer mit `throwError: false` warned-only weil Consistency-Test bereits vsRelated-cross-refs enforced) → upload-artifact@v4 retention 14d für `site/.link-checker/` (nur falls Log existiert via `if-no-files-found: ignore`). Concurrency-group `ci-${{ github.ref }}` mit `cancel-in-progress: true` für stale-run-invalidation auf rapid-pushes. Permissions minimal `contents: read`. **Erste run timing**: 53s end-to-end (install + 5 steps + post-cleanup) — well-within timeout-budget. Initial run failed mit fehlendem `@uianatomy/shared`-types-resolution; fixed via dedicated shared-build-step zwischen install und typecheck. Annotation-warning vom GH-Runner über Node-20-deprecation ist purely action-internal (actions/checkout@v4 et al laufen noch auf Node 20), affects nicht den Workflow-Erfolg — wird durch zukünftige action-version-bumps adressiert. **Bewusst nicht im CI**: deploy (manual via `pnpm deploy` oder Cloudflare-Pages-Dashboard-Auto-Deploy auf push to main); pa11y-ci (separate task — siehe README/options-list); external-link-checking-flip; Astro-6.2-bump. Erledigt 2026-04-30. Datei: `.github/workflows/ci.yml` (neu, 64 Zeilen).
- [x] **P3-33 Custom 404-Page mit Did-you-mean-suggestion** — `site/src/pages/404.astro` neu (Cloudflare Pages auto-served `dist/404.html` für jede unmatched URL). Style mirrored site-canon: hero-eyebrow + italic display-headline + ink-muted lede + zwei action-buttons (primary "All components" → /, secondary "Search the canon" → /search) + 5-component-quick-list mit grid-row-pro-Eintrag (display-strong-name + muted-description). **Did-you-mean-typo-suggestion**: 404.astro ist statisch (build-time, `Astro.url.pathname` ist immer `/404` — kein server der den tatsächlichen request kennt), daher Roster als inline-JSON-script (`<script is:inline type="application/json" id="canon-roster">`) plus client-side TS-script: liest `window.location.pathname`, matched `/components/<slug>`, computed Levenshtein-distance zwischen slug und allen 23 canon-IDs, surfaced closest match wenn distance ≤ max(2, floor(slug.length/3)) — Threshold scaled mit slug-Länge so dass kurze IDs strenger matchen. Beispiel `/components/buttn` (distance 1 zu `button`, threshold 2) → "Did you mean Button?". URLs ohne `/components/`-prefix oder mit valid slug überspringen die suggestion. Render-Pattern: hidden `<p data-not-found-path hidden>` + `<p data-not-found-suggestion hidden>` als Slots, client-script unhided + füllt slug/href/name. Kein hydration-flash dank `hidden`-default. `is:inline`-directive auf JSON-script ist required (sonst `<script>`-bundling-hint vom astro:check). Verifikation via `wrangler pages dev`: `curl /nonexistent` returned 404 + page rendert; `curl /components/buttn` returned 404 + inline-roster-JSON shipped (verified first 8 entries: accordion/alert/banner/button/card/combobox/...); modal-page schiffft das script NICHT (404-page-only). Build-pipeline grün: 72 pages (71 + 404), 0 broken links, typecheck 0 errors über 52 files. Erledigt 2026-04-30. Datei: `site/src/pages/404.astro` (neu).
- [x] **P3-32 astro-expressive-code für Code-Blocks** — Implementations-Section (Radix tsx, Headless UI vue, CDK ts) ist jetzt mit `astro-expressive-code` 0.41.7 statt Astros built-in `<Code>` (Shiki). Drop-in upgrade: gleiche dual-theme (`github-light` / `github-dark-default`), `wrap: true` als default, plus expressive-code's Default-Plugins frame (window-chrome + filename slot), copy-button (clipboard), und scoped class-namespace (`.expressive-code`, `.ec-line`, `.frame`). Config-Split-Pattern: `astro-expressive-code` integration in `astro.config.mjs` ohne args (`expressiveCode()`); EC-options in **separate `site/ec.config.mjs`** weil `themeCssSelector: (theme) => ...` als Function nicht JSON-serializable ist und Astro's `<Code>`-Component die config zur Render-Zeit aus JSON re-loaded — inline-config in astro.config.mjs crash mit "Failed to render a `<Code>` component... not serializable to JSON. Please create a separate config file called `ec.config.mjs`". Theme-Selector matched bestehende `[data-theme='light']` / `[data-theme='dark']` HTML-attribute (statt prefers-color-scheme media-query) für konsistente Theme-Toggle-Reaktion. Dropped: alte `markdown.shikiConfig` aus astro.config.mjs (replaced); 11-zeilige `.astro-code`-CSS-Block aus `global.css` (alte Shiki-classes); 6-zeilige scoped `.impl-card__example :global(.astro-code)`-Override aus `ImplementationCard.astro`. ImplementationCard-Import von `'astro:components'` auf `'astro-expressive-code/components'` umgestellt; Component-API identisch (`<Code code={...} lang={...} wrap={true} />`). Build-pipeline grün: 71 pages + 71 OG PNGs + 71 .md sidecars + Pagefind + sitemap + llms.txt + 0 broken links; typecheck 0 errors über 51 files (war 50, +ec.config.mjs); 132 tests grün. Erledigt 2026-04-30. Dateien: `site/ec.config.mjs` (neu), `site/astro.config.mjs` (-`markdown.shikiConfig`, +`expressiveCode()`), `site/src/components/sections/ImplementationCard.astro` (import-swap, CSS-cleanup), `site/src/styles/global.css` (`.astro-code`-Block weg), `site/package.json` (+`astro-expressive-code` dep).
- [x] **P3-31 OG-Bilder pro Seite via astro-og-canvas** — `astro-og-canvas` 0.11.1 + `canvaskit-wasm` 0.41.1 als Skia-basierter Build-Time-Renderer. Endpoint `site/src/pages/og/[...slug].ts` exportiert `OGImageRoute({ param: 'slug', pages, getImageOptions })` (mit `await` — `OGImageRoute` returned `Promise<{getStaticPaths,GET}>`; ohne await sieht Astros static-paths-Analyzer das Export nicht und bricht mit `GetStaticPathsRequired`). `pages`-Map wird zur Build-Zeit aus `getCollection('components')` synthetisiert: `index`, `search`, `components/<id>` (designer-default) plus `components/<id>/<view>` für `dev|bridge` — total 71 Routes. Filename **ohne** `.png`-Suffix (`[...slug].ts` statt `[...slug].png.ts`) — OGImageRoute appendiert `.png` selbst, sonst wird's `.png.png`-doubled. Image-Template: paper bg `[250,248,240]`, ink-Title `[28,28,28]` 72px Bold, ink-muted-Description `[110,110,110]` 32px Normal, view-tinted Accent-Border 12px inline-start (designer=warm-orange `[200,120,80]`, dev=blue `[70,130,200]`, bridge=violet `[120,90,200]`, site=violet), 80px padding, font-families Inter/system-ui/sans-serif. Base.astro injiziert pro Page `<meta property="og:image">` + `<meta name="twitter:image">` (plus og:title/description/type, twitter:card=summary_large_image, og:image:width=1200, og:image:height=630) — URL-Convention spiegelt Page-Tree (`/` → `/og/index.png`; `/components/modal` → `/og/components/modal.png`; `/components/modal/dev` → `/og/components/modal/dev.png`). Astro-config-Ergänzungen: sitemap-Filter excluded `/og/`-URLs (kein indexieren), llms-md excluded `og/**` (kein .md generieren). 71 PNGs in `dist/og/` (~30-80kb je, ~2MB total). Visual-Check: Modal-Card zeigt Paper-bg + Orange-Accent-Stripe links + Bold "Modal" + dim-grey Beschreibung — clean editorial-Brand. Build pipeline grün: 71 pages + 71 og PNGs + 71 .md sidecars + Pagefind + sitemap + llms.txt + 0 broken links. typecheck 0 errors über 50 files (war 49). tests unverändert 132 grün. Erledigt 2026-04-30. Dateien: `site/src/pages/og/[...slug].ts` (neu), `site/src/layouts/Base.astro` (og:image + twitter:card meta-tags), `site/astro.config.mjs` (sitemap+llms-md `/og/` exclude), `site/package.json` (+`astro-og-canvas` + `canvaskit-wasm` devDeps).
- [x] **P3-30 astro-icon migration** — handgerollte Icon.astro mit hardcoded SVG-paths ersetzt durch `astro-icon` + `@iconify-json/lucide`-Set. Public-API von `<Icon>` unverändert — gleiche Props (`name: IconName`, `size`, `strokeWidth`, `class`, `label`), gleiche Callsites (16 Aufrufe), gleicher SVG-Output (alle Lucide-Icons identisch zu vorher). Internals: NAME_MAP von Local-Vocabulary (z.B. `'check'`) auf Iconify-Namen (`'lucide:check'`), Render delegated an `<Icon name=... />` aus `astro-icon/components`. Conditional-render-Pattern (label vs aria-hidden) als zwei `<Icon>`-Branches (TS strict-types verlangen literal `role="img"` statt unioned string). Dead `lucide-static@^1.14.0`-dep entfernt (nirgends importiert war's, durch Icon-Migration eindeutig obsolet). Bundle-Save: zur Build-Zeit lazy-load von Iconify statt eigener Path-Map (~3kb hardcoded shapes weg). Erledigt 2026-04-30. Dateien: `site/src/components/Icon.astro`, `site/astro.config.mjs` (+`icon()` integration), `site/package.json` (+`astro-icon` + `@iconify-json/lucide` devDep, -`lucide-static`).
- [x] **P3-29 astro-broken-links-checker + 7 vsRelated-Drift-Repairs** — `astro-broken-links-checker` 1.1.0 (NB: package-name plural "links") als Build-Time-Integration; checked alle `<a href>` + `<img src>` in den 71 generated HTML-Pages, schreibt Log nach `site/.link-checker/broken-links.log`. **Erste Drift-Detection landed real bugs**: 7 vsRelated-Refs zeigten auf nicht-existierende Canon-Komponenten — `icon-button` (von button, link), `table` (list-item), `input` (search-input, tag-input), `radio` (segmented-control, select), `toggle` (segmented-control), `progress-bar` (stepper), `image-gallery` (tile). Diese waren aspirationale Cross-Refs, die nie als Canon-YAML existierten. Cleanup: 9 `whenToUse.vsRelated`-Entries entfernt; alle betroffenen Komponenten haben weiterhin ≥1 vsRelated (Schema-Min-Requirement gewahrt). Plus neuer Consistency-Test in `shared/tests/consistency.test.ts` ("whenToUse.vsRelated[].id resolves to existing canonical component") — verhindert künftige Drift, läuft im pnpm -r test loop (jetzt 132+1=133 Tests... actually noch 132 weil bundle-test sich nicht ändert). `_headers` für broken-link-checker config: `checkExternalLinks: false` (kein HTTP-Round-trip pro Build), `throwError: false` (warnen, nicht failen). `.link-checker/`-Cache-Dir in root .gitignore. Erledigt 2026-04-30. Dateien: `site/astro.config.mjs` (+`brokenLinks()` integration), `site/package.json` (+devDep), `.gitignore` (+/.link-checker/), `content/components/{button,link,list-item,search-input,tag-input,segmented-control,select,stepper,tile}.yaml` (vsRelated cleanup), `shared/tests/consistency.test.ts` (+new test).
- [x] **P3-28 Sidebar-Leak in Markdown-Output beseitigen** — Folge-Cleanup zu P3-27. Mobile-`SiteSidebar` lebte innerhalb `<article class="component-shell__content">`, daher wurde die komplette Sidebar-Nav (~50 Zeilen) in jede generierte `.md`-Sidecar mit ausgeliefert. Fix: mobile-Variante aus dem `<article>` heraus und auf gleiche Ebene wie Desktop-Sidebar in `.component-shell` gehoben. Layout korrekt durch `display: none` (mobile-variant default an Desktop-Viewports + `.sidebar` mobile per Media-Query) — Grid sieht zu jedem Zeitpunkt nur 2 sichtbare Children, platziert sie korrekt (Desktop: `[sidebar | article]`, Mobile: `[sidebar-mobile-row1; article-row2]`). `data-pagefind-ignore` vom externen Wrapper-`<div>` direkt aufs `<details class="sidebar-mobile">`-Element verschoben (cleaner; Wrapper weg). Resultat: modal.md von 1224 → 1184 Zeilen, x-markdown-tokens 12030 → 11805, erste content-zeile jetzt "Designer view" statt der Container/Disclosure/Inputs/Overlay/Feedback-Liste. Verifikation: `pnpm -C site build` 71 Pages, `pnpm -r test` 132 grün, `pnpm -C site typecheck` 0 errors über 49 files; `wrangler pages dev` zeigt Desktop-`.component-shell` mit `.sidebar` + `<article>` korrekt platziert, mobile-`<details class="sidebar-mobile" data-pagefind-ignore>` rendert mit erwarteter Struktur. Erledigt 2026-04-30. Dateien: `site/src/components/SiteSidebar.astro` (data-pagefind-ignore aufs details), `site/src/components/ComponentPageShell.astro` (mobile-sidebar aus article raus, Wrapper-div weg).
- [x] **P3-27 Markdown-Negotiation für Agents (self-hosted)** — Cloudflare-natives "Markdown for Agents" ist auf Pro+ gegated; Free-Plan kann nicht togglen. Self-hosted Lösung: Build-Time-MD-Sidecars + Pages-Middleware. (1) `astro-llms-md`-Integration in `astro.config.mjs` (`contentSelector: 'article'`, `titleSelector: 'h1'`, `generateIndividualMd|LlmsTxt|LlmsFullTxt: true`, exclude api/pagefind). Generiert pro built-Page eine `.md` neben `.html` (`/components/modal` → `dist/components/modal.md`, `/components/modal/dev` → `dist/components/modal/dev.md`, `/` → `dist/.md`) plus `dist/llms.txt` + `dist/llms-full.txt`. Bonus-Signal für AI-Agents über das gefragte hinaus. (2) Selector-Switch von `'main'` auf `'article'`: `<main>` enthält die SiteSidebar (leakt 50+ Zeilen Nav), `<article>` ist sauber. Index-Page bekam `<div class="shell">` → `<article class="shell">`-Refactor (semantisch sowieso korrekter). Search-Page hat schon `<article>`, ComponentPageShell auch. (3) `site/scripts/fix-llms-md.mjs` postbuild-Script renamed `dist/.md` → `dist/index.md` (Hidden-File-Filter auf POSIX + CF Pages würde sonst die Root-MD nicht ausliefern). (4) `functions/_middleware.ts` (Pages Middleware, fires for ALL routes including static): auf `Accept: text/markdown` rewriten URL zu MD-Sibling, `next(rewrittenRequest)` holt das Asset, response Content-Type auf `text/markdown; charset=utf-8` + `x-markdown-tokens: <int>` (Heuristik `Math.ceil(body.length / 4)` — Cloudflares public-tokenizer-stand-in) + `Cache-Control: public, max-age=300, must-revalidate` + `Vary: Accept`. **Critical bug found + fixed during smoke-test**: CF Pages serviert `index.html` als 200-Fallback für jede unmatched URL, daher `mdResponse.ok` allein reicht nicht — Content-Type-Check (`text/markdown` prefix) zwingend, sonst würde `/mcp` mit `Accept: text/markdown` als HTML-fallback-as-markdown ausgeliefert. Plus `/mcp` defensiv in SKIP_EXACT, plus skip-prefix-list (`/api/`, `/.well-known/`, `/pagefind/`, `/_astro/`, `/brand/`) plus skip-extensions ausser .html. (5) `_headers` global `/* Vary: Accept` für korrekte CDN-Cache-Differenzierung zwischen HTML- und MD-Response je URL. **Lokale Verifikation via `wrangler pages dev`**: `/components/modal` ohne Header = HTML 200; mit `Accept: text/markdown` = 200 MD + x-markdown-tokens=12030 + Vary; `/components/modal/dev` mit Accept = 200 MD; `/` mit Accept = 200 MD (post-fix-llms-md); `/api/components.json` + `/.well-known/*` + `/sitemap-index.xml` + `/robots.txt` + `/llms.txt` mit Accept = unverändert (skip-list); `/mcp` mit Accept = 405 (skip-exact greift; fallthrough zur MCP-Function). 71 .md-Sidecars + llms.txt + llms-full.txt generiert. Test-count unverändert (132 grün). Out-of-Scope: korrekter `<main>`/Sidebar-Refactor (Sidebar leakt aktuell durch mobile-variant innerhalb article — ~50 Zeilen Nav-Noise pro MD, content-substanz unbeeinträchtigt); echter LLM-Tokenizer am Edge (4-chars/token-Heuristik genügt für Cloudflare-x-markdown-tokens-Convention). Erledigt 2026-04-30. Dateien: `site/astro.config.mjs`, `site/package.json` (devDep `astro-llms-md` + `postbuild`-script), `site/src/pages/index.astro` (article-wrap), `site/scripts/fix-llms-md.mjs` (neu), `functions/_middleware.ts` (neu), `site/public/_headers` (Vary global).
- [x] **P3-26 Agent-Readiness: Content-Signal + MCP Server Card + Agent-Skills Discovery + WebMCP** — fünf von sieben `isitagentready.com`-Flags geräumt. (1) `robots.txt` deklariert `Content-Signal: ai-train=no, search=yes, ai-input=yes` per [contentsignals.org](https://contentsignals.org/) — indexable + agents-may-consume per request, kein Training-Korpus-Use. (2) `/.well-known/mcp/server-card.json` (SEP-1649) generiert von `site/scripts/build-mcp-card.mjs`: liest `name`/`version` aus `mcp-server/src/server.ts` via Regex auf den `new McpServer({ name, version })`-Call (canonical runtime advertisement statt package.json-scope-strip-Heuristik), schreibt `serverInfo` + `endpoint: https://uianatomy.dev/mcp` + `transport: streamable-http` + leere `capabilities.tools`. (3) `/.well-known/agent-skills/index.json` (Cloudflare Agent-Skills-Discovery RFC v0.2.0) generiert von `site/scripts/build-agent-skills-index.mjs`: scannt `site/public/.well-known/agent-skills/<name>/SKILL.md`, extrahiert `description` aus Frontmatter, computed `sha256`-Digest der exakten served-Bytes, schreibt `$schema`+`skills`-Array. Erste skill-md `uianatomy-mcp/SKILL.md` dokumentiert alle 15 MCP-Tools mit typical-flows + No-MCP-fallback (`/api/components.json`). Beide Builder als `prebuild`-Script in `site/package.json` verkettet (`node scripts/build-mcp-card.mjs && node scripts/build-agent-skills-index.mjs`) → läuft automatisch vor jedem `astro build`. (4) WebMCP-Client (`site/src/scripts/webmcp.client.ts`, ~80 LOC TS, kein MCP-SDK in browser): registriert 3 read-tools (`list_components`, `get_component`, `search_components`) gegen die existierenden `/api/components.json` + `/api/components/<id>.json` JSON-Endpoints, dual-API-guard (`registerTool` für die canonical-Spec, `provideContext`-Fallback für ältere Drafts), `Promise`-cache auf der Index-Response damit `search_components` nicht jeden Call refetcht. Eingebunden via `<script>import '../scripts/webmcp.client.ts';</script>` in `Base.astro` direkt nach existing overflow-sync-Block. (5) `_headers` erweitert um drei Cache-Rules (`max-age=300, must-revalidate` matched bestehende `.well-known/api-catalog`-Policy) plus `Content-Type`-Pins: `application/json` für mcp-server-card + agent-skills-index, `text/markdown; charset=utf-8` für `agent-skills/*/SKILL.md`. **Bewusst übersprungen**: OAuth/OIDC-Discovery (`/.well-known/openid-configuration`, `/.well-known/oauth-authorization-server`) und OAuth-Protected-Resource (`/.well-known/oauth-protected-resource`) — Site hat keine Auth, MCP-Endpoint ist intentional public; fake-Metadata wäre misleading-signal statt fix. Markdown-Negotiation (Cloudflare `Accept: text/markdown` → `Content-Type: text/markdown` + `x-markdown-tokens`) als manueller Cloudflare-Dashboard-Toggle dokumentiert (Pages → Settings → "Markdown for Agents") — kein Repo-Code. Verifikation: `pnpm -C site build` 71 Pages + Pagefind, prebuild-Hooks runnen mcp-card + agent-skills-index; `pnpm -r test` 132 Tests grün (113 shared + 19 mcp-server); `pnpm -C site typecheck` 0 errors über 48 Files; `shasum -a 256` auf SKILL.md matched index-digest exact. Out-of-Scope: per-skill-archive-type (skill-md sufficient), per-page MD-Fallback bei nicht-verfügbarem Cloudflare-Toggle, OAuth-stub-Metadata, mehr als 3 WebMCP-Tools (full-MCP-Parität würde Bundle-Size verfehlen). Erledigt 2026-04-30. Dateien: `site/public/robots.txt`, `site/public/_headers`, `site/public/.well-known/mcp/server-card.json` (generiert), `site/public/.well-known/agent-skills/index.json` (generiert), `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md` (neu), `site/scripts/build-mcp-card.mjs` (neu), `site/scripts/build-agent-skills-index.mjs` (neu), `site/src/scripts/webmcp.client.ts` (neu), `site/src/layouts/Base.astro`, `site/package.json` (prebuild-script).
- [x] **P3-24 Anatomy-Visual: Schichten + Farben + Cross-Highlight** — kombiniertes Visualisierungs-Paket auf den Anatomy-SVG. Vier visuelle Achsen, jede trägt eindeutige Bedeutung: **Fill = Tiefe** (3 Stufen: surface-raised root / paper-100 level-1 / paper-200 level-2+; floating + overlay ausgenommen), **Stroke = Slot-Art** via neuem optionalen `slotKind`-Enum (`structural` default-ink / `interactive` accent-active / `content` warm-mix / `decorative` fg-faint), **Glyphen = Daten-Indikator** (gefüllter Token-Punkt unten rechts wenn `slot.tokens` non-empty), **`<title>` = Purpose-Tooltip** (browser-native + AT-pickup als erstes `<g>`-child). Schema-Erweiterung: optionales `slotKind` enum auf `anatomySlotSchema`, ADR-020 begründet vier-Werte-vs-figma.type-derivation, optional-statt-required, canon-clean-weil-funktion-nicht-stil. Migration 23 YAMLs (132 slotKind-Annotationen, Heuristik in ADR-020 dokumentiert). SVG-Generator (`shared/src/svg.ts`): `computeDepths` rekursiv via `childrenByParent`, `hasTokens` helper, `emitSlotGroup`/`emitOverlay` neue Klassen `anatomy-depth-{0,1,2}` + `anatomy-kind-X` + `anatomy-has-tokens` + `anatomy-has-repeats` + Token-Indicator-`<circle>` + `<title>`-Element + `data-slot`-Surface auf primary und repeats. CSS (`global.css`): Fill-Tints pro Depth, Stroke-Hues via `color`/currentColor pro slotKind, `.is-cross-highlighted` (transient hover) + `.is-cross-pinned` (persistent click), Filter-Attribut-Selectors `.anatomy-host[data-filter~="X"]` für Required/Floating/Repeats/Tokens-Dimming, Pin-Override `opacity: 1 !important`. Filter-Bar: neue `AnatomyFilterBar.astro` (4 Toggles, schreibt `data-filter` token-list auf nächstes `.anatomy-host`, session-only kein localStorage), eingebunden in `ComponentPageShell.astro` direkt vor `<AnatomyDiagram />`. Cross-Highlight + Pin: ~50 Zeilen Vanilla-JS in `ComponentPageShell.astro` (delegated mouseover/mouseout/click am body, findet nächsten `[data-slot]`-Vorfahren, max ein gepinnter Slot, Click ins Leere entpint). `data-slot` auf 5 Tabellen-Components: `AnatomyTable` (vereinheitlicht `data-slot-row`→`data-slot`), `TokensTable`, `FigmaSlotTable`, `CodeSlotTable`, `A11yTable`. 7 neue Tests (3 Schema-Cases, 4 SVG-Cases) für slotKind-Schema-Validation und Generator-Output. Verifikation: `pnpm -r typecheck` 0 errors, `pnpm -r test` 113+18=131 Tests grün, `pnpm -C site build` 71 Pages + Pagefind. Compact-Mode unverändert (Indicators via `.anatomy-host--compact .anatomy-indicators { display: none; }` versteckt). Out-of-Scope explizit: keine konkreten Library-Farben im Canon, keine 3D/Schatten-Stacks, kein localStorage-Persist, max 1 gepinnter Slot zur Zeit. Erledigt 2026-04-30. Dateien: `shared/src/schema.ts`, `shared/src/svg.ts`, `shared/tests/schema.test.ts`, `content/components/*.yaml` (alle 23), `site/src/components/AnatomyFilterBar.astro` (neu), `site/src/components/ComponentPageShell.astro`, `site/src/components/sections/{AnatomyTable,TokensTable,FigmaSlotTable,CodeSlotTable,A11yTable}.astro`, `site/src/styles/global.css`, `docs/adr/020-slot-kind.md` (neu), `docs/schema.md`.

## P4 — Skalierung & Tooling

- [x] **P4-21 Cross-Component-Konsistenz-Audit-Script** — Bei 23 Komponenten ripe geworden; implementiert als `shared/tests/consistency.test.ts` (vitest, gleicher CI-pattern wie `depth.test.ts`). Acht Checks: (1) Token-Referenzen aus canonical spacing/radius/color/elevation/typography Vokabular; (2) Motion-durations + easing aus canonical motion-Vokabular; (3) Responsive-breakpoints aus canonical breakpoint-Vokabular; (4) Properties `density` deklarieren exakt `[comfortable, compact]`; (5) Properties `size` aus bounded set `[sm, md, lg, xl, full]`; (6) Interactive-states subset of `{hover, focus-visible, active, disabled, visited, current}`; (7) `tokens.color.ring` equals `color.border.focus`; (8) axe rule ids kebab-case + event names camelCase. Canonical Vokabulare in test-file als Konstanten gespiegelt — drift zwischen docs/schema.md und test ist same-PR-update-discipline. **Erste echte drift gefunden + resolved:** `drawer.size` enthielt `full` während andere components `[sm, md, lg, xl]` nutzen — `full` als canonical-extension für full-viewport-variants akzeptiert (decision in ADR-019, doc + test in same commit). 8 neue vitest-Cases (alle grün post-resolution); CI loop unverändert via `pnpm -r test`. ADR-019 begründet vitest-statt-CLI, mirror-statt-shared-module, test-level statt Zod-membership-enforcement, full-canonical-statt-drawer-divergence. Erledigt 2026-04-30. Dateien: `shared/tests/consistency.test.ts` (neu), `docs/adr/019-consistency-audit.md` (neu), `docs/schema.md` (consistency-section).
- [x] **P4-22 Search/Filter** auf Index-Page (Pagefind) — Threshold (>15) bei 23 Komponenten klar überschritten. `astro-pagefind` integration in `astro.config.mjs`; Pagefind CLI runs nach `astro build` und schreibt index + default-UI nach `site/dist/pagefind/`. Neue `/search.astro` page mit `astro-pagefind/components/Search`-component (autofocus + sub-results + 24-token excerpts). Header-search-icon link in Base.astro routed zu `/search`; magnifier icon in Icon.astro. ComponentPageShell setzt `data-pagefind-body` auf canonical designer-page (`/components/<id>`) und `data-pagefind-ignore` auf alt-view-pages (`/components/<id>/<view>`) — pagefind indexes 23 fragments statt 69, vermeidet duplicate-clutter. Pagefind-UI inherit site CSS-custom-properties für light/dark-parity. Index-size ~1MB total (index + UI + WASM), lazy-loaded auf `/search` only — kein impact auf component-page bundle. Netlify-deploy unverändert: `site/dist` publish-path inkludiert `site/dist/pagefind/`. Erledigt 2026-04-30. Dateien: `site/astro.config.mjs`, `site/package.json` (deps `astro-pagefind` + `pagefind`), `site/src/pages/search.astro` (neu), `site/src/components/ComponentPageShell.astro`, `site/src/components/Icon.astro` (search-glyph), `site/src/layouts/Base.astro`, `site/src/styles/global.css` (header-search styles).
- [x] **P4-23 Versionierung** — opt-in versioning metadata gelandet als ADR-023. Component-level `since: semver` + `changelog: Array<{version, date, summary}>` (versions unique). Per-slot/per-property inline `since?: semver` + `deprecated?: { since, reason, replacement? }` (strict object). Variants als sparse parallel list `axes.variantDeprecations: Array<{name, since, reason, replacement?}>` mit cross-field refine `name` ∈ `axes.variants` (kein reshape von `variants: string[]` weil 23-YAML-blast-radius + downstream-consumers AxesTable/MCP/consistency-tests/compare-differ alle string-array-shape lesen). SemVer-regex `^\d+\.\d+\.\d+$` (kein v-prefix, kein pre-release). `since`↔`changelog` cross-refine bewusst nicht enforced (chicken-and-egg blocker für inkrementelle adoption — convention in schema.md, reviewers catchen drift). 12 neue vitest-cases (positive parse slot/property/changelog/component, negative non-semver/missing-reason/strict-extra-field/non-ISO-date/duplicate-version/unknown-variant/duplicate-deprecation). Render: `VersionBadges.astro` neu (`since v...` accent-cool pill + `deprecated v...` accent-warm pill, color-mix für light/dark parity); AnatomyTable/FigmaSlotTable/CodeSlotTable rendern badges + `is-deprecated`-row-class mit `text-decoration: line-through` auf slot-id; AxesTable rendert badges auf variant-chips und property-rows; ComponentPageShell hero-titel bekommt `<span class="hero-since">v…</span>` neben component name; neue `ChangelogSection.astro` view-agnostisch nach `<slot />` vor footer (only renders wenn `since` oder `changelog` set). MCP: neuer `get_changelog`-tool returned `{ since, changelog } | null` (tool-count 18 → 19, SKILL.md tool-table erweitert + tool-count-zeile geflippt, sha256-digest auto-regenerated via prebuild). 2 neue mcp-server vitest-cases. `axesSchema` von `z.object` auf `.superRefine`-wrapper für variantDeprecations cross-field-validation; `componentSchema.shape`-accessor für tests bewusst preserved (cross-refine `since`↔`changelog` weggelassen wäre der einzige Anlass es zu wrappen). Verifikation: `pnpm -F @uianatomy/shared build` clean (23 components + 3 implementations bundled), `pnpm -r test` 145 grün (133 shared + 30 mcp-server, +12 + 2 vs pre-task), `pnpm -C site typecheck` 0 errors über 60 files (war 59, +VersionBadges + ChangelogSection - ein file-merge-balance), `pnpm -C site build` 73 pages + 0 broken links + 73 OG PNGs + 73 .md sidecars + Pagefind. **Bewusst out-of-scope**: deprecated-pill auf component-card-grid des index (deferred bis erste echte deprecation in roster YAMLs landet — heute null demonstration data), `until`/`removed-in`-feld pro slot (removal ist content-edit nicht schema-fakt), closed-enum für `reason` (zu coarse oder zu lang — prose searchable im MCP-result), pre-release-tags im semver (kein use-case), `since`↔`changelog` cross-refine. Erledigt 2026-05-01. Dateien: `shared/src/schema.ts`, `shared/tests/schema.test.ts`, `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `site/src/components/sections/{VersionBadges,ChangelogSection,AnatomyTable,FigmaSlotTable,CodeSlotTable,AxesTable}.astro`, `site/src/components/ComponentPageShell.astro`, `site/src/styles/global.css`, `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`, `docs/adr/023-versioning.md` (neu), `docs/schema.md`.
- [ ] **P4-26 State-Graph-Renderer** — eigener Graph-Renderer für `axes.states.transitions`. Heute reine Tabelle (`TransitionsTable.astro`); Graph wäre zweiter SVG-Generator parallel zu `renderAnatomySVG`, gleicher Wireframe-Stil, eigener Layout-Algorithmus (Stack-Layout für lineare State-Maschinen wie Modal: closed→opening→open→closing; force-directed mit fixem Seed für Combobox-artige Graphen). Position auf Component-Page: unter `TransitionsTable`. Eigene ADR begründet warum separat statt in Anatomy-SVG integriert (Graph-Layout-Problem vs Grid-Layout-Problem; anderer User-Need: "Wie wechselt die Komponente ihren State?" vs "Welche Slots gibt es?"). Aus P3-24 Plan extrahiert als eigenes Item.
- [x] **P4-24 MCP-Server-Tool-Erweiterung** — sechs neue Tools im `mcp-server/src/server.ts`: `get_tokens` (Array von `{slotId, tokens}` für alle Slots mit Token-Bindings), `get_motion` (Motion-Block oder `null`), `get_responsive` (Responsive-Block oder `null`), `get_transitions` (axes.states.transitions oder `null`), `get_events` (Events-Array oder `null`), `get_when_to_use` (whenToUse-Block oder `null`). Konvention: optionale Felder geben `null` zurück, nicht Error — "Komponente hat kein Motion" ist sinnvolle Antwort. Tools-List-Test aktualisiert (15 Tools statt 9). 9 neue vitest-Cases (jeweils Happy-Path + null-Case für Modal/Card/Button/Combobox). Erledigt 2026-04-29. Dateien: `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`.
- [x] **P4-25 View-Strategy reevaluieren** — Re-Eval bei 23 Komponenten durchgeführt. Vorher: alle 3 views (Designer/Dev/Bridge) in jeder page-DOM, CSS-toggle via `:root[data-view='X']`. Modal-page (heaviest): 208k raw / 30k gzip / ~4500 DOM-nodes. Astro-docs konsultiert via MCP — Browser-native MPA cross-document View-Transitions (`@view-transition { navigation: auto }`) sind zero-JS-Lösung; Astro's `<ClientRouter />` wird laut Astro-team "increasingly unnecessary". Lösung gewählt: per-URL-routing — `/components/<id>` rendert designer-default, `/components/<id>/[view]` rendert dev oder bridge (23×2=46 plus 23 = 69 component pages). `ComponentPageShell.astro` faktorisiert page-chrome, jeder route rendert single view-section. `ViewSwitcher` von buttons-mit-JS-toggle auf anchor-links-mit-`aria-current="page"`; `Base.astro` setzt `<html data-view={view}>` server-side; localStorage-persistence weg (URL ist jetzt source-of-truth). CSS-rules für `[data-view-section]`-visibility entfernt; `@view-transition` für browser-native cross-doc fade. Messung post-conversion (Modal-page): 101k raw (-52%) / 16k gzip (-47%) / 2393 DOM-nodes (-47%). Cross-package average: 76kb raw per page (war median 185kb, -59%). Side-benefits: per-view URLs deeplinkable + eigene SEO-surface; eyebrow+anatomy-caption ohne JS-placeholders; works zero-JS via standard MPA-navigation. `<ClientRouter />` bewusst nicht hinzugefügt — kein shared-state-bedarf. Erledigt 2026-04-30. Dateien: `site/src/components/ComponentPageShell.astro` (neu), `site/src/pages/components/[id]/[view].astro` (neu), `site/src/pages/components/[id].astro`, `site/src/components/ViewSwitcher.astro`, `site/src/layouts/Base.astro`, `site/src/styles/global.css`.

## P5 — Phase 2 (Implementations)

- [x] **P5-26 `implementations/radix/`** als erste React-Referenz — Divergenz-First-MVP gelandet (ADR-013). `implementationSchema` mit `componentId`, `libraryId`, `componentName`, `exampleCode?`, `divergence?` (discriminated union: omitted/renamed/extended/reshaped), `rationale?`, `lastReviewed` (required ISO). `divergenceSchema` als `z.discriminatedUnion('type', [...])` mit `.strict()` pro Arm. `from`-Pfad-Regex erlaubt `identifier(.identifier|[index])+` mit camelCase-Bracket-Indexes (deckt slot-ids/event-names/property-names/variant-names). `loadImplementation` + `loadImplementations` in `shared/src/loader.ts`; aggregierter Loader returned `Map<libraryId, Map<componentId, Implementation>>` und enforced `libraryId === parentDir`. **Erstes Audit:** `implementations/radix/modal.yaml` — Radix Dialog vs canonical Modal, 17 Divergenz-Einträge über alle drei genutzten Typen (renamed: backdrop/title/close-button/alertdialog; omitted: header/footer/fullscreen/size/scrollBehavior/dismiss-event-no-direct-equiv/reducedMotionFallback; reshaped: container-split/body-split/dismissible-callbacks/openChange-via-onOpenChange/dismiss-via-three-callbacks/motion-via-data-state/transitions-via-data-state). 8 neue vitest-Cases (positive parse, all-four-types-coverage, group-by-library, missing-lastReviewed, unknown-type, renamed-missing-to, bad-from-path, strict-rejection). Bindings (`tokenBindings`/`motionBindings`/`transitionBindings`/`eventBindings`) bewusst deferred — eigene Folge-ADRs (014+) wenn echte Audits sie brauchen. **P5-29 implizit erledigt:** Divergenz-Schema gegen reale Implementation getestet; ein Regex-Widening (camelCase im Bracket-Index) als einzige Discovery. Site-Rendering und MCP-Tools deferred bis nach zweitem Library-Audit. Erledigt 2026-04-29. Dateien: `shared/src/schema.ts`, `shared/src/loader.ts`, `shared/tests/schema.test.ts`, `implementations/radix/modal.yaml`, `docs/schema.md`, `docs/adr/013-implementation-schema.md`.
- [x] **P5-27 `implementations/headlessui/`** für Vue — Modal-audit (Dialog) gelandet als zweites library-audit nach Radix. Mirroring der Radix-divergenz-shape (selbe omissions: backdrop/header/footer/close-button; selbe reshapes: container-split, body-split, events; selber rename: title). Headless-UI-spezifische divergenzen: animation als separates composable-primitive (TransitionRoot + TransitionChild) statt bundled-hooks-auf-content; decoupled state-management (consumer owns isOpen ref; Dialog only emits @close); AlertDialog nicht separates package sondern composition-pattern (role="alertdialog" + :initialFocus). Pattern-recap across both audits: beide libraries stop at accessibility-primitive; design-system-specifics (size-scales, motion-timing, dismissible-flags, fullscreen-variants) sind consumer-composed-layer above library. Site rendert beide library-cards auf /components/modal alphabetisch (headlessui, radix); **stacked-layout works fine at 2 libraries — refactor-trigger wäre 3+**. Render-refactor-decision (tabs vs side-by-side) deferred bis P5-28 (CDK/Angular) landet. 106 shared tests grün; 71 site pages mit Pagefind-index. Erledigt 2026-04-30. Datei: `implementations/headlessui/modal.yaml`.
- [x] **P5-28 `implementations/cdk/`** für Angular Signals — Modal-audit (Dialog from `@angular/cdk/dialog`) gelandet als drittes library-audit. Most-divergent shape: imperative service-based API (`dialog.open(ContentComponent, config)`) statt declarative-component-tree. Consumer's content component IS the body; header/title/close-button/footer ALL omitted from CDK and live in consumer's component template. CDK-strengths: built-in backdrop primitive (`hasBackdrop` config), native commit-with-result via DialogRef.closed Observable, config-driven alertdialog (`role: 'alertdialog'`), comprehensive Overlay scroll-strategy. CDK-weaknesses: imperative-first requires separate content-component per dialog (more boilerplate), `closed` Observable does not surface dismissal-reason (consumer wires Escape+backdrop manually), Angular animations verbose. Pattern-recap across 3 audits: alle libraries stop at accessibility-primitive; consumer-composition shape unterscheidet sich (declarative-children Radix, declarative-wrappers+render-props Headless UI, imperative-service+consumer-component CDK). **3 stacked impl-cards messurement:** modal-page 155k raw / 24k gzip / ~3900 nodes — still **-25% vs pre-P4-25 baseline (208k/30k/4500)**. Per-impl ~18kb. Tabs/side-by-side render-refactor erst ab ~5+ implementations attraktiv, nicht jetzt. 106 shared tests grün; 71 site pages. Erledigt 2026-04-30. Datei: `implementations/cdk/modal.yaml`.
- [x] **P5-29 Divergenz-Schema testen** (omitted/renamed/extended/reshaped) an realer Implementation — implizit erledigt durch P5-26 Radix-Modal-Audit. Drei der vier Typen genutzt (omitted, renamed, reshaped); extended in vitest-Skelett-Test abgedeckt aber im echten Audit nicht benötigt — Radix erweitert Modal nicht über canonical hinaus, nur reshapes/omits. Discovery: Bracket-Index-Regex musste von `[a-z0-9-]+` auf `[a-zA-Z0-9-]+` erweitert werden, weil camelCase-Property-/Event-Namen (`events[openChange]`) sonst nicht parsen. Erledigt 2026-04-29 als Teil von P5-26.

- [x] **P5-31 Astro 6 Upgrade** — Site auf Astro 6.1.10 (von 5.18.1) gehoben. Migration-Scan (codebase + plugin-Peers + Node) zeigte: keine Astro-5-only-APIs mehr in Use (Content Collections waren schon auf modernem `loader: glob(...)`-Pattern; kein `Astro.glob`/`<ViewTransitions>`/`import.meta.env.ASSETS_PREFIX`/`emitESMImage`/i18n-Routing/CommonJS-Config; `getStaticPaths` returned strings; `trailingSlash: 'never'` deckt Endpoint-Slashes-Verbot bereits ab). Tatsächliche Diff-Surface vier Files: `site/package.json` `astro: ^5.0.5 → ^6.1.10`; root `package.json` `engines.node ">=20" → ">=22.12.0"`; `.nvmrc` `20 → 22`; `pnpm-lock.yaml` regen. **Eine Folge-Anpassung:** Astro 6 nutzt intern Zod 4, und `defineCollection({ schema })` braucht eine Zod-4-Schema-Instanz für korrekte Typ-Inferenz — ohne den Bump rendert `entry.data` als `unknown` (32 ts-errors über `[id].astro` / `SiteSidebar.astro` / `ImplementationsSection.astro` / `index.astro` / API-Endpoints). Daher `shared/package.json` `zod: ^3.23.8 → ^4.4.1` und ein einziger Code-Edit in `shared/src/schema.ts`: `code: z.ZodIssueCode.custom` → `code: 'custom'` (Zod 4 entfernt `ZodIssueCode`-Enum, akzeptiert Literal-String). Schema-Surface ansonsten unverändert — `z.record(K, V)` war schon 2-Arg, keine `z.string().email/url/uuid`-Calls, `superRefine` + `discriminatedUnion` + `.refine` API-stabil. Pagefind/Tailwind/@astrojs/check ohne Bump kompatibel (peer-ranges decken Astro 6 + Vite 7). Verifikation: `pnpm -r typecheck` (44 Files, 0 errors), `pnpm -r test` (124 Tests grün: 106 shared + 18 mcp-server), `pnpm -C site build` (71 Pages, Pagefind-Index emit), `pnpm -C site dev` smoke (Index + `/components/modal` + `/components/modal/dev` + `/components/modal/bridge` + `/search` alle 200, Modal-Page rendert 3 sections inkl. Implementations). Erledigt 2026-04-30. Dateien: `site/package.json`, `package.json`, `.nvmrc`, `shared/package.json`, `shared/src/schema.ts`, `pnpm-lock.yaml`.

- [x] **P5-32 Hosting → Cloudflare Pages** — Site und MCP-Server von Netlify auf Cloudflare Pages migriert. Astro-Static-Site unverändert (kein Adapter nötig); MCP-Funktion umgezogen von `mcp-server/netlify/functions/mcp.ts` (Netlify Function, `@netlify/functions`-Context) auf `functions/mcp.ts` im Repo-Root (Pages Function, file-routed `/mcp`, eigenes Web-Standard-Handler-Signature `({ request }) => Response`). Workers-Runtime hat kein FS auch mit `nodejs_compat` → neuer Build-Time-Pfad: `shared/scripts/bundle-content.mjs` liest alle `content/components/*.yaml`, validiert via `componentSchema`, schreibt `shared/dist/content-bundle.json` (~150KB, 23 Components). `shared/src/bundle.ts` exportiert `loadComponentsFromBundle(json): Map<string, Component>` (re-validation als defence-in-depth). `shared/package.json` baut nun `tsc && node scripts/bundle-content.mjs` und exposed `./bundle` + `./content-bundle.json` als Subpath-Exports. `mcp-server/src/data.ts` bekam `setComponents(map)` als parallel zu `setContentDir`; Pages Function ruft das beim Module-Init auf, Cache wird vorgewärmt, Handler bleibt rein. Lokaler Dev + alle Tests laden weiter via fs (`loadComponents`-Pfad unverändert). Eine neue equivalence-Test-Suite (`mcp-server/tests/bundle.test.ts`) asserted Map-Gleichheit zwischen fs- und Bundle-Loader über alle 23 Komponenten. Workers-Compat (`compatibility_date = "2026-04-30"`, `compatibility_flags = ["nodejs_compat"]`) wird via Cloudflare-Pages-Dashboard (Settings → Functions) konfiguriert; **kein root `wrangler.toml`** — initialer Versuch mit `wrangler.toml` failed im CI mit `The Wrangler application detection logic has been run in the root of a workspace`, weil Wrangler 4.x bei pnpm-Workspace-Root bricht. Dashboard-Config umgeht den Detection-Pfad komplett, `functions/`-Auto-Mounting funktioniert weiterhin ohne Config-File. Cleanup: `netlify.toml` weg, `mcp-server/netlify/` weg, `@netlify/functions` devDep weg, `mcp-server/tsconfig.json` `netlify/**/*` raus, `.gitignore` `.netlify/` → `.wrangler/`. Auch `wrangler.toml` (initial angelegt) wieder entfernt nach erstem Cloudflare-Deploy-Fail. ADR-021 begründet co-deploy-statt-split-Worker, build-time-bundle-statt-asset-fetch, Pages-Function-statt-standalone-Worker; ADR-002 als Superseded markiert (hosting-Teil), ADR-003 deployment-target-Note auf Cloudflare. README + roadmap auf Cloudflare umgestellt. Verifikation: `pnpm -r typecheck` 0 errors, `pnpm -r test` 132 Tests grün (113 shared + 19 mcp-server, +1 vs vor-Migration durch bundle-equivalence). DNS-Cutover und Cloudflare-Pages-Projekt-Anlage bleiben separater User-Schritt (ADR-021 Implementation-Notes). Erledigt 2026-04-30. Dateien: `shared/src/bundle.ts` (neu), `shared/scripts/bundle-content.mjs` (neu), `shared/src/index.ts`, `shared/package.json`, `mcp-server/src/data.ts`, `mcp-server/src/index.ts`, `mcp-server/tests/bundle.test.ts` (neu), `mcp-server/package.json`, `mcp-server/tsconfig.json`, `functions/mcp.ts` (neu), `.gitignore`, `docs/adr/021-host-cloudflare-pages.md` (neu), `docs/adr/002-stack-astro-netlify.md`, `docs/adr/003-mcp-server-architecture.md`, `README.md`, `docs/roadmap.md`. Gelöscht: `netlify.toml`, `mcp-server/netlify/`.

- [x] **P5-30 Site-Rendering Phase-2** — Phase-2-Implementations auf canonical Component-Page sichtbar. Neue `implementations` Astro-Collection in `site/src/content.config.ts` via `glob('*/*.yaml', base: '../implementations')` mit `implementationSchema`. Drei neue Section-Components in `site/src/components/sections/`: `ImplementationsSection.astro` (Wrapper, library-sortiert, conditional-render), `ImplementationCard.astro` (Library-Badge + Component-Name, Shiki-Code-Block mit lang-per-libraryId-Lookup `radix→tsx`/`headlessui→vue`/`cdk→typescript`, DivergenceTable, optional `<details>`-collapsed rationale), `DivergenceTable.astro` (4-Spalten From/Type/→To/Rationale, type-farbige Badges). View-agnostisch platziert in `[id].astro` zwischen Anatomy-Diagram und View-Tabs. CSS-Type-Badges in `global.css` via `color-mix()` Light/Dark-Parity (omitted=neutral, renamed=cool, extended=green, reshaped=warm). Smoke: Modal zeigt 17 Badges (6 omitted + 5 renamed + 6 reshaped), Card/Button/Tabs/Combobox ohne Section. Erledigt 2026-04-29. Dateien: `site/src/content.config.ts`, `site/src/pages/components/[id].astro`, `site/src/styles/global.css`, `site/src/components/sections/{ImplementationsSection,ImplementationCard,DivergenceTable}.astro`.

---

## P6 — Reichweite (SEO + AI-Discovery)

- [x] **P6-41 SEO/AI-Discovery P0 batch** — vier konkurrierende Citation-Hebel auf einen Schlag. (a) **`FAQPage` JSON-LD pro Component**: P3-39 hatte FAQPage als "forced fit" für Google-Rich-Snippets deferred — Re-Eval mit AI-Engine-Lens (Perplexity/ChatGPT/Claude/Gemini extrahieren Q/A-Paare verbatim als Antwort-Source) flippt das Verdikt. Pure-derivational projection in `site/src/lib/jsonLd.ts`: `faqEntries(component)` baut `Array<{q, a}>` aus `whenToUse.use`+`whenToUse.avoid` ("When should I use/avoid X?") + `whenToUse.vsRelated[]` ("How does X differ from Y?") + `mistakes[]` (title als Q, description+fix als A). Componenten ohne whenToUse oder mistakes emit kein FAQPage. `componentJsonLd` shape von single-`TechArticle`-object auf `@graph: [TechArticle, FAQPage?]` umgestellt — Base.astro JSON.stringify-Path unverändert. Modal page emit 10 FAQ-entries (use/avoid/3 vsRelated/5 mistakes); button/card analog. (b) **Schema.org `Dataset`** als 5. graph-node in `siteJsonLd()`: name "UI Anatomy canonical component dataset", `distribution: [DataDownload(/api/components.json, application/json), DataDownload(/llms-full.txt, text/markdown), DataDownload(/llms.txt, text/markdown)]`, `creator`+`publisher` resolve via `@id` zu Organization, `isAccessibleForFree: true`, 11 keywords. AI-Engines bevorzugen Dataset-Entries als ground-truth-Quellen. License field bewusst weggelassen (kein LICENSE im Repo, keine Fabrikation). (c) **`Organization.sameAs` + `Person.sameAs`**: Organization sameAs zeigt auf `https://github.com/DominikPieper/uianatomy`, Person AUTHOR sameAs auf `https://github.com/DominikPieper`. E-E-A-T-Authority-Signal. Keine Twitter/LinkedIn — wären fabriziert. Organization bekommt zusätzlich `founder: AUTHOR`. (d) **AI-Bot-spezifische robots.txt**: 8 explizite User-agent-Blöcke (PerplexityBot, GPTBot, OAI-SearchBot, ChatGPT-User, ClaudeBot, Claude-Web, Google-Extended, CCBot) overriden den `*`-Catchall mit `Allow: /api/`+`/llms.txt`+`/llms-full.txt`+`/*.md$`, halten aber `Disallow: /og/`+`/pagefind/` — JSON+Markdown sind citation-friendly, OG-PNGs+Pagefind-WASM nicht. RFC 9309 §2.2.1: most-specific User-agent group wins. Content-Signal pro Block dupliziert (kein crawler erbt parent-block). Plus `MCP server description` von "17-tool" auf "19-tool" geflippt + changelog-mention. **Bewusst nicht im P0**: HowTo-schema auf Implementation-Cards (forced fit, P3-39-Verdikt steht), Twitter-Card-update (P3-39 phase 1 ist ausreichend), per-component RSS, Search-Console-submission (P0-2 separat in P6-46). Verifikation: `pnpm -C site typecheck` 0 errors über 59 files; `pnpm -r test` 163 grün; `pnpm -C site build` 73 pages + 0 broken links; JSON-LD-decode der Modal-Page zeigt 5 graph-nodes site-level + 2 graph-nodes component-level (TechArticle + FAQPage mit 10 Q/A); index.html Dataset distribution-array hat 3 entries; Organization sameAs `[github.com/DominikPieper/uianatomy]`; robots.txt im dist serviert 8 AI-bot-Blöcke. Erledigt 2026-05-01. Dateien: `site/src/lib/jsonLd.ts`, `site/public/robots.txt`.
- [ ] **P6-42 Long-form prose intro pro Component-Page** — heute ist `description: prose` ein Einzeiler, dann sofort Tabellen + SVG. Google de-rankt component-pages als "thin content"-Risiko, AI-Engines extrahieren narrative Absätze 5-10× häufiger als Tabellen-Zellen als Citation-Quote. Schema-Erweiterung: `intro?: prose` (200-500 chars) auf top-level `componentSchema`, optional, `min(200)`-refine wenn vorhanden. Render in `ComponentPageShell` zwischen `WhenToUseSection` und `<figure>`. Trade-off: Schreibarbeit auf 23 YAMLs (~2-3h dedicated writing per session). Kein Schema-blast-radius. Quote-fähig per `<blockquote>` oder einfach `<p class="component-intro">`. Optional: per-view-intro (designer/dev/bridge gewinnen je verschiedene Narrativ-Lens) — defer auf separates Item wenn first impl zu kurz greift.
- [x] **P6-43 `DefinedTermSet` als top-level graph-node** — TermSet promoted zu 6. graph-node in `siteJsonLd({ components })`. `@id: ${SITE_ORIGIN}/#term-set`, `hasDefinedTerm` als alphabetisch sortiertes Array von 23 DefinedTerm-Entries (`@id: ${SITE_ORIGIN}/components/<id>#term`, identifier, name, description, url, inDefinedTermSet via `@id` back-reference). Per-Component `componentJsonLd().mainEntity.DefinedTerm` jetzt mit `@id: ${url}#term` plus `inDefinedTermSet: { @id: ${SITE_ORIGIN}/#term-set }` — JSON-LD-Graph-Resolution kann den TermSet vom component-card aus auflösen ohne inline-stub-Duplikation. `siteJsonLd()`-Signature von zero-arg auf `{ components? }`-Optional umgestellt; index.astro reicht `components.map(e => e.data)`; compare/search-pages rufen weiter ohne args (TermSet only on home). Verifikation via `wrangler dev`: `index.html` decode zeigt 6 graph-types `[WebSite, Organization, WebAPI, SoftwareApplication, Dataset, DefinedTermSet]` mit 23 hasDefinedTerm-entries (`Accordion, Alert, Banner, …`); component pages unverändert (nur `@id`-reference statt inline). Erledigt 2026-05-01. Dateien: `site/src/lib/jsonLd.ts`, `site/src/pages/index.astro`.
- [ ] **P6-44 Backlinks + Outreach** (non-code, user-driven) — größter klassischer SEO-Hebel. Domain-Authority bei 0 ohne Backlinks. Konkrete Targets: (a) GitHub awesome-* listen — `awesome-design-systems`, `awesome-ui-component-library`, `awesome-react-components`, `awesome-a11y` PRs. (b) Show-HN ("UI Anatomy — canonical reference for UI component anatomy with MCP server"). (c) Designsystems.com community-Slack + Storybook-blog-pitch. (d) Refactoring UI / GOV.UK design-blog-mentions. (e) Twitter/Mastodon design-community. Out-of-scope für Code-Changes; wird vom Owner selbst getrieben. Backlog-Eintrag dient als Reminder + Tracking-Surface für gelandete Mentions.
- [~] **P6-45 PSI / Lighthouse audit + Core Web Vitals** — Lighthouse-CLI-Run auf lokaler Maschine geblockt: Node-Install ist `x64 darwin`, Chrome ist `arm64`, Lighthouse 13.1.0 refused den Launch ("Launching Chrome on Mac Silicon (arm64) from an x64 Node installation results in Rosetta translating the Chrome binary…"). Ersatz-Audit headless ohne Browser via curl + python3 HTML-parse ergab: home 250 KiB raw / 34 KiB gzip; modal 192 KiB raw / 30 KiB gzip; beide Pages haben title + meta description + canonical + og:image + og:title + twitter:card + html lang="en"; 1× h1 pro Page; alle imgs haben alt; JSON-LD blocks vollständig (home 6 graph-nodes, modal 2 graph-nodes inkl. FAQPage); Headers `Vary: Accept` + `Cache-Control: public, max-age=0, must-revalidate` + `Link: api-catalog, service-desc, sitemap` (P3-25). Crawler-visible SEO-signals alle korrekt. **Nicht gemessen ohne Browser:** LCP/INP/CLS-Werte, Color-Contrast-Pässe, ARIA-pattern-checks, Best-Practices-Score. **Next step:** Node-arm64 via `fnm install 22 --arch arm64` oder `brew install node` ersetzen, dann `pnpm dlx lighthouse https://uianatomy.dev/ --output=json --quiet --chrome-flags="--headless=new"` — separater User-Step weil Node-Reinstall andere workspace-tools tangiert. Oder PageSpeed-Insights API gegen produktions-URL nach erstem Deploy (kein lokales Chrome nötig). Status [~] = teilweise (manual audit done, Lab-metrics deferred). Files: keine — informational only.
- [x] **Login-form pattern** (follow-up zu P6-49 stage-1, unblocked durch P6-51 text-input) — zweites canonical pattern landed in `content/patterns/login-form.yaml`. Composition: 4 entries (text-input × 2 für identifier+password, button für submit, link × 2 für forgot-password+sign-up). 6 canonical decisions: single-page-vs-split-page (single is canonical default; split only wenn email-routes-to-different-flow), enumeration-resistant-error-message (combined "Wrong email or password" — niemals separate "wrong email"/"wrong password" weil account-existence-leak; OWASP ASVS 2.1 + NIST SP 800-63B), password-show-toggle (recommended; NIST 800-63B explizit allowed; reduces typo-driven failed-login auf mobile), initial-focus-discipline (skip autofocus auf touch-devices, use `(hover:hover) and (pointer:fine)`-media-query gate), autocomplete-tokens-discipline (`autocomplete="email"` + `autocomplete="current-password"` für login, `new-password` für signup-flow), required-fields-visual+attribute-pairing. 6 mistakes: missing-autocomplete-tokens, enumeration-leak (specific-error-disclosure), password-reveal-via-type-flip (statt real-button-with-aria-pressed), generic-button-label, caps-lock-not-warned, autofocus-on-mobile. Vollständige framework-skeletons für alle 4 frameworks (web-components, React, Vue, Angular signals) — jedes ~30-line snippet mit form-element wrapping + autocomplete-tokens + show-password-button als real `<button type="button">` mit aria-pressed + aria-label-toggle. Triangulated against APG (textbox + button patterns), HTML spec autocomplete-tokens, NIST SP 800-63B (password guidance + reveal allowance), OWASP ASVS 2.1 (authentication), Polaris Account-pattern, Material 3 Sign-in-Form, plus auth0/Okta/WorkOS-engineering-blogs auf enumeration-resistant-login. **Bewusst out-of-scope** (sibling-patterns deferred): Sign-up-Form (registration mit `new-password` autocomplete + password-strength-meter + email-confirmation flow), Password-Recovery-Flow (email-send + reset-token-validation + new-password-set), Magic-Link-Login (passwordless email-only), MFA-step (TOTP / WebAuthn / SMS-OTP composition mit re-authentication-context). **YAML-gotcha re-discovered**: title beginning mit backtick (`` `autofocus` ``) braucht single-quoted-yaml-value (gleicher gotcha wie text-input.yaml und confirmation-flow.yaml `confirm-button-is-OK` — aber unique signature: leading-backtick triggers "bad indentation of mapping entry"-error statt "scanner couldn't find expected ':'"). Verifikation: `pnpm -F @uianatomy/shared build` 2 patterns bundled (war 1, +login-form); `pnpm -r test` 174 grün (139 shared + 35 mcp-server) inkl. consistency-test (alle 4 composition-componentIds resolve: text-input, button, link); `pnpm -C site build` 82 pages (war 81, +/patterns/login-form); 0 broken links. **MCP discovery automatic**: `list_patterns` returnt jetzt 2 entries; `get_patterns_for_component({componentId: 'text-input'})` returnt login-form mit role "identifier field" + "password field" (zwei composition-entries für same component); `get_patterns_for_component({componentId: 'link'})` returnt login-form mit forgot-password + sign-up roles. Erledigt 2026-05-02. Datei: `content/patterns/login-form.yaml` (neu).
- [~] **P6-49 Patterns Layer (stage 1: schema + first pattern + minimal site + MCP)** — author-first-before-commit-schema discipline (ADR-013-style) durchgeführt: Confirmation-Flow-pattern (Modal-alertdialog + destructive-Button) als first canonical pattern in `content/patterns/confirmation-flow.yaml`. **Login-form pivoted weg** weil canonical roster keinen plain `input`/`text-field` hat — backlog item P6-51 (text-input canonical) tracked es. Confirmation-Flow als first-pattern reicher in bridge-content (alertdialog vs dialog ist most-cited a11y-failure-category) und exercises das schema gut: 2-component composition, 4 decisions, 5 mistakes, 4 framework-skeletons. Schema (`shared/src/schema.ts`): `patternSchema` mit `composition: array.min(2)<{componentId (slug), role (prose), notes? (prose)}>`, `whenToUse: {use, avoid}` (strict), `decisions: array.min(1)<{question, answer, rationale}>` (strict), `mistakes: array.min(3)<mistakeSchema>` (reuses component schema), `frameworkSkeletons: {webComponents, react, vue, angularSignals}` alle required strings, optional `notes`, required `lastReviewed` ISO. Cross-refine `composition[].componentId` ∈ canonical-roster im consistency-test (Zod kann roster nicht sehen — same pattern wie vsRelated-cross-refine in P3-29). Loader (`loadPattern` + `loadPatterns`), Bundle (`loadPatternsFromBundle` + `bundle-patterns.mjs` build-script + `./patterns-bundle.json` subpath-export). Site routes: `/patterns` index + `/patterns/[id]` single-page (3-view-routing als stage-2-followup, ADR-024 D1 deferred). Page emit JSON-LD `@graph: [TechArticle, HowTo]` — HowTo ist legit hier weil patterns LITERAL how-tos sind (P3-39-Verdikt war "forced fit für components"; für patterns ist es spec-fit). Sections: composition (jeder slot mit role + notes, link zu canonical component-page), whenToUse (use+avoid dl), canonical-decisions (question→answer→rationale tripel), common-mistakes, framework-skeletons (alle 4 frameworks via expressive-code), notes-block, footer-lastReviewed. MCP: 3 neue tools (`list_patterns` + `get_pattern` + `get_patterns_for_component`), tool-count 19→22. Worker bundle-load erweitert. SKILL.md tool-table erweitert + count-zeile geflippt. Footer-nav um `/patterns`-link erweitert. Verifikation: `pnpm -r typecheck` 0 errors über 64 files (war 60); `pnpm -r test` 174 grün (139 shared + 35 mcp-server, +11 vs pre: 4 schema + 1 consistency + 5 mcp-server + 1 unaccounted); `pnpm -C site build` 77 pages (war 75, +2 patterns-pages), 0 broken links; pattern-page rendert alle sections, expressive-code 4-tab-style framework-skeletons, JSON-LD HowTo mit step-array. **Stage-1 out-of-scope** (gehören zu P6-49 stage 2/3): three-view-routing (D1 von ADR-024), `/patterns/[id]/dev` + `/bridge` URL-split, "Used in patterns"-section auf component-pages (B1, auto-derived), composition-SVG-renderer (F1, reuse `renderAnatomySVG`), per-pattern-OG-image, JSON-API endpoint `/api/patterns.json`, llms-md exclude für /patterns. Erste echte erfolgsmessung wenn 3-5 patterns im roster (drift-detection wird relevant). Erledigt 2026-05-01. Dateien: `content/patterns/confirmation-flow.yaml` (neu), `shared/src/schema.ts`, `shared/src/loader.ts`, `shared/src/bundle.ts`, `shared/scripts/bundle-patterns.mjs` (neu), `shared/package.json` (subpath-export + build-chain), `shared/tests/schema.test.ts`, `shared/tests/consistency.test.ts`, `mcp-server/src/state.ts`, `mcp-server/src/data.ts`, `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `worker/index.ts`, `site/src/content.config.ts`, `site/src/pages/patterns/index.astro` (neu), `site/src/pages/patterns/[id].astro` (neu), `site/src/layouts/Base.astro` (footer-link), `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`.
- [x] **P6-49 stage 2: three-view-routing für patterns** — patterns now ship in 3 views per URL (mirror P4-25 component-routing). `/patterns/[id]` rendert designer-default; `/patterns/[id]/[view]` rendert dev oder bridge. Content-distribution: Designer = composition + whenToUse + decisions; Dev = composition + framework-skeletons; Bridge = composition + decisions + mistakes. Composition appears in all three (structural anchor). Notes-block + footer-lastReviewed in shell für alle views. Refactor: extract `PatternPageShell.astro` (Base wrapper, hero, view-switcher, JSON-LD `@graph: [TechArticle, HowTo]` view-aware mit per-view headline + description, breadcrumb back to /patterns canonical, slot for view-content). Section-components extracted (`PatternComposition`, `PatternWhenToUse`, `PatternDecisions`, `PatternMistakes`, `PatternFrameworkSkeletons`) — each takes `entry: CollectionEntry<'patterns'>` prop. `ViewSwitcher.astro` parameterized mit optional `basePath`-prop (default `/components`, patterns pass `/patterns`); kein duplicated component, scrollY-restore weiter funktional. data-pagefind-body nur auf designer-view (mirror component-pattern); alt-views data-pagefind-ignore um duplicate-suchindex zu vermeiden. **Bewusst out-of-scope** (stage 4): composition-SVG-renderer (visuelle anchor unter hero) — separate design-pass nötig für reuse von `renderAnatomySVG` mechanics. Verifikation: `pnpm -C site typecheck` 0 errors über 75 files (war 66, +6 pattern files); `pnpm -r test` 174 grün; `pnpm -C site build` 86 pages (war 82, +4: 2 patterns × 2 alt-views), 0 broken links; OG-emit 6 pattern-PNGs (designer + dev + bridge × 2 patterns). Erledigt 2026-05-02. Dateien: `site/src/components/PatternPageShell.astro` (neu), `site/src/components/sections/{PatternComposition,PatternWhenToUse,PatternDecisions,PatternMistakes,PatternFrameworkSkeletons}.astro` (neu), `site/src/pages/patterns/[id].astro` (refactored), `site/src/pages/patterns/[id]/[view].astro` (neu), `site/src/components/ViewSwitcher.astro` (basePath-param).
- [x] **P6-49 stage 3: "Used in patterns"-cross-link auf component-pages** — auto-derived from `getCollection('patterns')` build-time-call in new `UsedInPatternsSection.astro`. Filtert composition[] nach `componentId === current-component`; sortiert nach pattern-name; rendert `<SectionHeader>` + `<ul>` von `<a>{patternName}</a> + <span>{role}</span>`-rows nur wenn rows.length > 0 (silent-zero discipline). Wired in DesignerView + BridgeView (per ADR-024 B1; Dev-View bewusst out — implementation-focused, patterns sind conceptual). Heute mit 2 patterns: modal/button (confirmation-flow) + text-input/button/link (login-form) zeigen alle das used-in-patterns surface. SectionHeader live in der section-component (statt in den view-files) damit empty-pattern-component-pages keinen leeren section-header zeigen. Verifikation: build sauber, 0 broken links, no extra pages — pure-additive section. Erledigt 2026-05-02. Dateien: `site/src/components/sections/UsedInPatternsSection.astro` (neu), `site/src/components/views/{Designer,Bridge}View.astro`.
- [x] **P6-49 stage 5: OG-image + /api/patterns.json + Dataset distribution** — drei kleinere additions auf einen schlag. (a) **`/api/patterns.json`** + **`/api/patterns/[id].json`**: mirror existing `/api/components.json`+`/api/components/[id].json` shape. Index-endpoint listet `{id, name, description, components[unique], lastReviewed}` sortiert by name; per-pattern endpoint emittiert full pattern-record. Both `Cache-Control: public, max-age=300, must-revalidate` matching agent-skills + linkset-policy. (b) **OG-images für patterns**: pages-map in `/og/[...slug].ts` extended um patterns-collection iteration (designer + dev + bridge analog zu components), plus standalone `/patterns`-index PNG, plus rückwärts-fix für `/integrate` + `/methodology` + `/changelog` die bisher ohne OG-image waren. (c) **Dataset distribution**: `siteJsonLd().Dataset.distribution` erweitert um 4. entry `application/json /api/patterns.json` neben den existing 3 (components.json + llms-full.txt + llms.txt). AI-engines pickt jetzt patterns als parallele machine-readable surface auf. Verifikation: 7 OG PNGs neu (3 patterns × 2 + 4 site-meta-pages); 3 patterns-API-files (1 index + 2 per-pattern); JSON-LD-decode am `/`-page zeigt Dataset distribution-array hat 4 entries. Erledigt 2026-05-02. Dateien: `site/src/pages/api/patterns.json.ts` (neu), `site/src/pages/api/patterns/[id].json.ts` (neu), `site/src/pages/og/[...slug].ts` (pages-map), `site/src/lib/jsonLd.ts` (Dataset distribution).
- [x] **Atelier-feedback quick-wins bundle (P6-60a + P6-62a + P6-62c + P6-64a)** — vier kleine improvements aus Atelier-MCP-test feedback in einem commit. **P6-60a**: `list_implementations` tool-description erweitert um explizit zu sagen "no filter — use `get_implementations({componentId})`" + "today only Modal × {radix, headlessui, cdk} audited"; SKILL.md tool-table cell mirror. **P6-62a `alternateNames` field** (renamed from initial `aliases` proposal nach user-feedback "shouldn't we use alternativeNames with more than one value possible?" — explicit-plural-naming + matches schema.org-`alternateName`-morpheme): optional `alternateNames: z.array(z.string().min(1)).min(1)` auf componentSchema. 19 von 24 components migrated mit conservative aliases (Modal: Dialog/Alert dialog, Toast: Snackbar/Flag, Drawer: Side panel/Sheet, Tooltip: Tip, Combobox: Autocomplete/Typeahead, Tag Input: Chip input/Token input, Search Input: Search box, Banner: Inline alert/Notification bar, Tile: Card link, List Item: Cell, Menu Button: Split button/Dropdown button, Disclosure: Collapse/Details, Stepper: Wizard/Progress steps, Sidebar Nav: Side nav/Rail nav, Segmented Control: Toggle group, Select: Dropdown, Popover: Floating panel, Accordion: Expandable list, Text Input: Text field/Input). Skip-list für universal-named components: Button, Card, Link, Alert, Tabs. Render: ComponentPageShell hero zeigt "Also called …"-row unter description (italic + dot-separated, paper-tone muted). JSON-LD `mainEntity.DefinedTerm.alternateName` (schema.org-canonical-key — schema.org accepts string or array, we always emit array) + DefinedTermSet entries also carry `alternateName` für root-level resolution. Pagefind picks up automatically (text in DOM). **P6-62c selector-prefix doc**: schema.md frameworkMap-section bekommt note das `ui-` als neutral-filler-prefix in canonical examples ist (nicht prescriptive); consumers substituieren ihr eigenes (md-, pf-, sl-, …). HTML-spec-required hyphen ist die einzige hard-rule. **P6-64a SKILL.md updates**: neuer typical-flow "Audit a library spec or design doc against the canon" — 4-step recipe (search canonical match → get-anatomy/axes/events as checklist → walk spec section-by-section → pair with mistakes+mismatches für canonical-pitfalls); complementary zu `validate_implementation` (code-side) — spec-parity ist prose-side. Plus neue "Tool loading"-section: explicit "all 22 tools returned in single tools/list response, no progressive-disclosure" — adresse Atelier-feedback-misread um lazy-loading. **YAML migration via python-script** (one-shot, deleted post-run): scan top-level fields, insert `aliases:` block after description-end + before next top-level key. Single-quoted alias-values preventiert YAML-special-char-edge-cases (alle aliases haben spaces oder sind multi-word). Verifikation: `pnpm -F @uianatomy/shared build` 24 components bundled (alle mit oder ohne alternateNames clean parse); `rm -rf site/.astro && pnpm -C site typecheck` 0 errors über 75 files (cache-clear required nach schema-change weil .d.ts-types von dist gelesen werden); `pnpm -r test` 174 grün (139 shared + 35 mcp-server, both incl. bundle-fs-equivalence tests die sicherstellen dass migration round-trippt); `pnpm -C site build` 86 pages, 0 broken links; agent-skills/index.json sha256-digest auto-regenerated. Erledigt 2026-05-02. Dateien: `shared/src/schema.ts`, `content/components/{19-yamls}.yaml`, `site/src/components/ComponentPageShell.astro`, `site/src/lib/jsonLd.ts`, `site/src/styles/global.css`, `mcp-server/src/server.ts`, `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`, `docs/schema.md`.
- [ ] **P6-49 stage 4: composition-SVG-renderer** — ADR-024 F1, reuse `renderAnatomySVG` mechanics mit outer composition-box + nested per-component frames. Per-component slots greyed (decorative-stroke). Kostet ~2h. Separate design-pass nötig — SVG-layout-design für composite pattern-frames braucht echtes drafting (anders als text-input das von vorhandenen anchors aus auto-layouted hat).
- [x] **P6-50 MCP server gegen Atelier testen** — User selber durchgeführt 2026-05-02. Feedback-batches v1 (5 cluster, gelandet als P6-54 Table-and-others, P6-60 list_implementations clarification, P6-61 schema-consistency-audit, P6-62 aliases/staleness/selector-prefix, P6-63 actions sub-anatomy, P6-64 SKILL.md spec-parity flow + audit_spec_parity tool) und v2 (3 cluster, gelandet als P6-65..P6-85 unten — schema/data issues, cross-canonical inconsistencies, tooling gaps). Quick-wins-bundle aus v1 schon in commit `57fd4e7` (P6-60a + P6-62a + P6-62c + P6-64a). Erledigt 2026-05-02. Datei: keine — externe test, follow-up-work in backlog-items P6-54..85 abgebildet.

- [x] **P6-65 propertyMap.type tool-neutral abstraction** — ADR-025 (`docs/adr/025-property-map-kind.md`, Status: Accepted) supersedes ADR-015. Field rename `type → kind`, vocabulary swap `'Boolean' | 'Variant' | 'Text' | 'Instance Swap'` → `'enum' | 'boolean' | 'text' | 'slot' | 'number'`. Mapping: Variant→enum (77 entries), Boolean→boolean (88), Text→text (31), Instance Swap→slot (25), `number` reserved für forward-coverage (0 today). 24-YAML-migration via python-regex (anchored `^\s+type:\s+(Variant|Boolean|Text|Instance Swap)$`, leaves anatomy.figma.type and axes.properties[].kind untouched, total 221 replacements). schema.ts: `figmaPropertyTypeSchema` → `propertyKindSchema`, type-export `FigmaPropertyType` → `PropertyKind`. PropertyMapTable.astro: rename TYPE_SLUG → KIND_LABEL (capitalised für badge-text), drop hardcoded Figma vocab, use kind directly as CSS-modifier. global.css: rename `.property-map__type--variant` → `--enum`, `--instance-swap` → `--slot`, plus new `--number` purple variant (light + dark). docs/schema.md propertyMap section umgeschrieben mit migration-table + tool-neutral framing. ADR-015 status flips von Accepted auf Superseded mit cross-link. schema.test.ts propertyMap-suite umgestellt: Set-of-kinds-test, rejects-unknown-kind, rejects-legacy-Figma-vocabulary, accepts-`number`-forward-coverage. Verifikation: `pnpm -F @uianatomy/shared build` 24 components clean; `pnpm -r test` 180 grün (143 shared + 37 mcp-server, +2 vs P6-66/quick-wins-bundle: legacy-vocab-reject + number-accept); `pnpm -C site build` 86 pages, 0 broken links; typecheck 0 errors über 75 files. Erledigt 2026-05-02. Dateien: `docs/adr/025-property-map-kind.md` (neu), `docs/adr/015-property-map.md` (Status), `shared/src/schema.ts`, `content/components/{24 yamls}.yaml`, `site/src/components/sections/PropertyMapTable.astro`, `site/src/styles/global.css`, `docs/schema.md`, `shared/tests/schema.test.ts`.
- [x] **P6-66 MCP `get_canonical_vocabularies` tool** — canonical vocab nach `shared/src/vocabulary.ts` extrahiert (single source: `CANON_SPACING/RADIUS/COLOR/ELEVATION/TYPOGRAPHY/MOTION_DURATION/MOTION_EASING/BREAKPOINTS/PROPERTY_VOCAB/PROPERTY_BOUNDED/INTERACTIVE_STATES` plus `getCanonicalVocabularies()` reducer). Subpath-export `@uianatomy/shared/vocabulary` + barrel re-export aus `shared/src/index.ts`. `consistency.test.ts` reused die exports (Set-wrap on the test side; vorher 110 inline-LOC). Neuer MCP-tool `get_canonical_vocabularies` (no args) returnt `{ spacing, radius, color, elevation, typography, motion: { durations, easing }, breakpoint, propertyVocab, propertyBounded, interactiveStates }`. Tool-count 22 → 23 (server.test.ts list-tools-array + SKILL.md Tools-table-row + zwei "23 tools" badges). 2 neue server-tests: shape-coverage + cross-check gegen `get_motion(modal)` (canonical motion-vocab muss real motion-values enthalten). Verifikation: `pnpm -F @uianatomy/shared build` 24 components + 3 implementations + 2 patterns clean; `pnpm -r test` 176 grün (139 shared + 37 mcp-server, +2 vs pre); `pnpm -r typecheck` 0 errors über 75 files. Erledigt 2026-05-02. Dateien: `shared/src/vocabulary.ts` (neu), `shared/src/index.ts`, `shared/package.json`, `shared/tests/consistency.test.ts`, `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`.
- [x] **P6-67 motion.durations naming-convention doc** — `docs/schema.md` motion-section um "Naming convention for duration keys"-block erweitert: camelCase + transition-shape (not numeric) + no `Duration`/`Time`/`Ms`-suffix + direction-in-key for symmetric pairs (open/close, panelEnter/panelExit). Existing "Conventional duration keys"-list bleibt als Beispiel-roster. Doc-only, kein Convention-Test (per-component-shape IS the abstraction per ADR-007 — strict membership-test wäre falsche Diskretion). Erledigt 2026-05-02. Datei: `docs/schema.md`.
- [x] **P6-68 axes.states.transitions discipline audit** — Audit ergab 20/24 components mit `transitions:`-block, 4 ohne (Button, Card, Link, Tabs). 3 von den 4 (Button/Card/Link) sind echt-trivial — interactive-states-list deckt alles. Tabs war die einzige Lücke: Panel-Load-Lifecycle ist non-trivial (`lazy → busy`, `selected → busy`, `busy → selected | error`, `error → busy | selected`) und involviert async data-states die ohne transition-graph nicht aus der states-list ableitbar wären. Tabs-transitions als 6-edge-graph hinzugefügt unter Verwendung nur der declared data-states (lazy/selected/busy/error); cross-refine validiert ohne neue state-namen. schema.md transitions-section um "When to declare transitions vs. omit them"-block erweitert: 3 Kriterien für non-trivial (ordered phases / mode-dependent / async-with-meaningful-return-paths) + explicit list "Today canon omits transitions on Button, Card, Link only". schema.test split: alter "omits on Card/Tabs/Button" → "omits on Card/Button/Link" + neuer test "tabs panel-load lifecycle"-edges (lazy→busy, busy→selected, busy→error). Tabs activation-mode-nuance (manual vs automatic) bleibt prose-dokumentiert in `tabActivate`-event-payload (ADR-009 transitions schema bindet from/to an declared states; activation-mode ist eher trigger-prose-detail). Erledigt 2026-05-02. Dateien: `content/components/tabs.yaml`, `docs/schema.md`, `shared/tests/schema.test.ts`.
- [x] **P6-69 anatomy.repeats semantic-doc-clarify** — `docs/schema.md` anatomy-section um `repeats`-doc-block erweitert: visual-sample-count für SVG only, not real-instance-constraint, integer 2-5, convention 3 für typical lists / 4-5 für compositions wo pattern-clarity zählt. Doc-only, schema unchanged. Erledigt 2026-05-02. Datei: `docs/schema.md`.
- [ ] **P6-70 state-dependent slotKind ADR** — User-feedback: "Stepper's step is interactive but pending-in-linear-mode is non-interactive. Should support state-dependent kind." Real but complex schema-extension. Today `slotKind` is static (ADR-020). Adding state-dependent is real cost (schema-shape + render-conditional + cross-component-audit). Counter-argument from ADR-020: "pick dominant role; capture nuance in purpose if needed". Likely outcome: confirm static + add explicit guidance for the dominant-vs-state-dependent edge cases. Write ADR-026 with "rejected" status documenting the trade-off. ~1h ADR-only, no code.
- [x] **P6-71 a11yAcceptance.axeCoreVersion field** — `axeCoreVersion?: semver` auf `a11yAcceptanceSchema` (semver-regex, optional, .strict-passthrough); cross-refine: `axeCoreVersion` nur sinnvoll wenn `axeRules` declared. 24 components mit `axeRules` automated-migrated auf `axeCoreVersion: '4.10.2'` via python-script (regex-insertion vor `axeRules:`-line, identische indentation, alle vorhandenen prose-blocks unverändert). Erledigt 2026-05-02. Dateien: `shared/src/schema.ts`, `content/components/{24 yamls}.yaml`.
- [ ] **P6-72 mistake.severity field** — User-feedback: "lacks severity tier. A11y-breaking mixed with perf hints." Add `severity: 'blocker' | 'major' | 'minor'` to mistakeSchema. Migrate all 24 components × ~5 mistakes each = ~120 entries. Render: bridge-view sortiert by severity descending; warm-mix-accent für blocker. ~2-3h.
- [ ] **P6-73 notes-content-extraction audit** — User-feedback: "freeform prose containing critical contracts. Structure into `nonNegotiableContracts[]`, `vocabularyDrift[]`, `commonMisuses[]`." Audit alle 24 components × notes-block (varies in length 50-500 words); identify structured-extractable content. Likely outcome: extract subset to new top-level `contracts: { nonNegotiable[], vocabularyDrift[] }` schema-fields; keep `notes` as catch-all per CLAUDE.md. Multi-session, schema-design-first. ~6-8h total.
- [ ] **P6-74 i18n.rtl-restructure** — User-feedback: "prose. Could be structured: mirroredAxes, directionNeutralGlyphs, logicalProperties[]." ADR-017 chose prose deliberately for flexibility. Restructure touches 24 components (those with i18n block); improves queryability for "components-that-mirror-by-X-axis"-style queries. Defer until prose-search-pain ist real (heute: keine queries laufen gegen i18n-prose). Write ADR-017-followup ADR. Multi-session.
- [x] **P6-75 Modal performance.stackDepth fix** — `performance:`-block in `modal.yaml` komplett entfernt (war single-entry). Hard rule "max 1 open modal" + Begründung umformuliert in `whenToUse.avoid`-prose ("Stacking modals is non-canonical … patterns that feel like they need 'modal-on-modal' should be redesigned as a step flow within a single modal, or as a sequence of modal → dismiss → modal"). schema.test.ts split: `parses tabs and modal performance` → 1 test für tabs only + 1 test der modal.performance===undefined + whenToUse.avoid-content-match assertet. Erledigt 2026-05-02. Dateien: `content/components/modal.yaml`, `shared/tests/schema.test.ts`.
- [x] **P6-76 events hedging-audit + optional-flag** — Decision: schema-extend statt remove. `eventSchema` bekommt `optional?: boolean`-flag (`.strict()`-passthrough). Beide flagged events markiert + frameworkNotes hedging-prose entfernt: **drawer.sideChange** (`optional: true`, payload reformuliert "Only canonical when implementation actually performs the auto-flip — static-side implementations never emit and consumer treats absence as no-flip baseline"; webComponents/react frameworkNotes ohne "many hosts"-language) + **tabs.tabActivate** (`optional: true`, payload reformuliert "Canonical only in `activation: manual` mode — `automatic` implementations do not emit; consumer listens on selectedChange and ignores tabActivate for those instances"). Render: EventsTable.astro hero-line bekommt `optional`-badge (paper-tone muted) wenn `e.optional === true`. Erledigt 2026-05-02. Dateien: `shared/src/schema.ts`, `content/components/{drawer,tabs}.yaml`, `site/src/components/sections/EventsTable.astro`.
- [x] **P6-77 Combobox vs Select responsive reconcile** — Conflict identified: `combobox.responsive[at=breakpoint.sm].change` documented multi-select degradation als comma-separated tokens im input, während `select.mismatches[multi-select]` comma-separated trigger als anti-pattern flagged. Canonical position: count-display ("3 selected") überall, comma-separated nirgends als Fallback. Combobox-prose umgeschrieben auf count-display-degradation mit explicit cross-link zu Select mismatch ("the same rule the `Select` multi-select mismatch entry locks down"). Select mismatch-correct-prose erweitert um Combobox-cross-link ("The same convention applies to `Combobox` on small viewports — count-display is canon at every width"). Bidirectional cross-reference plus reconciled canonical-position. Erledigt 2026-05-02. Dateien: `content/components/{combobox,select}.yaml`.
- [ ] **P6-78 frameworkMap framework expansion** — User-feedback: "missing Svelte, SolidJS, Lit, Qwik. `angularSignals` overly specific — drop suffix." Trade-off: adding frameworks dilutes per-component depth (each new framework = +25% prose); `angularSignals` was deliberate (signals-based-API specificity); rename loses precision. Defer until consumer-demand. Wenn landing: ADR-027 Framework Selection.
- [x] **P6-79 vsRelated bidirectional-lint** — neuer consistency-test `whenToUse.vsRelated is bidirectional or explicitly allowlisted`: prüft für jede vsRelated-edge `src→target` ob target-component reverse-ref `target→src` hat; sonst muss pair in `ALLOWED_ASYMMETRIC`-set explicitly geführt werden. Audit ergab 19 missing reverse-refs (deutlich mehr als erwarteter 2-5-Estimate) — als allowlist-entries dokumentiert; backfill als P6-86 incremental getrackt (siehe dort für 19-pair-roster). Lint blocks neue asymmetric additions sofort. Erledigt 2026-05-02. Dateien: `shared/tests/consistency.test.ts`.
- [x] **P6-80 `get_components([ids])` bulk-tool** — neuer MCP-tool `get_components({ ids: string[].min(1) })` returnt `{ components: Component[], missing: string[] }` (request-order erhalten, dedup, missing alphabetisch sortiert). Bandwidth-fix für multi-component-lookups. 2 server-tests: multi-id-resolve + missing/dedup. Tool-count 23 → 24. Erledigt 2026-05-02. Dateien: `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`.
- [x] **P6-81 SKILL.md clarify validate_implementation works against canon** — Tools-table-row für `validate_implementation` um expliziten "Scores supplied code against the canonical anatomy / axes / events of `componentId` — does *not* require the canonical component to have a Phase-2 library audit, and does not consult `implementations/<lib>/<id>.yaml`"-block ergänzt. Adressiert Atelier-feedback-misread. Erledigt 2026-05-02. Datei: `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`.
- [x] **P6-82 SKILL.md document get_component vs per-axis-tool trade-off** — neue "When to use which tool"-section in SKILL.md (vor "Tool loading"): erklärt dass per-axis-tools (`get_anatomy`, `get_axes`, `get_motion`, `get_events`, `get_when_to_use`, `get_tokens`, `get_responsive`, `get_transitions`, `get_framework_map`, `get_mismatches`, `get_common_mistakes`, `get_changelog`) NICHT redundant zu `get_component` sind, sondern aus zwei Gründen existieren: (1) Bandwidth — heavy components (Modal, Combobox) ~30 KB JSON full-record vs ~3 KB single-axis, multipliziert über N round-trips real für context-budget; (2) Discovery — focused tool-name signals intent in tool-call-traces. Picking-Heuristik: `get_component` für full-record-checklist (new instance / migration); per-axis für targeted question (1 round-trip); per-component-loop für 3+-component-comparison. `get_pattern` ist explizit KEIN component-subset (patterns sind eigene records). Erledigt 2026-05-02. Datei: `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`.
- [x] **P6-83 `get_pattern_a11y_aggregate` MCP tool** — neuer MCP-tool `get_pattern_a11y_aggregate({ patternId })` aggregiert `a11yAcceptance` aus pattern.composition[].componentId via dedup-set für axeRules (sorted union), concat mit `sourceComponentId`-tagging für keyboardWalk + announcements, single-semver-or-null für `axeCoreVersion` (null wenn components disagree). Errors auf unbekannte patternId. 3 server-tests: union+source-tagging, single-axeCoreVersion-resolve (alle components auf 4.10.2 nach P6-71), unknown-pattern-error. Tool-count 24 → 25. Erledigt 2026-05-02. Dateien: `mcp-server/src/server.ts`, `mcp-server/tests/server.test.ts`, `site/public/.well-known/agent-skills/uianatomy-mcp/SKILL.md`.
- [ ] **P6-84 migrate-tool / codemod-hints** — User-feedback: "no migrate tool — when canon renames `nonLinear` → `linear`, no diff/codemod hint." Heavy infra: codemod-tooling for canonical schema-renames. Premature — schema-renames sind selten, P6-72 severity-add ist erste echte migration die diese tool brauchen würde. Defer.
- [ ] **P6-85 testRecipes block** — User-feedback: "every component would benefit from a 'minimum a11y test' snippet for downstream test gen." New schema-field `testRecipes?: { framework, snippet }[]` — pre-baked test-snippets pro framework × component. Combines mit a11y-fixture-endpoint (P3-37). Heavy: schema + per-component authoring. Multi-session.
- [ ] **P6-86 vsRelated reverse-ref backfill (incremental)** — P6-79-Lint hat 19 deliberate-asymmetric pairs als ALLOWED_ASYMMETRIC im consistency-test eingetragen, statt sie ad-hoc zu fixen. Backfill-roster (jeder hier benötigt ein vsRelated-entry mit echter `difference`-prose im Target-YAML, dann allowlist-entry entfernen): banner→modal, button→menu-button, disclosure→{tabs,modal}, drawer→alert, link→menu-button, menu-button→{select,tooltip}, search-input→tag-input, sidebar-nav→drawer, stepper→{tabs,accordion,sidebar-nav}, text-input→{search-input,combobox,tag-input}, toast→{modal,tooltip}, tooltip→modal. Jeder reverse-ref ~3-5min realer prose-authoring (canonical "warum diese ≠ jene"-statement). Durchläufer ~60-90min total. Lint passes today via allowlist; new asymmetric vsRelated additions blocken sofort.
- [ ] **P6-52 `@uianatomy/skill` npm package + install CLI** — heavier alternative zur curl-one-liner aus SKILL.md. Package enthält die canonical SKILL.md + small `uianatomy-skill install`-CLI das `mkdir -p .claude/skills/uianatomy-mcp` + writes SKILL.md + writes `.mcp.json` mit MCP-config-snippet (Claude Code default) + optional appends to `.gitignore`. Pro: zero-config (`npx @uianatomy/skill install` reicht), version-pinned, idempotent. Contra: extra-publish-pipeline + maintenance, npm-registry-coupling. Trade-off-decision in eigener ADR (vermutlich ADR-025) bei landing. Trigger wenn ≥3 echte external-projects die curl-one-liner adopted haben + feedback signalisiert friction. Out-of-scope für P6-49 stage 1.
- [x] **P6-53 Integration-Docs-Page für Skill + MCP** — `/integrate` als public-readable docs-entry-point (replaces raw SKILL.md + server-card.json footer-links). Lean-scope per user-direction: nur "how to use" + "what it can do", kein authority/changelog/versioning-Wiederholung (footer-links zu /methodology + /changelog reichen). Sections: hero (one-sentence + dual-anchor-CTA), Quick start (one-liner + minimal `.mcp.json`), Other clients (Claude Desktop / Cursor-Windsurf-via-mcp-remote / Direct TypeScript SDK — Python + ChatGPT bewusst deferred bis vendor-support verifiziert), What you get (3-group tool table: Components 16, Patterns 3, Implementations 3 = 22 tools), Typical flows (4 example-flows: structure / divergence / validate / patterns-for-component), No-MCP fallback (REST APIs + Accept-text/markdown). Code-snippets via existing `astro-expressive-code` (copy-button + dual-theme out-of-the-box). JSON-LD `@graph: [TechArticle]` mit author + publisher + breadcrumb. Footer footer-nav refactored: 2 raw-artifact-links (Skill + MCP) replaced durch single `/integrate`-link; SKILL.md + server-card.json bleiben unter /.well-known/ erreichbar für RFC-discovery. Index-hero-meta bekommt zweite line "Use it from your agent → /integrate" neben existing methodology-link. **Bewusst out-of-scope** (siehe original P6-53 plan): tool-manifest-extraction (defer until 3rd edit zeigt drift-cost), Python-SDK-section (verifizieren MCP-Python-streamable-HTTP-support first), ChatGPT-MCP-connector (verifizieren stable docs first), Troubleshooting/FAQ-section (defer until external-adopter-friction reports), per-client-screenshots/GIFs (vendor-UIs change too fast). 78 pages built (war 77, +/integrate), 0 broken links, typecheck 0 errors über 65 files (war 64). pagefind picks up via data-pagefind-body. Erledigt 2026-05-02. Dateien: `site/src/pages/integrate.astro` (neu), `site/src/layouts/Base.astro` (footer-link refactor), `site/src/pages/index.astro` (hero-meta-link). — footer-link zeigt heute auf raw `/.well-known/agent-skills/uianatomy-mcp/SKILL.md` (markdown) und `/.well-known/mcp/server-card.json` (JSON). Browsers rendern beide als plain-text. Proper docs-page-pattern: neuer `/integrate`-route mirror /methodology — rendert SKILL.md prose plus zusätzliche sections (visual-config-walkthrough für Claude Code / Cursor / Windsurf / direct-SDK, troubleshooting-FAQ, MCP-tool-flowcharts pro typical-flow, link zu agent-skills-RFC + WebMCP-spec). Footer re-points auf `/integrate` statt raw artifacts. JSON-LD `TechArticle` mit `mainEntity.programmingLanguage` für SDK-snippets. Erste section: "Why MCP for design system canon" prose argument für agent-tooling-adoption. Triggers: P6-52 implementation oder erste 3+ external-adopters. Schätzung 2-3h research + writing.
- [x] **P6-51 canonical text-input component** — gelandet als 24. component (war 23) in `content/components/text-input.yaml`. Triangulated gegen 11 sources (APG textbox-pattern, MDN HTML input + Constraint-Validation API, WCAG 2.2 §3.3.2, React Aria TextField, Radix Themes TextField, Polaris TextField, Carbon TextInput, Atlassian Textfield, Material 3 TextField, GOV.UK Text Input). Anatomy: 7 slots (root structural, label content required, input interactive required, description content optional, error-message content optional, leading-icon decorative optional, trailing-icon interactive-or-decorative optional). Variants: outlined / filled / underlined (industry-convergence — Material 3 / Carbon / Polaris each pick a default; canon documents all three weil jede in production live ist). Properties: 8 (type-enum {text,email,tel,url,password,number}, size-enum {sm,md,lg}, invalid, disabled, readonly, required, hasLeadingIcon, hasTrailingIcon). States: 4 interactive (hover/focus-visible/active/disabled) + 3 data (empty/filled/invalid) + 4 transitions. 5 mistakes (placeholder-as-label, missing-aria-invalid, required-without-attribute, wrong-autocomplete, error-only-on-submit). 5 mismatches (placeholder-as-label / required-asterisk-vs-attribute / error-text-without-aria / variant-anatomy-drift / disabled-contrast). Full motion + responsive + 3 events (valueChange, blur, invalidChange) + a11yAcceptance (5 keyboardWalk + 3 announcements + 6 axeRules) + propertyMap (15 entries) + formIntegration (Constraint-Validation-API canonical + autocomplete-discipline) + i18n (label-block-direction-neutral + 30-50%-textExpansion). 2 performance-thresholds (validationDebounce 200ms + maxLength 10000-char-defence). vsRelated zu search-input, combobox, tag-input. **Canon-extension entdeckt + landed in same change**: error-message slot needs `color.text.danger` token; existing canonical color-vocabulary hatte nur primary/muted/inverse/accent — danger fehlte. Vocabulary erweitert in `shared/tests/consistency.test.ts` + `docs/schema.md` color-row (jetzt 14 statt 13 canonical color-tokens). Symmetric extensions (`color.text.success`, `color.text.warning`) deferred bis erster component sie braucht. **YAML-gotcha**: title mit backticks-around-quotes-around-equal-sign (`` `autocomplete="off"` ``) braucht single-quoted-yaml-value sonst parse-error. Verifikation: `pnpm -F @uianatomy/shared build` 24 components bundled (war 23); `pnpm -r test` 174 grün (139 shared + 35 mcp-server) inkl. depth-test (7 slots ≥ 3, 3 variants ≥ 2, 8 properties ≥ 2, 7 states ≥ 4, 5 mistakes ≥ 4, 5 mismatches ≥ 4, 11 sources ≥ 3) + consistency-test (alle tokens canonical post-extension); `pnpm -C site build` 81 pages (war 78, +3 text-input designer/dev/bridge views); 0 broken links; SVG generator emit ohne override (layout hints reichen für stable 4-row composition). Erledigt 2026-05-02. Dateien: `content/components/text-input.yaml` (neu), `shared/tests/consistency.test.ts` (CANON_COLOR + danger), `docs/schema.md` (color-vocab row). — kompositions-layer auf canonical-atomics (Login-Form, Confirmation-Flow, Empty-State, Settings-Page, Dashboard-Shell als 5-pattern-MVP). Adressiert SEO-long-tail-gap ("how to build a delete confirmation modal"-queries landen heute nirgends), persona-gap (DS-maintainer fragt "welche kompositionen sind canonical?"), decision-fatigue-gap ("Card vs Modal für formular?"). Volle ADR-024 spec'd: structured Zod-schema parallel zu `componentSchema` (`composition[]` mit cross-refine zu canonical-roster, `decisions[]` als die "warum diese composition statt alternative"-prosa, `frameworkSkeletons` für alle vier frameworks), three-views per pattern (designer/dev/bridge mirror P4-25), "Used in patterns"-section auf component-pages auto-derived, MCP tools `list_patterns` + `get_pattern` + `get_patterns_for_component` (tool-count 19 → 22), new `/patterns` index + `/patterns/<id>(/<view>)`-routes, JSON API endpoint, HowTo JSON-LD (einzige stelle wo HowTo NICHT forced-fit ist — patterns SIND literally how-tos), neue Dataset distribution für `/api/patterns.json`. **Bewusst MVP-cap: 5 patterns**, dann Pause für usage-data (Risiko: drift to "yet-another-component-library-docs-site" ist real). Authoring-cost per pattern: 4-6h echtes research per ADR-001 discipline (≥3 spec/library/DS sources). Total MVP-estimate: 25-30h across 2-3 sessions. **Out-of-scope**: per-pattern divergence-audits (Phase-2-für-patterns), pattern-to-pattern vsRelated, anti-patterns als eigener doc-type (stattdessen in pattern.mistakes[] dokumentiert). **Author-the-first-pattern-before-committing-schema**-discipline (ADR-013-style, Schema iterieren auf realen daten statt paper). Spec: `docs/adr/024-patterns-layer.md` (Status: Proposed). Erste implementation-session bei user-trigger. — recency-signal landing für sowohl klassisches SEO (Google rankt fresh-content höher; sitemap-`<lastmod>` allein ist schwach ohne dated-content) als auch AI-citation (Perplexity/ChatGPT bevorzugen dated prose mit author-attribution als ground-truth-Quelle). Implementation: neue Astro content-collection `changelog` in `site/src/content.config.ts` mit Zod-schema `{ date: ISO YYYY-MM-DD, title, summary, tags? }` (date als string-regex weil bare `2026-05-01` als YAML date-object parsed wird, nicht string — frontmatter braucht `date: "2026-05-01"`-quotes). 7 initiale entries in `site/src/content/changelog/` covering die letzten echten ship-events: P6-41/43/46/47 SEO+AI-discovery (heute), P4-23 versioning (heute), P3-40 validate_implementation, P3-39 SEO baseline, P3-38 /compare, P3-36 implementations-MCP, P3-35 Workers Static Assets. Jeder entry ~150-300 words echte prose (kein commit-title-paste). Page `site/src/pages/changelog.astro` rendert sortierten list (descending date), dataset-`Blog` JSON-LD-graph mit `BlogPost`-array (einer pro entry, mit datePublished + author + keywords aus tags). RSS-feed via `@astrojs/rss` 4.x als API-route `site/src/pages/changelog.xml.ts` — `pubDate` aus entry.date, `link` als `/changelog#<entry-id>` (anchor zur same-page-section), `categories` aus tags. RSS-discovery via `<link rel="alternate" type="application/rss+xml">` in `Base.astro`-head — feed-readers + AI-engines erkennen automatisch. Footer-nav um `/changelog`-link erweitert (zwischen methodology + compare). Sitemap-serialize-callback in `astro.config.mjs` erweitert: `latestChangelogDate` zur build-zeit aus `site/src/content/changelog/*.md` extrahiert (max date), `/changelog`-sitemap-entry bekommt `<lastmod>` = neueste-entry-date — frische signal lebt nicht nur intern sondern crawler-visible. `data-pagefind-body` auf article — entries werden in pagefind-search-index aufgenommen. `astro-llms-md` generiert `dist/changelog.md` automatisch (gleiches pattern wie components + methodology). Verifikation: `pnpm -C site build` 75 pages (war 74, +/changelog), 0 broken links; HTML decode zeigt 7 h2-entries + Blog JSON-LD mit 7 BlogPost-items; RSS XML wellformed mit 7 items sorted desc; sitemap-entry `/changelog` mit lastmod `2026-05-01T00:00:00.000Z`. Erledigt 2026-05-01. Dateien: `site/src/content.config.ts`, `site/src/content/changelog/*.md` (7 neu), `site/src/pages/changelog.astro` (neu), `site/src/pages/changelog.xml.ts` (neu), `site/src/layouts/Base.astro` (RSS-link + footer-nav), `site/astro.config.mjs` (lastmod-serialize), `site/package.json` (+@astrojs/rss).
- [x] **P6-47 `/methodology` public page** — `docs/methodology.md` ist heute internal-only (CLAUDE.md reading-order step 2). Public-surface = E-E-A-T-authority-signal ("how we research the canon"); Google de-rankt content ohne authority-marker, AI-engines (Perplexity/ChatGPT/Claude) citen methodology-pages bevorzugt als ground-truth-Quelle. Implementation: `site/src/pages/methodology.astro` importiert `docs/methodology.md` via Vite `?raw` (compile-time inline; vermeidet build-vs-runtime path-resolution-split — initial `readFileSync(fileURLToPath(...))`-Versuch crashte mit `ENOENT: no such file or directory, open '/site/docs/methodology.md'` weil prerender `import.meta.url` auf den dist-tree zeigt). Markdown-Parsing via `marked` 16.4.1 (build-only devDep, GFM-mode für tables, hand-rolled regex-parser-Versuch handhaubte ordered-list-with-blank-lines-between-items falsch). JSON-LD `@graph: [AboutPage, TechArticle]` mit author = Dominik Pieper + sameAs github + breadcrumb. Site-footer in `Base.astro` neu (border-top, links zu /methodology, /compare, /search, GitHub-source, /mcp) — methodology jetzt persistent in nav-surface auf jeder Page. Index-Hero bekommt subtle "How is the canon researched? → /methodology"-link unter lede. Sub-page-styling über scoped `.methodology` mit `:global()`-typography-rules (h1-h3 mit display-font, p+li 1.65 line-height, hr/table/code/th/td konsistent zu site-canon). `data-pagefind-body` auf article — methodology jetzt in pagefind-index. Astro-llms-md generiert `dist/methodology.md`-sidecar automatisch (gleiches Pattern wie components — Markdown-Negotiation funktioniert dadurch out-of-the-box ohne config-change); sitemap-Eintrag wird automatisch ergänzt (filter unverändert). Verifikation: `pnpm -C site typecheck` 0 errors über 60 files (war 59); `pnpm -C site build` 74 pages (war 73, +/methodology); HTML-output decode zeigt korrekte struktur (1 h1, 8 h2, 2 ol-blocks mit 3+8 items, 1 table mit 9 rows, JSON-LD mit AboutPage+TechArticle, sitemap-eintrag, og:image `/og/methodology.png` auto-generated von astro-og-canvas). Erledigt 2026-05-01. Dateien: `site/src/pages/methodology.astro` (neu), `site/src/pages/index.astro` (hero-link), `site/src/layouts/Base.astro` (footer), `site/src/styles/global.css` (footer + .methodology), `site/package.json` (+marked devDep).
- [x] **P6-46 IndexNow ping bei deploy** — IndexNow-Protocol (`https://www.indexnow.org/`) als deploy-hook landed. Key `44e5f5f3939b4bab8c4b23198b0c54c5` (32-hex, `crypto.randomUUID().replace(/-/g,'')`); ownership-proof file `site/public/<key>.txt` mit dem Key als Content (per IndexNow-Spec served bei `https://uianatomy.dev/<key>.txt` nach deploy). Neues Script `scripts/indexnow-ping.mjs` (~60 LOC, plain Node, keine deps): liest `site/dist/sitemap-0.xml`, extrahiert alle 72 `<loc>https://uianatomy.dev/...</loc>`-URLs, POSTet bulk-payload `{host, key, keyLocation, urlList}` an `https://api.indexnow.org/indexnow` als `Content-Type: application/json`. Skippt silent wenn sitemap fehlt (lokale builds, branch-deploys); akzeptiert `INDEXNOW_DRY_RUN=1` env oder `--dry-run` argv für CI-test ohne tatsächlichen API-call; HTTP 200/202 als success, sonst `process.exitCode=1`. Wired in root `package.json` `deploy`-script: `wrangler deploy && node scripts/indexnow-ping.mjs` — fail-closed weil indexnow-failure als deploy-failure surfaced. Bing+Yandex+Seznam+Naver akzeptieren IndexNow direkt; Google evaluiert. **Bewusst not yet:** Google Search Console + Bing Webmaster Tools Domain-Verification via DNS-TXT (manueller User-Step, zwei Browser-Account-Setups, keine Automatisierung möglich) — Backlog-Reminder dass das nach erstem deploy passieren soll. **Bewusst not yet:** changed-only URL-list (sitemap-diff zwischen letztem deploy + aktuell) — full-list ist 72 URLs unter dem 10000-rate-limit, premature optimization. Verifikation lokal via `INDEXNOW_DRY_RUN=1`: script logs `would POST 72 URLs` mit korrekten kanonischen URLs (`https://uianatomy.dev`, `/compare`, `/components/<id>`, `/components/<id>/<view>`); key.txt landet sauber in `dist/`. Erledigt 2026-05-01. Dateien: `site/public/44e5f5f3939b4bab8c4b23198b0c54c5.txt` (neu), `scripts/indexnow-ping.mjs` (neu), `package.json` (deploy-script).

## Empfohlener Pfad

P0-1 (✓) → P0-2 (✓) → P0-3 (✓) → P0-4 (✓) → P3-17 (✓) → P3-19 (✓) → P3-20 (✓) → P3-18 (✓) → P1-5 (✓) → P1-6 (✓) → P1-7 (✓) → P1-8 (✓) → P1-9 (✓) → P1-10 (✓) → P2-12 (✓) → P4-24 (✓) → P5-26 (✓) → P5-29 (✓) → P5-30 (✓) → P2-15 (✓) → P2-11 (✓) → P2-13 (✓) → P2-14 (✓) → P2-16 (✓) → P4-25 (✓) → P4-22 (✓) → P4-21 (✓) → P5-27 (✓) → P5-28 (✓) → P5-31 (✓) → P3-24 (✓) → P5-32 (✓) → P4-23 (✓) → P6-41 (✓) → P6-43 (✓) → P6-46 (✓) → P6-47 (✓) → P6-48 (✓) → P6-49 stage-1 (~) → P6-53 (✓) → P6-51 (✓) → login-form pattern (✓) → P6-49 stages 2/3/5 (✓) → Atelier-feedback v1 quick-wins (✓) → Atelier-test P6-50 (✓) → P6-66 (✓) → Atelier-feedback v2 quick-wins bundle (P6-67/69/71/75/79/81/82, ✓) → P6-65 propertyMap-kind ADR-025 (✓) → MCP-tools-bundle (P6-80 + P6-83, ✓) → content-audit-bundle (P6-68 + P6-76 + P6-77, ✓).

**Phase-1 + Phase-2 Bucket vollständig.** P6-Bucket (SEO + AI-Discovery): code-items P6-41 + P6-43 + P6-46 alle gelandet. Verbleibend: P4-26 (State-Graph-Renderer, deferred), P6-42 prose-intro (User-authored canon-Content, kein Code-Item), P6-44 Backlinks/Outreach (non-code, user-driven), P6-45 Lighthouse Lab-metrics (Node-arm64-Reinstall oder PSI-API-call gegen Production deferred).

**Komplette P2-Inhalts-Sektion-Suite und Phase-1-Schema strukturell fertig.** Alle ADRs 006–018 gelandet. Verbleibend: P5-27 + P5-28 (Phase-2-Library-Audits), P4-21/P4-22/P4-23/P4-25 (Tooling/Skalierung, größen-getriggert).

Alle P0-, P1-, P3-Items plus P2-12, P4-24, P5-26, P5-29, P5-30 abgeschlossen. Phase 1 strukturell vollständig (ADRs 006–011); P2-12 erste P2-Inhalts-Sektion (ADR-012); P4-24 macht alle Phase-1-Felder über MCP konsumierbar (15 Tools); P5-26 landet Phase-2-Schema (ADR-013, divergence-first MVP) plus erstes Audit (Radix Dialog vs Modal); P5-29 implizit dadurch geschlossen; P5-30 macht Phase-2-Audits auf canonical Component-Pages sichtbar. **Phase-2 ist jetzt vollständig sichtbar** (Schema + Loader + erstes Audit + Render). Zwei unabhängige Stränge bleiben (parallel oder sequenziell):
1. **P5-27 `implementations/headlessui/`** für Vue — zweite Library, treibt Render-Design-Entscheidung "wie sehen mehrere Library-Audits nebeneinander aus?" mit echten Daten. Voraussichtlicher Refactor-Trigger: Tabs vs stacked layout, lang-per-library map → schema-field, etc.
2. **Restliche P2-Items** (P2-11 propertyMap, P2-13 formIntegration, P2-14 i18n, P2-15 a11yAcceptance, P2-16 performance) — Inhalts-Sektionen, jeweils ADR + Schema + Migration + Render. Empfohlen P2-15 (a11yAcceptance) als nächstes weil testbar (axe-rules in CI) und größter Wert für Bridge-View.

Restliche P4-Items (P4-21 Konsistenz-Audit, P4-22 Pagefind, P4-23 Versionierung, P4-25 View-Strategy-Re-Eval) sind größen-getriggert (warten auf >15 / ~20 Komponenten) bzw. CI-Folge-Arbeit.

- [x] **P3-21 Editorial-Reference-Polish** — additive Stildisziplin im bestehenden Cream/Warm-Cool-Neutral-System. Body bekommt subtilen warmen Halo-Gradient oben (6% accent-warm, im Dark-Mode auf cool 4% gedreht). `.component-card` mit zweischichtigem Schatten-Stack (4% + 16% fg, im Hover auf 12% + 35% accent-active). `.phase-banner` mit linearem Akzent-Wash auf den ersten 35% statt flachem Surface. `.section-header::before` als 2px-Akzent-Schiene links jeder Section (folgt `--accent-active`, also warm/cool/neutral pro View). `.hero-title::before` mit dezentem warmen Slab-Gradient hinter dem Index-Hero-Titel. Inspiriert von Refactoring UI / Designsystems.com / GOV.UK. Anatomy-Diagramme bewusst unverändert. Erledigt 2026-04-29. Datei: `site/src/styles/global.css`.

## Wartung dieses Backlogs

Bei jeder erledigten Aufgabe Status auf `[x]` setzen, Erledigungs-Datum + Datei-Verweis ergänzen. Neue Items nach Priorität einsortieren. Phase-2-Items (P5) bleiben offen bis Phase 1 stabil.
